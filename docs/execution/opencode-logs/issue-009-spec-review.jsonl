{"type":"step_start","timestamp":1783897837374,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5898cf3c001Ig4lkZxYk2LhHS","messageID":"msg_f5898c6de001d15dRXL42stTuI","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","type":"step-start"}}
{"type":"text","timestamp":1783897842833,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5898dd010015moONXegdbQv72","messageID":"msg_f5898c6de001d15dRXL42stTuI","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","type":"text","text":"I’ll establish the issue’s exact requirements and branch delta first, then trace each acceptance criterion to implementation and tests without modifying the worktree.","time":{"start":1783897840897,"end":1783897842805},"metadata":{"azure":{"itemId":"msg_0c74edd636192079016a541ef2f1f08190829236a7563333d4","phase":"commentary"}}}}
{"type":"tool_use","timestamp":1783897842849,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_aRsjfS5RFrL0voAHoOyqkyCW","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/AGENTS.md"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/AGENTS.md</path>\n<type>file</type>\n<content>\n1: # Zentra Agent Instructions\n2: \n3: ## Source Of Truth\n4: \n5: - Read `docs/design/orchestrator.md` before changing architecture.\n6: - Read `docs/plans/mvp.md` before implementing the MVP.\n7: - Treat `docs/context/zoe-client.md` as the client boundary between Zentra and Zoe.\n8: - The imported Zoe ticket-governance document is a reference, not permission to copy Zoe-specific paths into Zentra.\n9: \n10: ## Engineering Rules\n11: \n12: - Use Node.js 24 or newer and pnpm 10.\n13: - Follow test-driven development for behavioral changes.\n14: - Invoke subprocesses with executable and argument arrays and `shell: false`.\n15: - Pass explicit minimal environments to workers and validations.\n16: - Do not expose a general shell capability.\n17: - Keep the event journal as the source of truth and projections rebuildable.\n18: - Use only `completed`, `cancelled`, `denied`, `timed_out`, or `failed` as terminal outcomes.\n19: - Never automatically retry a potentially effectful operation after an uncertain result.\n20: - Keep implementation work off `main` and use isolated worktrees.\n21: - Do not commit, push, or file issues unless the user explicitly requests it.\n22: - Put each full sentence on its own physical line in long Markdown documents.\n23: \n24: ## Scope Discipline\n25: \n26: - Complete the deterministic local tracer bullet before adding real coding harnesses.\n27: - Do not add distributed execution, personal workflows, email, meetings, or devices to the MVP.\n28: - Do not introduce plugin APIs until the core contracts are exercised end to end.\n29: - Prefer one measurable vertical outcome over unused horizontal infrastructure.\n30: \n31: ## Security Boundary\n32: \n33: - Agent reasoning never grants execution authority.\n34: - Installation never grants authority.\n35: - Workers and reviewers must not inherit arbitrary parent secrets.\n36: - File writes must remain inside the assigned worktree and reviewed relative paths.\n37: - Integration must validate a disposable candidate before updating the integration branch.\n\n(End of file - total 37 lines)\n</content>","metadata":{"preview":"# Zentra Agent Instructions\n\n## Source Of Truth\n\n- Read `docs/design/orchestrator.md` before changing architecture.\n- Read `docs/plans/mvp.md` before implementing the MVP.\n- Treat `docs/context/zoe-client.md` as the client boundary between Zentra and Zoe.\n- The imported Zoe ticket-governance document is a reference, not permission to copy Zoe-specific paths into Zentra.\n\n## Engineering Rules\n\n- Use Node.js 24 or newer and pnpm 10.\n- Follow test-driven development for behavioral changes.\n- Invoke subprocesses with executable and argument arrays and `shell: false`.\n- Pass explicit minimal environments to workers and validations.\n- Do not expose a general shell capability.\n- Keep the event journal as the source of truth and projections rebuildable.\n- Use only `completed`, `cancelled`, `denied`, `timed_out`, or `failed` as terminal outcomes.\n- Never automatically retry a potentially effectful operation after an uncertain result.\n- Keep implementation work off `main` and use isolated worktrees.","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/AGENTS.md","text":"# Zentra Agent Instructions\n\n## Source Of Truth\n\n- Read `docs/design/orchestrator.md` before changing architecture.\n- Read `docs/plans/mvp.md` before implementing the MVP.\n- Treat `docs/context/zoe-client.md` as the client boundary between Zentra and Zoe.\n- The imported Zoe ticket-governance document is a reference, not permission to copy Zoe-specific paths into Zentra.\n\n## Engineering Rules\n\n- Use Node.js 24 or newer and pnpm 10.\n- Follow test-driven development for behavioral changes.\n- Invoke subprocesses with executable and argument arrays and `shell: false`.\n- Pass explicit minimal environments to workers and validations.\n- Do not expose a general shell capability.\n- Keep the event journal as the source of truth and projections rebuildable.\n- Use only `completed`, `cancelled`, `denied`, `timed_out`, or `failed` as terminal outcomes.\n- Never automatically retry a potentially effectful operation after an uncertain result.\n- Keep implementation work off `main` and use isolated worktrees.\n- Do not commit, push, or file issues unless the user explicitly requests it.\n- Put each full sentence on its own physical line in long Markdown documents.\n\n## Scope Discipline\n\n- Complete the deterministic local tracer bullet before adding real coding harnesses.\n- Do not add distributed execution, personal workflows, email, meetings, or devices to the MVP.\n- Do not introduce plugin APIs until the core contracts are exercised end to end.\n- Prefer one measurable vertical outcome over unused horizontal infrastructure.\n\n## Security Boundary\n\n- Agent reasoning never grants execution authority.\n- Installation never grants authority.\n- Workers and reviewers must not inherit arbitrary parent secrets.\n- File writes must remain inside the assigned worktree and reviewed relative paths.\n- Integration must validate a disposable candidate before updating the integration branch.","lineStart":1,"lineEnd":37,"totalLines":37,"truncated":false}},"title":"AGENTS.md","time":{"start":1783897842812,"end":1783897842847}},"metadata":{"azure":{"itemId":"fc_0c74edd636192079016a541ef4b28c8190bcc0a27ce66ba930"}},"id":"prt_f5898e476001sjMW7iWwV3gmgV","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898c6de001d15dRXL42stTuI"}}
{"type":"tool_use","timestamp":1783897842875,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_1SjJCgTNUfHxPKMjRkXH0F26","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/issues/009-high-require-content-aware-independent-review.md"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/issues/009-high-require-content-aware-independent-review.md</path>\n<type>file</type>\n<content>\n1: # 009 - Require Content-Aware Independent Review\n2: \n3: Severity: High.\n4: Status: Open.\n5: Execution wave: Wave 1, Pod B.\n6: Suggested owner scope: Reviewer protocol, CLI reviewer configuration, review evidence, and fail-closed defaults.\n7: Dependencies: None.\n8: Conflicts and serialization notes: Implement and integrate issue 009 before issue 006, use one Pod B writer for shared tracer evidence files, and serialize any shared CLI composition file with the active packaging writer.\n9: \n10: ## Problem\n11: \n12: The deployable CLI uses fixed worker and reviewer identities and a bundled reviewer that approves solely because those identities differ.\n13: The reviewer process receives hashes and identities but not the bounded diff or validation evidence needed to assess content.\n14: \n15: ## Repository Evidence\n16: \n17: `src/reviews/reviewer-adapter.ts:101-118` invokes the reviewer with only diff digest, validation digest, worker ID, and reviewer ID.\n18: `fixtures/deterministic-reviewer.mjs:54-64` approves when worker and reviewer identities differ and does not inspect code or validation content.\n19: `src/cli/main.ts:28-30` fixes the worker and reviewer identities, and `src/cli/main.ts:324-335` always composes the bundled deterministic reviewer for task execution.\n20: \n21: ## Failure Sequence Or User Impact\n22: \n23: A worker produces a dangerous or incorrect diff that still passes focused validation.\n24: The bundled reviewer receives valid digests and two different fixed identities.\n25: It approves without reading the diff or validation evidence.\n26: Zentra presents the result as independently reviewed and proceeds toward integration despite no substantive review.\n27: \n28: ## Acceptance Criteria\n29: \n30: - [ ] The deployable CLI defaults to deny when no content-aware reviewer is configured.\n31: - [ ] The independently identified reviewer receives the bounded exact diff and bounded validation evidence through standard input rather than command-line arguments.\n32: - [ ] The reviewer returns an explicit content-based decision bound to the exact diff and validation evidence digests.\n33: - [ ] Reviewer output is strictly bounded, schema validated, single-decision, and fails closed on timeout, truncation, malformed content, or evidence-digest mismatch.\n34: - [ ] The deterministic identity-only reviewer remains test-only and cannot be selected accidentally in a production package.\n35: \n36: ## Required Tests\n37: \n38: - [ ] Add CLI end-to-end tests proving missing reviewer configuration denies execution before commit or integration.\n39: - [ ] Add reviewer protocol tests for actual diff inspection, stdin bounds, evidence-digest mismatch, timeout, malformed output, and same-identity denial.\n40: - [ ] Add an adversarial diff that passes focused validation but is denied by a configured content-aware reviewer.\n41: \n42: ## Final Verification\n43: \n44: Run `pnpm test`, `pnpm check`, and `pnpm build`.\n45: Inspect the packed tarball and verify the production CLI cannot silently use the deterministic reviewer fixture.\n46: Run a complete task with no reviewer and with a configured reviewer and verify deny-by-default and content-bound approval behavior.\n47: \n48: ## Non-Goals\n49: \n50: This issue does not prescribe a particular model vendor or review implementation.\n51: This issue does not let reviewer reasoning grant execution authority beyond returning a bounded decision.\n52: This issue does not remove digest verification or worker-reviewer identity separation.\n53: Authenticated external reviewer protocols, cryptographic challenges, signing, and broader anti-replay design are separate future work and are not acceptance scope for this issue.\n\n(End of file - total 53 lines)\n</content>","metadata":{"preview":"# 009 - Require Content-Aware Independent Review\n\nSeverity: High.\nStatus: Open.\nExecution wave: Wave 1, Pod B.\nSuggested owner scope: Reviewer protocol, CLI reviewer configuration, review evidence, and fail-closed defaults.\nDependencies: None.\nConflicts and serialization notes: Implement and integrate issue 009 before issue 006, use one Pod B writer for shared tracer evidence files, and serialize any shared CLI composition file with the active packaging writer.\n\n## Problem\n\nThe deployable CLI uses fixed worker and reviewer identities and a bundled reviewer that approves solely because those identities differ.\nThe reviewer process receives hashes and identities but not the bounded diff or validation evidence needed to assess content.\n\n## Repository Evidence\n\n`src/reviews/reviewer-adapter.ts:101-118` invokes the reviewer with only diff digest, validation digest, worker ID, and reviewer ID.\n`fixtures/deterministic-reviewer.mjs:54-64` approves when worker and reviewer identities differ and does not inspect code or validation content.\n`src/cli/main.ts:28-30` fixes the worker and reviewer identities, and `src/cli/main.ts:324-335` always composes the bundled deterministic reviewer for task execution.\n","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/issues/009-high-require-content-aware-independent-review.md","text":"# 009 - Require Content-Aware Independent Review\n\nSeverity: High.\nStatus: Open.\nExecution wave: Wave 1, Pod B.\nSuggested owner scope: Reviewer protocol, CLI reviewer configuration, review evidence, and fail-closed defaults.\nDependencies: None.\nConflicts and serialization notes: Implement and integrate issue 009 before issue 006, use one Pod B writer for shared tracer evidence files, and serialize any shared CLI composition file with the active packaging writer.\n\n## Problem\n\nThe deployable CLI uses fixed worker and reviewer identities and a bundled reviewer that approves solely because those identities differ.\nThe reviewer process receives hashes and identities but not the bounded diff or validation evidence needed to assess content.\n\n## Repository Evidence\n\n`src/reviews/reviewer-adapter.ts:101-118` invokes the reviewer with only diff digest, validation digest, worker ID, and reviewer ID.\n`fixtures/deterministic-reviewer.mjs:54-64` approves when worker and reviewer identities differ and does not inspect code or validation content.\n`src/cli/main.ts:28-30` fixes the worker and reviewer identities, and `src/cli/main.ts:324-335` always composes the bundled deterministic reviewer for task execution.\n\n## Failure Sequence Or User Impact\n\nA worker produces a dangerous or incorrect diff that still passes focused validation.\nThe bundled reviewer receives valid digests and two different fixed identities.\nIt approves without reading the diff or validation evidence.\nZentra presents the result as independently reviewed and proceeds toward integration despite no substantive review.\n\n## Acceptance Criteria\n\n- [ ] The deployable CLI defaults to deny when no content-aware reviewer is configured.\n- [ ] The independently identified reviewer receives the bounded exact diff and bounded validation evidence through standard input rather than command-line arguments.\n- [ ] The reviewer returns an explicit content-based decision bound to the exact diff and validation evidence digests.\n- [ ] Reviewer output is strictly bounded, schema validated, single-decision, and fails closed on timeout, truncation, malformed content, or evidence-digest mismatch.\n- [ ] The deterministic identity-only reviewer remains test-only and cannot be selected accidentally in a production package.\n\n## Required Tests\n\n- [ ] Add CLI end-to-end tests proving missing reviewer configuration denies execution before commit or integration.\n- [ ] Add reviewer protocol tests for actual diff inspection, stdin bounds, evidence-digest mismatch, timeout, malformed output, and same-identity denial.\n- [ ] Add an adversarial diff that passes focused validation but is denied by a configured content-aware reviewer.\n\n## Final Verification\n\nRun `pnpm test`, `pnpm check`, and `pnpm build`.\nInspect the packed tarball and verify the production CLI cannot silently use the deterministic reviewer fixture.\nRun a complete task with no reviewer and with a configured reviewer and verify deny-by-default and content-bound approval behavior.\n\n## Non-Goals\n\nThis issue does not prescribe a particular model vendor or review implementation.\nThis issue does not let reviewer reasoning grant execution authority beyond returning a bounded decision.\nThis issue does not remove digest verification or worker-reviewer identity separation.\nAuthenticated external reviewer protocols, cryptographic challenges, signing, and broader anti-replay design are separate future work and are not acceptance scope for this issue.","lineStart":1,"lineEnd":53,"totalLines":53,"truncated":false}},"title":"docs/issues/009-high-require-content-aware-independent-review.md","time":{"start":1783897842816,"end":1783897842851}},"metadata":{"azure":{"itemId":"fc_0c74edd636192079016a541ef4b2b88190b273c06f61f568b4"}},"id":"prt_f5898e47d0018yss3TdgPyOQS4","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898c6de001d15dRXL42stTuI"}}
{"type":"tool_use","timestamp":1783897842876,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_bPnTMUPTVNeCQxgQb6qvpMr9","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/execution/issue-009-implementation-report.md"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/execution/issue-009-implementation-report.md</path>\n<type>file</type>\n<content>\n1: # Issue 009 Implementation Report\n2: \n3: ## Status\n4: \n5: Implemented and verified.\n6: \n7: The deployable CLI now denies task execution when no content-aware reviewer is configured.\n8: A configured reviewer receives bounded review evidence over standard input and must return one bounded, schema-valid decision bound to the exact evidence digests.\n9: \n10: ## Root Cause\n11: \n12: The deployable CLI previously selected the bundled deterministic reviewer unconditionally.\n13: That reviewer received only identities and digests, so it could approve based on identity separation without inspecting the diff or validation evidence.\n14: \n15: ## Files Changed\n16: \n17: - `src/cli/main.ts` adds explicit reviewer process configuration, deny-by-default behavior, and production wiring that no longer resolves the deterministic reviewer fixture.\n18: - `src/reviews/reviewer-adapter.ts` adds the bounded standard-input reviewer protocol, process containment, strict decision validation, and evidence digest verification.\n19: - `tests/fixtures/content-aware-reviewer.mjs` provides a test-only reviewer that inspects diff content and validation evidence.\n20: - `tests/reviews/deterministic-reviewer-adapter.test.ts` covers the content-aware protocol and fail-closed cases.\n21: - `tests/orchestration/cli.test.ts` covers missing configuration, configured review, adversarial denial, and built CLI wiring.\n22: \n23: ## Tests Added\n24: \n25: - Exact diff and validation evidence are delivered through standard input rather than command-line arguments.\n26: - Oversized reviewer input is rejected before process execution.\n27: - Evidence digest mismatch fails closed.\n28: - Reviewer timeout fails closed with the typed `timed_out` outcome.\n29: - Malformed output fails closed.\n30: - Output truncation fails closed.\n31: - Multiple decisions fail closed.\n32: - Matching worker and reviewer identities fail closed before reviewer execution.\n33: - A dangerous authentication-bypass diff is denied despite successful focused validation.\n34: - Missing CLI reviewer configuration records `denied` without creating a worktree, commit, or integration branch.\n35: - The built production CLI does not reference the deterministic reviewer adapter or fixture.\n36: \n37: ## Commands And Results\n38: \n39: - `pnpm install --frozen-lockfile`: passed with `Already up to date` and `Done in 113ms`.\n40: - `shasum -a 256 pnpm-lock.yaml` before and after installation: unchanged at `90b4d829baf7d6ca730bfac365df0b8feffdef040b7291bb079d3a4228509afc`.\n41: - `git diff --exit-code -- pnpm-lock.yaml`: passed with no lockfile changes.\n42: - `pnpm test`: passed, 15 test files and 490 tests.\n43: - `pnpm check`: passed with TypeScript reporting no errors.\n44: - `pnpm build`: passed with TypeScript reporting no errors.\n45: - `pnpm exec vitest run tests/reviews/deterministic-reviewer-adapter.test.ts tests/reviews/review-gate.test.ts tests/orchestration/cli.test.ts`: passed, 3 test files and 110 tests.\n46: - `pnpm pack --pack-destination /var/folders/10/9mqn0tw54gg6j709prq0ytv40000gn/T/opencode`: passed and produced `zentra-0.1.0.tgz` for package inspection.\n47: - Built CLI inspection test: passed and confirmed that `dist/src/cli/main.js` contains neither `deterministic-reviewer.mjs` nor `DeterministicReviewerAdapter`.\n48: \n49: ## Acceptance Criteria Evidence\n50: \n51: - Missing reviewer configuration creates a terminal `denied` task before worker or worktree setup.\n52: - The reviewer request contains the exact diff, complete validation report, and their canonical SHA-256 digests in one bounded JSON standard-input payload.\n53: - Reviewer command-line arguments contain only operator-configured process arguments and never review evidence.\n54: - Reviewer decisions use explicit `approve` or `deny` values and include reviewer identity, both evidence digests, timestamp, and bounded reason.\n55: - Strict Zod parsing rejects unknown, missing, or incorrectly typed decision fields.\n56: - Aggregate standard output and standard error are bounded, and timeout, cancellation, nonzero exit, excessive output, malformed JSON, multiple decisions, identity mismatch, and digest mismatch all reject the review.\n57: - The adversarial CLI test proves that successful focused validation does not override a content-based denial.\n58: - Production CLI composition has no deterministic reviewer fallback and requires explicit reviewer executable and identity configuration.\n59: \n60: ## Security Boundary\n61: \n62: Reviewer reasoning grants no execution authority.\n63: The reviewer can return only one bounded decision, and the existing review gate independently checks approval, identity separation, validation success, and evidence digests before integration.\n64: The reviewer process uses `shell: false`, an executable and argument array, a minimal allowlisted environment, bounded input and output, a fixed timeout, and process-group termination.\n65: No worktree path, inherited arbitrary secret, or general shell capability is provided by the reviewer protocol.\n66: \n67: ## Remaining Concerns\n68: \n69: The current broad package contents still include repository source, tests, and `fixtures/deterministic-reviewer.mjs` because package allowlisting and deterministic package contents belong to issues 016 and 019 and were outside this issue's owned files.\n70: The production CLI cannot select that fixture implicitly, and selecting any reviewer now requires explicit executable, arguments, and identity configuration.\n71: Distribution hardening should remove test-only source and fixtures from the published package once those packaging issues are implemented.\n72: \n73: ## Commit Identity\n74: \n75: - Branch: `fix/predeploy-b-review-artifacts`.\n76: - Implementation commit: `e0347429a8b9fd69aa6cbf283c3388c864ec742c` (`fix: require content-aware independent review`).\n77: - Report commit: the commit containing this file.\n\n(End of file - total 77 lines)\n</content>","metadata":{"preview":"# Issue 009 Implementation Report\n\n## Status\n\nImplemented and verified.\n\nThe deployable CLI now denies task execution when no content-aware reviewer is configured.\nA configured reviewer receives bounded review evidence over standard input and must return one bounded, schema-valid decision bound to the exact evidence digests.\n\n## Root Cause\n\nThe deployable CLI previously selected the bundled deterministic reviewer unconditionally.\nThat reviewer received only identities and digests, so it could approve based on identity separation without inspecting the diff or validation evidence.\n\n## Files Changed\n\n- `src/cli/main.ts` adds explicit reviewer process configuration, deny-by-default behavior, and production wiring that no longer resolves the deterministic reviewer fixture.\n- `src/reviews/reviewer-adapter.ts` adds the bounded standard-input reviewer protocol, process containment, strict decision validation, and evidence digest verification.\n- `tests/fixtures/content-aware-reviewer.mjs` provides a test-only reviewer that inspects diff content and validation evidence.\n- `tests/reviews/deterministic-reviewer-adapter.test.ts` covers the content-aware protocol and fail-closed cases.","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/docs/execution/issue-009-implementation-report.md","text":"# Issue 009 Implementation Report\n\n## Status\n\nImplemented and verified.\n\nThe deployable CLI now denies task execution when no content-aware reviewer is configured.\nA configured reviewer receives bounded review evidence over standard input and must return one bounded, schema-valid decision bound to the exact evidence digests.\n\n## Root Cause\n\nThe deployable CLI previously selected the bundled deterministic reviewer unconditionally.\nThat reviewer received only identities and digests, so it could approve based on identity separation without inspecting the diff or validation evidence.\n\n## Files Changed\n\n- `src/cli/main.ts` adds explicit reviewer process configuration, deny-by-default behavior, and production wiring that no longer resolves the deterministic reviewer fixture.\n- `src/reviews/reviewer-adapter.ts` adds the bounded standard-input reviewer protocol, process containment, strict decision validation, and evidence digest verification.\n- `tests/fixtures/content-aware-reviewer.mjs` provides a test-only reviewer that inspects diff content and validation evidence.\n- `tests/reviews/deterministic-reviewer-adapter.test.ts` covers the content-aware protocol and fail-closed cases.\n- `tests/orchestration/cli.test.ts` covers missing configuration, configured review, adversarial denial, and built CLI wiring.\n\n## Tests Added\n\n- Exact diff and validation evidence are delivered through standard input rather than command-line arguments.\n- Oversized reviewer input is rejected before process execution.\n- Evidence digest mismatch fails closed.\n- Reviewer timeout fails closed with the typed `timed_out` outcome.\n- Malformed output fails closed.\n- Output truncation fails closed.\n- Multiple decisions fail closed.\n- Matching worker and reviewer identities fail closed before reviewer execution.\n- A dangerous authentication-bypass diff is denied despite successful focused validation.\n- Missing CLI reviewer configuration records `denied` without creating a worktree, commit, or integration branch.\n- The built production CLI does not reference the deterministic reviewer adapter or fixture.\n\n## Commands And Results\n\n- `pnpm install --frozen-lockfile`: passed with `Already up to date` and `Done in 113ms`.\n- `shasum -a 256 pnpm-lock.yaml` before and after installation: unchanged at `90b4d829baf7d6ca730bfac365df0b8feffdef040b7291bb079d3a4228509afc`.\n- `git diff --exit-code -- pnpm-lock.yaml`: passed with no lockfile changes.\n- `pnpm test`: passed, 15 test files and 490 tests.\n- `pnpm check`: passed with TypeScript reporting no errors.\n- `pnpm build`: passed with TypeScript reporting no errors.\n- `pnpm exec vitest run tests/reviews/deterministic-reviewer-adapter.test.ts tests/reviews/review-gate.test.ts tests/orchestration/cli.test.ts`: passed, 3 test files and 110 tests.\n- `pnpm pack --pack-destination /var/folders/10/9mqn0tw54gg6j709prq0ytv40000gn/T/opencode`: passed and produced `zentra-0.1.0.tgz` for package inspection.\n- Built CLI inspection test: passed and confirmed that `dist/src/cli/main.js` contains neither `deterministic-reviewer.mjs` nor `DeterministicReviewerAdapter`.\n\n## Acceptance Criteria Evidence\n\n- Missing reviewer configuration creates a terminal `denied` task before worker or worktree setup.\n- The reviewer request contains the exact diff, complete validation report, and their canonical SHA-256 digests in one bounded JSON standard-input payload.\n- Reviewer command-line arguments contain only operator-configured process arguments and never review evidence.\n- Reviewer decisions use explicit `approve` or `deny` values and include reviewer identity, both evidence digests, timestamp, and bounded reason.\n- Strict Zod parsing rejects unknown, missing, or incorrectly typed decision fields.\n- Aggregate standard output and standard error are bounded, and timeout, cancellation, nonzero exit, excessive output, malformed JSON, multiple decisions, identity mismatch, and digest mismatch all reject the review.\n- The adversarial CLI test proves that successful focused validation does not override a content-based denial.\n- Production CLI composition has no deterministic reviewer fallback and requires explicit reviewer executable and identity configuration.\n\n## Security Boundary\n\nReviewer reasoning grants no execution authority.\nThe reviewer can return only one bounded decision, and the existing review gate independently checks approval, identity separation, validation success, and evidence digests before integration.\nThe reviewer process uses `shell: false`, an executable and argument array, a minimal allowlisted environment, bounded input and output, a fixed timeout, and process-group termination.\nNo worktree path, inherited arbitrary secret, or general shell capability is provided by the reviewer protocol.\n\n## Remaining Concerns\n\nThe current broad package contents still include repository source, tests, and `fixtures/deterministic-reviewer.mjs` because package allowlisting and deterministic package contents belong to issues 016 and 019 and were outside this issue's owned files.\nThe production CLI cannot select that fixture implicitly, and selecting any reviewer now requires explicit executable, arguments, and identity configuration.\nDistribution hardening should remove test-only source and fixtures from the published package once those packaging issues are implemented.\n\n## Commit Identity\n\n- Branch: `fix/predeploy-b-review-artifacts`.\n- Implementation commit: `e0347429a8b9fd69aa6cbf283c3388c864ec742c` (`fix: require content-aware independent review`).\n- Report commit: the commit containing this file.","lineStart":1,"lineEnd":77,"totalLines":77,"truncated":false}},"title":"docs/execution/issue-009-implementation-report.md","time":{"start":1783897842818,"end":1783897842870}},"metadata":{"azure":{"itemId":"fc_0c74edd636192079016a541ef4b2c481908d69ca13d9dbf257"}},"id":"prt_f5898e480001ySyjCf50dV4Atq","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898c6de001d15dRXL42stTuI"}}
{"type":"tool_use","timestamp":1783897842909,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"bash","callID":"call_qoj63hBSNxVsmJh9lMnQt1Li","state":{"status":"completed","input":{"command":"git diff \"$(git merge-base fix/pre-deployment HEAD)\"..HEAD","workdir":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts","timeout":120000},"output":"diff --git a/docs/execution/issue-009-implementation-report.md b/docs/execution/issue-009-implementation-report.md\nnew file mode 100644\nindex 0000000..d976d30\n--- /dev/null\n+++ b/docs/execution/issue-009-implementation-report.md\n@@ -0,0 +1,77 @@\n+# Issue 009 Implementation Report\n+\n+## Status\n+\n+Implemented and verified.\n+\n+The deployable CLI now denies task execution when no content-aware reviewer is configured.\n+A configured reviewer receives bounded review evidence over standard input and must return one bounded, schema-valid decision bound to the exact evidence digests.\n+\n+## Root Cause\n+\n+The deployable CLI previously selected the bundled deterministic reviewer unconditionally.\n+That reviewer received only identities and digests, so it could approve based on identity separation without inspecting the diff or validation evidence.\n+\n+## Files Changed\n+\n+- `src/cli/main.ts` adds explicit reviewer process configuration, deny-by-default behavior, and production wiring that no longer resolves the deterministic reviewer fixture.\n+- `src/reviews/reviewer-adapter.ts` adds the bounded standard-input reviewer protocol, process containment, strict decision validation, and evidence digest verification.\n+- `tests/fixtures/content-aware-reviewer.mjs` provides a test-only reviewer that inspects diff content and validation evidence.\n+- `tests/reviews/deterministic-reviewer-adapter.test.ts` covers the content-aware protocol and fail-closed cases.\n+- `tests/orchestration/cli.test.ts` covers missing configuration, configured review, adversarial denial, and built CLI wiring.\n+\n+## Tests Added\n+\n+- Exact diff and validation evidence are delivered through standard input rather than command-line arguments.\n+- Oversized reviewer input is rejected before process execution.\n+- Evidence digest mismatch fails closed.\n+- Reviewer timeout fails closed with the typed `timed_out` outcome.\n+- Malformed output fails closed.\n+- Output truncation fails closed.\n+- Multiple decisions fail closed.\n+- Matching worker and reviewer identities fail closed before reviewer execution.\n+- A dangerous authentication-bypass diff is denied despite successful focused validation.\n+- Missing CLI reviewer configuration records `denied` without creating a worktree, commit, or integration branch.\n+- The built production CLI does not reference the deterministic reviewer adapter or fixture.\n+\n+## Commands And Results\n+\n+- `pnpm install --frozen-lockfile`: passed with `Already up to date` and `Done in 113ms`.\n+- `shasum -a 256 pnpm-lock.yaml` before and after installation: unchanged at `90b4d829baf7d6ca730bfac365df0b8feffdef040b7291bb079d3a4228509afc`.\n+- `git diff --exit-code -- pnpm-lock.yaml`: passed with no lockfile changes.\n+- `pnpm test`: passed, 15 test files and 490 tests.\n+- `pnpm check`: passed with TypeScript reporting no errors.\n+- `pnpm build`: passed with TypeScript reporting no errors.\n+- `pnpm exec vitest run tests/reviews/deterministic-reviewer-adapter.test.ts tests/reviews/review-gate.test.ts tests/orchestration/cli.test.ts`: passed, 3 test files and 110 tests.\n+- `pnpm pack --pack-destination /var/folders/10/9mqn0tw54gg6j709prq0ytv40000gn/T/opencode`: passed and produced `zentra-0.1.0.tgz` for package inspection.\n+- Built CLI inspection test: passed and confirmed that `dist/src/cli/main.js` contains neither `deterministic-reviewer.mjs` nor `DeterministicReviewerAdapter`.\n+\n+## Acceptance Criteria Evidence\n+\n+- Missing reviewer configuration creates a terminal `denied` task before worker or worktree setup.\n+- The reviewer request contains the exact diff, complete validation report, and their canonical SHA-256 digests in one bounded JSON standard-input payload.\n+- Reviewer command-line arguments contain only operator-configured process arguments and never review evidence.\n+- Reviewer decisions use explicit `approve` or `deny` values and include reviewer identity, both evidence digests, timestamp, and bounded reason.\n+- Strict Zod parsing rejects unknown, missing, or incorrectly typed decision fields.\n+- Aggregate standard output and standard error are bounded, and timeout, cancellation, nonzero exit, excessive output, malformed JSON, multiple decisions, identity mismatch, and digest mismatch all reject the review.\n+- The adversarial CLI test proves that successful focused validation does not override a content-based denial.\n+- Production CLI composition has no deterministic reviewer fallback and requires explicit reviewer executable and identity configuration.\n+\n+## Security Boundary\n+\n+Reviewer reasoning grants no execution authority.\n+The reviewer can return only one bounded decision, and the existing review gate independently checks approval, identity separation, validation success, and evidence digests before integration.\n+The reviewer process uses `shell: false`, an executable and argument array, a minimal allowlisted environment, bounded input and output, a fixed timeout, and process-group termination.\n+No worktree path, inherited arbitrary secret, or general shell capability is provided by the reviewer protocol.\n+\n+## Remaining Concerns\n+\n+The current broad package contents still include repository source, tests, and `fixtures/deterministic-reviewer.mjs` because package allowlisting and deterministic package contents belong to issues 016 and 019 and were outside this issue's owned files.\n+The production CLI cannot select that fixture implicitly, and selecting any reviewer now requires explicit executable, arguments, and identity configuration.\n+Distribution hardening should remove test-only source and fixtures from the published package once those packaging issues are implemented.\n+\n+## Commit Identity\n+\n+- Branch: `fix/predeploy-b-review-artifacts`.\n+- Implementation commit: `e0347429a8b9fd69aa6cbf283c3388c864ec742c` (`fix: require content-aware independent review`).\n+- Report commit: the commit containing this file.\ndiff --git a/src/cli/main.ts b/src/cli/main.ts\nindex 996755f..72f3504 100644\n--- a/src/cli/main.ts\n+++ b/src/cli/main.ts\n@@ -8,8 +8,7 @@ import { Command, CommanderError } from \"commander\";\n \n import { ValidationRunner } from \"../capabilities/validation-runner.js\";\n import {\n-  resolveBundledFixtures,\n-  type BundledFixturePaths,\n+  resolveBundledFixture,\n } from \"../fixtures/bundled-fixtures.js\";\n import { IntegrationQueue } from \"../integration/integration-queue.js\";\n import { SqliteEventJournal } from \"../journal/sqlite-journal.js\";\n@@ -18,7 +17,10 @@ import { TracerBulletOrchestrator } from \"../orchestration/tracer-bullet.js\";\n import { ProjectConfigSchema, type ProjectConfig } from \"../projects/project-config.js\";\n import { ProjectRegistry } from \"../projects/project-registry.js\";\n import { ReviewGate } from \"../reviews/review-gate.js\";\n-import { DeterministicReviewerAdapter } from \"../reviews/reviewer-adapter.js\";\n+import {\n+  ProcessReviewerAdapter,\n+  type ReviewerAdapter,\n+} from \"../reviews/reviewer-adapter.js\";\n import { TaskService } from \"../tasks/task-service.js\";\n import type { TaskView } from \"../tasks/task-projection.js\";\n import { ProcessSupervisor } from \"../workers/process-supervisor.js\";\n@@ -26,7 +28,6 @@ import { GitClient } from \"../workspaces/git-client.js\";\n import { WorktreeManager } from \"../workspaces/worktree-manager.js\";\n \n const WORKER_ID = \"zentra-deterministic-worker\";\n-const REVIEWER_ID = \"zentra-deterministic-reviewer\";\n const WORKER_TIMEOUT_MS = 120_000;\n const MAX_OPERATIONAL_JSON_BYTES = 16_384;\n const MAX_TASK_ID_LENGTH = 128;\n@@ -81,6 +82,9 @@ interface RunOptions extends ProjectOptions, DatabaseTaskOptions {\n   readonly title: string;\n   readonly file: string;\n   readonly content: string;\n+  readonly reviewerExecutable?: string;\n+  readonly reviewerArgument: readonly string[];\n+  readonly reviewerId?: string;\n }\n \n interface RecoverOptions extends ProjectOptions, DatabaseTaskOptions {}\n@@ -180,6 +184,14 @@ function createProgram(\n     .requiredOption(\"--title <title>\", \"task title\")\n     .requiredOption(\"--file <relative-path>\", \"one root-level file\")\n     .requiredOption(\"--content <text>\", \"replacement file content\")\n+    .option(\"--reviewer-executable <path>\", \"content-aware reviewer executable\")\n+    .option(\n+      \"--reviewer-argument <value>\",\n+      \"argument passed to the configured reviewer executable\",\n+      (value: string, previous: string[]) => [...previous, value],\n+      [],\n+    )\n+    .option(\"--reviewer-id <id>\", \"independent reviewer identity\")\n     .action(async (options: RunOptions) => {\n       assertSafeTaskId(options.taskId);\n       assertSafeTitle(options.title);\n@@ -195,18 +207,33 @@ function createProgram(\n         projectId: projectConfig.projectId,\n         title: options.title,\n       });\n-      const fixtures = attestedFixtures(fixtureAnchor);\n+      const reviewer = configuredReviewer(options);\n+      if (reviewer === null) {\n+        const denied = await withSystem(\n+          options.database,\n+          configs,\n+          \"read-write\",\n+          (system) => Promise.resolve(denyMissingReviewer(system.tasks, {\n+            taskId: options.taskId,\n+            projectId: projectConfig.projectId,\n+            title: options.title,\n+          })),\n+        );\n+        setResult(taskRunResult(denied));\n+        return;\n+      }\n+      const workerFixture = attestedWorkerFixture(fixtureAnchor);\n       const result = await withSystem(options.database, configs, \"read-write\", async (system) => {\n-        const taskView = await system.execution(fixtures).orchestrator.run({\n+        const taskView = await system.execution(workerFixture, reviewer.adapter).orchestrator.run({\n           taskId: options.taskId,\n           projectId: projectConfig.projectId,\n           title: options.title,\n           workerId: WORKER_ID,\n-          reviewerId: REVIEWER_ID,\n+          reviewerId: reviewer.reviewerId,\n           workerRequest: {\n             executable: process.execPath,\n             args: [\n-              fixtures[\"deterministic-worker.mjs\"],\n+              workerFixture,\n               \"--file\",\n               options.file,\n               \"--content\",\n@@ -316,7 +343,7 @@ function composeSystem(\n       const { projects, git, worktrees } = developmentDependencies();\n       return new RecoveryService(journal, tasks, projects, worktrees, git);\n     },\n-    execution(fixtures: BundledFixturePaths) {\n+    execution(workerFixture: string, reviewer: ReviewerAdapter) {\n       const { projects, git, worktrees } = developmentDependencies();\n       const supervisor = new ProcessSupervisor();\n       const validations = new ValidationRunner(supervisor);\n@@ -327,10 +354,7 @@ function composeSystem(\n           worktrees,\n           supervisor,\n           validations,\n-          new DeterministicReviewerAdapter(\n-            supervisor,\n-            fixtures[\"deterministic-reviewer.mjs\"],\n-          ),\n+          reviewer,\n           new ReviewGate(),\n           new IntegrationQueue(git, validations),\n         ),\n@@ -373,14 +397,61 @@ function loadProjects(configPath: string): readonly ProjectConfig[] {\n   }\n }\n \n-function attestedFixtures(anchor?: string | URL): BundledFixturePaths {\n+function attestedWorkerFixture(anchor?: string | URL): string {\n   try {\n-    return anchor === undefined ? resolveBundledFixtures() : resolveBundledFixtures(anchor);\n+    return anchor === undefined\n+      ? resolveBundledFixture(\"deterministic-worker.mjs\")\n+      : resolveBundledFixture(\"deterministic-worker.mjs\", anchor);\n   } catch {\n     throw new CliFailure(\"BUNDLED_FIXTURE_INVALID\");\n   }\n }\n \n+function configuredReviewer(options: RunOptions): {\n+  readonly reviewerId: string;\n+  readonly adapter: ReviewerAdapter;\n+} | null {\n+  const executable = options.reviewerExecutable;\n+  const reviewerId = options.reviewerId;\n+  if (executable === undefined && reviewerId === undefined && options.reviewerArgument.length === 0) {\n+    return null;\n+  }\n+  if (\n+    executable === undefined ||\n+    executable.length === 0 ||\n+    reviewerId === undefined ||\n+    reviewerId.length === 0\n+  ) {\n+    throw new CliFailure(\"INVALID_COMMAND\");\n+  }\n+  return {\n+    reviewerId,\n+    adapter: new ProcessReviewerAdapter({\n+      executable,\n+      args: options.reviewerArgument,\n+    }),\n+  };\n+}\n+\n+function denyMissingReviewer(\n+  tasks: TaskService,\n+  input: { readonly taskId: string; readonly projectId: string; readonly title: string },\n+): TaskView {\n+  tasks.create({\n+    ...input,\n+    correlationId: input.taskId,\n+  });\n+  return tasks.append(\n+    input.taskId,\n+    \"task.denied\",\n+    {\n+      stage: \"setup\",\n+      reason: \"content-aware reviewer is not configured\",\n+    },\n+    null,\n+  );\n+}\n+\n function assertSafeRootFile(candidate: string): void {\n   if (\n     candidate.length === 0 ||\ndiff --git a/src/reviews/reviewer-adapter.ts b/src/reviews/reviewer-adapter.ts\nindex fec96ed..74a606b 100644\n--- a/src/reviews/reviewer-adapter.ts\n+++ b/src/reviews/reviewer-adapter.ts\n@@ -1,4 +1,5 @@\n import { createHash } from \"node:crypto\";\n+import { spawn } from \"node:child_process\";\n import { z } from \"zod\";\n import type { ProcessSupervisor } from \"../workers/process-supervisor.js\";\n import type { ValidationReport } from \"../capabilities/validation-runner.js\";\n@@ -31,7 +32,7 @@ export class ReviewerExecutionError extends Error {\n     stderr: string,\n   ) {\n     super(\n-      `reviewer fixture failed: outcome=${outcome}${\n+      `reviewer process failed: outcome=${outcome}${\n         stderr === \"\" ? \"\" : `, stderr=${stderr}`\n       }`,\n     );\n@@ -47,6 +48,29 @@ export const ReviewDecisionSchema = z.strictObject({\n   reason: z.string().min(1),\n });\n \n+const ProcessReviewDecisionSchema = z.strictObject({\n+  reviewerId: z.string().min(1),\n+  decision: z.enum([\"approve\", \"deny\"]),\n+  diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n+  validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n+  decidedAt: z.string().datetime({ offset: true }),\n+  reason: z.string().min(1).max(4_096),\n+});\n+\n+const REVIEWER_ENV_ALLOWLIST = [\"PATH\", \"HOME\", \"TMPDIR\", \"LANG\", \"LC_ALL\"] as const;\n+const DEFAULT_REVIEWER_TIMEOUT_MS = 30_000;\n+const DEFAULT_MAX_REVIEW_INPUT_BYTES = 2 * 1_024 * 1_024;\n+const DEFAULT_MAX_REVIEW_OUTPUT_BYTES = 16 * 1_024;\n+\n+export interface ProcessReviewerOptions {\n+  readonly executable: string;\n+  readonly args?: readonly string[];\n+  readonly cwd?: string;\n+  readonly timeoutMs?: number;\n+  readonly maxInputBytes?: number;\n+  readonly maxOutputBytes?: number;\n+}\n+\n function sha256(value: string): string {\n   return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n }\n@@ -78,6 +102,176 @@ export function canonicalValidationDigest(validation: ValidationReport): string\n   return sha256(canonical);\n }\n \n+export class ProcessReviewerAdapter implements ReviewerAdapter {\n+  private readonly args: readonly string[];\n+  private readonly cwd: string;\n+  private readonly timeoutMs: number;\n+  private readonly maxInputBytes: number;\n+  private readonly maxOutputBytes: number;\n+\n+  constructor(private readonly options: ProcessReviewerOptions) {\n+    this.args = options.args ?? [];\n+    this.cwd = options.cwd ?? \"/tmp\";\n+    this.timeoutMs = options.timeoutMs ?? DEFAULT_REVIEWER_TIMEOUT_MS;\n+    this.maxInputBytes = options.maxInputBytes ?? DEFAULT_MAX_REVIEW_INPUT_BYTES;\n+    this.maxOutputBytes = options.maxOutputBytes ?? DEFAULT_MAX_REVIEW_OUTPUT_BYTES;\n+    for (const [name, value] of [\n+      [\"timeoutMs\", this.timeoutMs],\n+      [\"maxInputBytes\", this.maxInputBytes],\n+      [\"maxOutputBytes\", this.maxOutputBytes],\n+    ] as const) {\n+      if (!Number.isSafeInteger(value) || value <= 0) {\n+        throw new Error(`${name} must be a positive safe integer`);\n+      }\n+    }\n+  }\n+\n+  async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n+    if (input.workerId === input.reviewerId) {\n+      throw new Error(\n+        `reviewer identity must differ from worker identity: ${input.workerId}`,\n+      );\n+    }\n+\n+    const diffSha256 = sha256(input.diff);\n+    const validationSha256 = canonicalValidationDigest(input.validation);\n+    const request = JSON.stringify({\n+      schemaVersion: 1,\n+      workerId: input.workerId,\n+      reviewerId: input.reviewerId,\n+      diff: input.diff,\n+      validation: input.validation,\n+      diffSha256,\n+      validationSha256,\n+    });\n+    const requestBytes = Buffer.byteLength(request, \"utf8\");\n+    if (requestBytes > this.maxInputBytes) {\n+      throw new Error(\n+        `reviewer protocol input exceeded bounded evidence limit of ${this.maxInputBytes} bytes`,\n+      );\n+    }\n+\n+    const output = await this.execute(request, signal);\n+    const lines = output.trim().split(\"\\n\").filter((line) => line.trim() !== \"\");\n+    if (lines.length !== 1) {\n+      throw new Error(\n+        `reviewer protocol requires exactly one decision, received ${lines.length}`,\n+      );\n+    }\n+\n+    let decoded: unknown;\n+    try {\n+      decoded = JSON.parse(lines[0]!);\n+    } catch {\n+      throw new Error(\"reviewer protocol returned invalid JSON\");\n+    }\n+    const parsed = ProcessReviewDecisionSchema.safeParse(decoded);\n+    if (!parsed.success) {\n+      throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n+    }\n+    if (parsed.data.reviewerId !== input.reviewerId) {\n+      throw new Error(\"reviewer protocol returned a reviewer identity mismatch\");\n+    }\n+    if (parsed.data.diffSha256 !== diffSha256) {\n+      throw new Error(\"reviewer protocol returned a diff evidence digest mismatch\");\n+    }\n+    if (parsed.data.validationSha256 !== validationSha256) {\n+      throw new Error(\"reviewer protocol returned a validation evidence digest mismatch\");\n+    }\n+\n+    return ReviewDecisionSchema.parse({\n+      reviewerId: parsed.data.reviewerId,\n+      approved: parsed.data.decision === \"approve\",\n+      diffSha256: parsed.data.diffSha256,\n+      validationSha256: parsed.data.validationSha256,\n+      decidedAt: parsed.data.decidedAt,\n+      reason: parsed.data.reason,\n+    });\n+  }\n+\n+  private execute(request: string, signal: AbortSignal): Promise<string> {\n+    if (signal.aborted) {\n+      return Promise.reject(new ReviewerExecutionError(\"cancelled\", \"\"));\n+    }\n+\n+    return new Promise((resolve, reject) => {\n+      const env: Record<string, string> = {};\n+      for (const key of REVIEWER_ENV_ALLOWLIST) {\n+        const value = process.env[key];\n+        if (value !== undefined) env[key] = value;\n+      }\n+      const child = spawn(this.options.executable, [...this.args], {\n+        cwd: this.cwd,\n+        shell: false,\n+        detached: true,\n+        env,\n+        stdio: [\"pipe\", \"pipe\", \"pipe\"],\n+      });\n+      const stdout: Buffer[] = [];\n+      const stderr: Buffer[] = [];\n+      let outputBytes = 0;\n+      let outcome: \"cancelled\" | \"timed_out\" | \"failed\" | undefined;\n+      let reason = \"\";\n+      let settled = false;\n+\n+      const kill = (): void => {\n+        if (child.pid === undefined) return;\n+        try {\n+          process.kill(-child.pid, \"SIGKILL\");\n+        } catch {\n+          child.kill(\"SIGKILL\");\n+        }\n+      };\n+      const stop = (\n+        nextOutcome: \"cancelled\" | \"timed_out\" | \"failed\",\n+        nextReason: string,\n+      ): void => {\n+        if (outcome !== undefined) return;\n+        outcome = nextOutcome;\n+        reason = nextReason;\n+        kill();\n+      };\n+      const timer = setTimeout(\n+        () => stop(\"timed_out\", \"reviewer timeout exceeded\"),\n+        this.timeoutMs,\n+      );\n+      const onAbort = (): void => stop(\"cancelled\", \"reviewer cancelled\");\n+      signal.addEventListener(\"abort\", onAbort, { once: true });\n+      const capture = (target: Buffer[], chunk: Buffer): void => {\n+        const remaining = this.maxOutputBytes - outputBytes;\n+        if (remaining > 0) target.push(chunk.subarray(0, Math.min(remaining, chunk.byteLength)));\n+        outputBytes += chunk.byteLength;\n+        if (outputBytes > this.maxOutputBytes) {\n+          stop(\"failed\", `reviewer output limit of ${this.maxOutputBytes} bytes exceeded`);\n+        }\n+      };\n+      child.stdout.on(\"data\", (chunk: Buffer) => capture(stdout, chunk));\n+      child.stderr.on(\"data\", (chunk: Buffer) => capture(stderr, chunk));\n+      child.on(\"error\", (error) => stop(\"failed\", `reviewer spawn failed: ${error.message}`));\n+      child.on(\"close\", (code) => {\n+        if (settled) return;\n+        settled = true;\n+        clearTimeout(timer);\n+        signal.removeEventListener(\"abort\", onAbort);\n+        const stderrText = Buffer.concat(stderr).toString(\"utf8\");\n+        if (outcome !== undefined) {\n+          reject(new ReviewerExecutionError(outcome, reason || stderrText));\n+          return;\n+        }\n+        if (code !== 0) {\n+          reject(new ReviewerExecutionError(\"failed\", stderrText));\n+          return;\n+        }\n+        resolve(Buffer.concat(stdout).toString(\"utf8\"));\n+      });\n+      child.stdin.on(\"error\", () => {\n+        // The close event reports the authoritative process outcome.\n+      });\n+      child.stdin.end(request);\n+    });\n+  }\n+}\n+\n export class DeterministicReviewerAdapter implements ReviewerAdapter {\n   constructor(\n     private readonly supervisor: ProcessSupervisor,\ndiff --git a/tests/fixtures/content-aware-reviewer.mjs b/tests/fixtures/content-aware-reviewer.mjs\nnew file mode 100644\nindex 0000000..8a81098\n--- /dev/null\n+++ b/tests/fixtures/content-aware-reviewer.mjs\n@@ -0,0 +1,26 @@\n+#!/usr/bin/env node\n+\n+import { createHash } from \"node:crypto\";\n+\n+if (process.argv.length !== 2) {\n+  throw new Error(\"review evidence must be provided only through stdin\");\n+}\n+\n+let input = \"\";\n+process.stdin.setEncoding(\"utf8\");\n+for await (const chunk of process.stdin) input += chunk;\n+\n+const request = JSON.parse(input);\n+const dangerous = request.diff.includes(\"requireAuthentication = false\");\n+const decision = {\n+  reviewerId: request.reviewerId,\n+  decision: dangerous ? \"deny\" : \"approve\",\n+  diffSha256: createHash(\"sha256\").update(request.diff, \"utf8\").digest(\"hex\"),\n+  validationSha256: request.validationSha256,\n+  decidedAt: new Date().toISOString(),\n+  reason: dangerous\n+    ? \"Denied dangerous authentication bypass in the reviewed diff\"\n+    : `Reviewed exact diff and validation output: ${request.validation.stdout}`,\n+};\n+\n+process.stdout.write(`${JSON.stringify(decision)}\\n`);\ndiff --git a/tests/orchestration/cli.test.ts b/tests/orchestration/cli.test.ts\nindex a6b8eec..c1c416e 100644\n--- a/tests/orchestration/cli.test.ts\n+++ b/tests/orchestration/cli.test.ts\n@@ -30,6 +30,10 @@ import { GitClient } from \"../../src/workspaces/git-client.js\";\n const execFileAsync = promisify(execFile);\n const temporaryDirectories: string[] = [];\n const ATTACKER_MARKER = \"ATTACKER_MARKER_DO_NOT_PRINT\";\n+const CONTENT_AWARE_REVIEWER = path.resolve(\n+  import.meta.dirname,\n+  \"../fixtures/content-aware-reviewer.mjs\",\n+);\n \n interface Fixture {\n   readonly baseDirectory: string;\n@@ -72,9 +76,14 @@ async function fixture(): Promise<Fixture> {\n   await gitOk(repositoryPath, [\"config\", \"user.email\", \"fixture@zentra.local\"]);\n   mkdirSync(path.join(repositoryPath, \"test\"));\n   writeFileSync(path.join(repositoryPath, \"greeting.txt\"), \"hello\\n\", \"utf8\");\n+  writeFileSync(\n+    path.join(repositoryPath, \"auth.ts\"),\n+    \"export const requireAuthentication = true;\\n\",\n+    \"utf8\",\n+  );\n   writeFileSync(\n     path.join(repositoryPath, \"test\", \"greeting.test.mjs\"),\n-    'import assert from \"node:assert/strict\";\\nimport { readFile } from \"node:fs/promises\";\\nimport test from \"node:test\";\\ntest(\"greeting\", async () => assert.equal(await readFile(new URL(\"../greeting.txt\", import.meta.url), \"utf8\"), \"hello from CLI\\\\n\"));\\n',\n+    'import assert from \"node:assert/strict\";\\nimport { readFile } from \"node:fs/promises\";\\nimport test from \"node:test\";\\ntest(\"greeting\", async () => assert.match(await readFile(new URL(\"../greeting.txt\", import.meta.url), \"utf8\"), /^hello(?: from CLI)?\\\\n$/));\\n',\n     \"utf8\",\n   );\n   writeFileSync(\n@@ -120,8 +129,12 @@ async function invoke(\n   };\n }\n \n-function runArguments(testFixture: Fixture, taskId = \"task-cli\"): readonly string[] {\n-  return [\n+function runArguments(\n+  testFixture: Fixture,\n+  taskId = \"task-cli\",\n+  configureReviewer = true,\n+): readonly string[] {\n+  const args = [\n     \"task\", \"run\",\n     \"--config\", testFixture.configPath,\n     \"--database\", testFixture.databasePath,\n@@ -130,6 +143,14 @@ function runArguments(testFixture: Fixture, taskId = \"task-cli\"): readonly strin\n     \"--file\", \"greeting.txt\",\n     \"--content\", \"hello from CLI\\n\",\n   ];\n+  if (configureReviewer) {\n+    args.push(\n+      \"--reviewer-executable\", process.execPath,\n+      \"--reviewer-argument\", CONTENT_AWARE_REVIEWER,\n+      \"--reviewer-id\", \"content-reviewer-1\",\n+    );\n+  }\n+  return args;\n }\n \n function fileSha256(filePath: string): string {\n@@ -147,7 +168,11 @@ function databaseSchema(databasePath: string): unknown[] {\n   }\n }\n \n-function copiedSourceFixtureLayout(): { readonly anchor: URL; readonly reviewer: string } {\n+function copiedSourceFixtureLayout(): {\n+  readonly anchor: URL;\n+  readonly reviewer: string;\n+  readonly worker: string;\n+} {\n   const baseDirectory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-fixture-layout-\")));\n   temporaryDirectories.push(baseDirectory);\n   const moduleDirectory = path.join(baseDirectory, \"src\", \"fixtures\");\n@@ -155,15 +180,13 @@ function copiedSourceFixtureLayout(): { readonly anchor: URL; readonly reviewer:\n   mkdirSync(moduleDirectory, { recursive: true });\n   mkdirSync(fixtureDirectory);\n   const root = path.resolve(import.meta.dirname, \"../..\");\n-  copyFileSync(\n-    path.join(root, \"fixtures\", \"deterministic-worker.mjs\"),\n-    path.join(fixtureDirectory, \"deterministic-worker.mjs\"),\n-  );\n+  const worker = path.join(fixtureDirectory, \"deterministic-worker.mjs\");\n+  copyFileSync(path.join(root, \"fixtures\", \"deterministic-worker.mjs\"), worker);\n   const reviewer = path.join(fixtureDirectory, \"deterministic-reviewer.mjs\");\n   copyFileSync(path.join(root, \"fixtures\", \"deterministic-reviewer.mjs\"), reviewer);\n   const anchorPath = path.join(moduleDirectory, \"bundled-fixtures.ts\");\n   writeFileSync(anchorPath, \"// isolated resolver anchor\\n\", \"utf8\");\n-  return { anchor: pathToFileURL(anchorPath), reviewer };\n+  return { anchor: pathToFileURL(anchorPath), reviewer, worker };\n }\n \n describe(\"Zentra CLI\", () => {\n@@ -216,6 +239,59 @@ describe(\"Zentra CLI\", () => {\n     expect(status.json).toEqual({ command: \"task.status\", task: run.json.task });\n   });\n \n+  it(\"denies by default without reviewer configuration before commit or integration\", async () => {\n+    const testFixture = await fixture();\n+    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n+\n+    const result = await invoke(runArguments(testFixture, \"task-no-reviewer\", false));\n+\n+    expect(result).toMatchObject({\n+      code: 1,\n+      stdout: \"\",\n+      json: {\n+        command: \"task.run\",\n+        outcome: \"denied\",\n+        task: {\n+          taskId: \"task-no-reviewer\",\n+          lifecycle: \"terminal\",\n+          terminalOutcome: \"denied\",\n+        },\n+      },\n+    });\n+    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n+    expect((await new GitClient().run(testFixture.repositoryPath, [\n+      \"show-ref\",\n+      \"--verify\",\n+      \"refs/heads/zentra/integration\",\n+    ])).exitCode).not.toBe(0);\n+    expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\"))).toBe(false);\n+  });\n+\n+  it(\"denies an adversarial diff that passes focused validation without committing it\", async () => {\n+    const testFixture = await fixture();\n+    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n+    const args = [\n+      ...runArguments(testFixture, \"task-dangerous-review\"),\n+      \"--file\", \"auth.ts\",\n+      \"--content\", \"export const requireAuthentication = false;\\n\",\n+    ];\n+\n+    const result = await invoke(args);\n+\n+    expect(result).toMatchObject({\n+      code: 1,\n+      stdout: \"\",\n+      json: {\n+        command: \"task.run\",\n+        outcome: \"denied\",\n+        task: { terminalOutcome: \"denied\" },\n+      },\n+    });\n+    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n+    expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:auth.ts\"]))\n+      .toBe(\"export const requireAuthentication = true;\");\n+  });\n+\n   it(\"returns a successful no-op recovery decision for a completed task\", async () => {\n     const testFixture = await fixture();\n     await invoke(runArguments(testFixture, \"task-recover\"));\n@@ -877,7 +953,7 @@ describe(\"Zentra CLI\", () => {\n     });\n   });\n \n-  it.each([\"invalid layout\", \"missing fixture\", \"modified reviewer\"] as const)(\n+  it.each([\"invalid layout\", \"missing worker\", \"modified worker\"] as const)(\n     \"rejects %s before the task journal, reviewer adapter, or worktree is reached\",\n     async (failure) => {\n       const testFixture = await fixture();\n@@ -885,10 +961,10 @@ describe(\"Zentra CLI\", () => {\n       let anchor = layout.anchor;\n       if (failure === \"invalid layout\") {\n         anchor = pathToFileURL(path.join(path.dirname(layout.reviewer), \"bundled-fixtures.ts\"));\n-      } else if (failure === \"missing fixture\") {\n-        rmSync(layout.reviewer);\n+      } else if (failure === \"missing worker\") {\n+        rmSync(layout.worker);\n       } else {\n-        writeFileSync(layout.reviewer, \"process.exit(0);\\n\", \"utf8\");\n+        writeFileSync(layout.worker, \"process.exit(0);\\n\", \"utf8\");\n       }\n \n       const result = await invoke(\n@@ -934,7 +1010,7 @@ describe(\"built CLI help\", () => {\n     expect(result.stdout).toMatch(/\\brecover\\b/);\n   });\n \n-  it(\"runs the bundled worker and reviewer through the built entry point\", async () => {\n+  it(\"runs the bundled worker and configured content-aware reviewer through the built entry point\", async () => {\n     const root = path.resolve(import.meta.dirname, \"../..\");\n     const testFixture = await fixture();\n \n@@ -952,6 +1028,14 @@ describe(\"built CLI help\", () => {\n     });\n   });\n \n+  it(\"does not wire the deterministic test reviewer into the built production CLI\", () => {\n+    const root = path.resolve(import.meta.dirname, \"../..\");\n+    const builtCli = readFileSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), \"utf8\");\n+\n+    expect(builtCli).not.toContain(\"deterministic-reviewer.mjs\");\n+    expect(builtCli).not.toContain(\"DeterministicReviewerAdapter\");\n+  });\n+\n   it(\"executes help through a real symlink to the built entry point\", async () => {\n     const root = path.resolve(import.meta.dirname, \"../..\");\n     const directory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-cli-link-\")));\ndiff --git a/tests/reviews/deterministic-reviewer-adapter.test.ts b/tests/reviews/deterministic-reviewer-adapter.test.ts\nindex 238dd5f..a52d29c 100644\n--- a/tests/reviews/deterministic-reviewer-adapter.test.ts\n+++ b/tests/reviews/deterministic-reviewer-adapter.test.ts\n@@ -2,6 +2,7 @@ import { afterEach, describe, expect, it } from \"vitest\";\n import { ProcessSupervisor } from \"../../src/workers/process-supervisor.js\";\n import {\n   DeterministicReviewerAdapter,\n+  ProcessReviewerAdapter,\n   ReviewerExecutionError,\n } from \"../../src/reviews/reviewer-adapter.js\";\n import type { ValidationReport } from \"../../src/capabilities/validation-runner.js\";\n@@ -36,6 +37,29 @@ function getFixturePath(): string {\n   return path.join(__dirname, \"../../fixtures/deterministic-reviewer.mjs\");\n }\n \n+function getContentAwareFixturePath(): string {\n+  return path.resolve(import.meta.dirname, \"../fixtures/content-aware-reviewer.mjs\");\n+}\n+\n+function processReviewer(\n+  fixture: string,\n+  options: { readonly timeoutMs?: number; readonly maxInputBytes?: number; readonly maxOutputBytes?: number } = {},\n+): ProcessReviewerAdapter {\n+  return new ProcessReviewerAdapter({\n+    executable: process.execPath,\n+    args: [fixture],\n+    timeoutMs: options.timeoutMs ?? 5_000,\n+    ...(options.maxInputBytes === undefined ? {} : { maxInputBytes: options.maxInputBytes }),\n+    ...(options.maxOutputBytes === undefined ? {} : { maxOutputBytes: options.maxOutputBytes }),\n+  });\n+}\n+\n+function reviewerScript(source: string): string {\n+  const fixture = path.join(makeTempDir(), \"reviewer.mjs\");\n+  writeFileSync(fixture, source, \"utf8\");\n+  return fixture;\n+}\n+\n describe(\"DeterministicReviewerAdapter\", () => {\n   const validationReport: ValidationReport = {\n     name: \"focused\",\n@@ -97,6 +121,34 @@ describe(\"DeterministicReviewerAdapter\", () => {\n     expect(decision.approved).toBe(true);\n   });\n \n+  it(\"denies a dangerous diff even when focused validation passes\", async () => {\n+    const adapter = processReviewer(getContentAwareFixturePath());\n+    const dangerousDiff = [\n+      \"diff --git a/src/auth.ts b/src/auth.ts\",\n+      \"--- a/src/auth.ts\",\n+      \"+++ b/src/auth.ts\",\n+      \"@@ -1 +1 @@\",\n+      \"-export const requireAuthentication = true;\",\n+      \"+export const requireAuthentication = false;\",\n+      \"\",\n+    ].join(\"\\n\");\n+\n+    const decision = await adapter.review(\n+      {\n+        workerId: \"worker-1\",\n+        reviewerId: \"reviewer-1\",\n+        diff: dangerousDiff,\n+        validation: validationReport,\n+      },\n+      AbortSignal.timeout(5_000),\n+    );\n+\n+    expect(validationReport.outcome).toBe(\"completed\");\n+    expect(validationReport.exitCode).toBe(0);\n+    expect(decision.approved).toBe(false);\n+    expect(decision.reason).toMatch(/authentication|dangerous|security/i);\n+  });\n+\n   it(\"rejects when worker and reviewer have the same identity\", async () => {\n     const supervisor = new ProcessSupervisor();\n     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n@@ -252,3 +304,120 @@ describe(\"DeterministicReviewerAdapter\", () => {\n     },\n   );\n });\n+\n+describe(\"ProcessReviewerAdapter\", () => {\n+  const validationReport: ValidationReport = {\n+    name: \"focused\",\n+    outcome: \"completed\",\n+    exitCode: 0,\n+    stdout: \"focused validation passed\",\n+    stderr: \"\",\n+    startedAt: \"2026-01-01T00:00:00.000Z\",\n+    finishedAt: \"2026-01-01T00:00:01.000Z\",\n+    command: [\"node\", \"--test\"],\n+    argvSha256: createHash(\"sha256\").update(JSON.stringify([\"node\", \"--test\"])).digest(\"hex\"),\n+    outputSha256: createHash(\"sha256\")\n+      .update(JSON.stringify({ stdout: \"focused validation passed\", stderr: \"\" }))\n+      .digest(\"hex\"),\n+    provenance: {\n+      invocationId: \"content-review-validation\",\n+      canonicalCwd: \"/tmp\",\n+      subjectSha256: \"content-review-subject\",\n+    },\n+  };\n+  const input = {\n+    workerId: \"worker-1\",\n+    reviewerId: \"reviewer-1\",\n+    diff: \"diff --git a/file b/file\\n-old\\n+new\\n\",\n+    validation: validationReport,\n+  } as const;\n+\n+  it(\"sends the exact diff and validation evidence through stdin rather than argv\", async () => {\n+    const decision = await processReviewer(getContentAwareFixturePath()).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    );\n+\n+    expect(decision.approved).toBe(true);\n+    expect(decision.reason).toContain(validationReport.stdout);\n+  });\n+\n+  it(\"rejects reviewer input that exceeds its byte bound before spawning\", async () => {\n+    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n+    const adapter = processReviewer(fixture, { maxInputBytes: 128 });\n+\n+    await expect(adapter.review(\n+      { ...input, diff: \"x\".repeat(129) },\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/input.*limit|evidence.*large|bounded/i);\n+  });\n+\n+  it(\"fails closed when the reviewer returns an evidence digest mismatch\", async () => {\n+    const fixture = reviewerScript(`\n+      let body = \"\";\n+      process.stdin.setEncoding(\"utf8\");\n+      for await (const chunk of process.stdin) body += chunk;\n+      const request = JSON.parse(body);\n+      console.log(JSON.stringify({\n+        reviewerId: request.reviewerId,\n+        decision: \"approve\",\n+        diffSha256: \"0\".repeat(64),\n+        validationSha256: request.validationSha256,\n+        decidedAt: new Date().toISOString(),\n+        reason: \"reviewed\"\n+      }));\n+    `);\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/diff.*digest.*mismatch|evidence.*mismatch/i);\n+  });\n+\n+  it(\"fails closed when the reviewer times out\", async () => {\n+    const fixture = reviewerScript(\"setInterval(() => {}, 1_000);\");\n+\n+    await expect(processReviewer(fixture, { timeoutMs: 20 }).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toEqual(expect.objectContaining({ outcome: \"timed_out\" }));\n+  });\n+\n+  it(\"fails closed on malformed reviewer output\", async () => {\n+    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => console.log(\"not json\"));');\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/invalid|protocol|json/i);\n+  });\n+\n+  it(\"fails closed when reviewer output exceeds its byte bound\", async () => {\n+    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => process.stdout.write(\"x\".repeat(1_024)));');\n+\n+    await expect(processReviewer(fixture, { maxOutputBytes: 128 }).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/output.*limit|truncat/i);\n+  });\n+\n+  it(\"requires exactly one reviewer decision\", async () => {\n+    const fixture = reviewerScript(`\n+      let body = \"\";\n+      process.stdin.setEncoding(\"utf8\");\n+      for await (const chunk of process.stdin) body += chunk;\n+      const request = JSON.parse(body);\n+      const decision = JSON.stringify({ reviewerId: request.reviewerId, decision: \"deny\", diffSha256: request.diffSha256, validationSha256: request.validationSha256, decidedAt: new Date().toISOString(), reason: \"denied\" });\n+      console.log(decision);\n+      console.log(decision);\n+    `);\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/exactly one|single.*decision|protocol/i);\n+  });\n+\n+  it(\"rejects matching worker and reviewer identities before spawning\", async () => {\n+    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n+\n+    await expect(processReviewer(fixture).review(\n+      { ...input, reviewerId: input.workerId },\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/identity|differ|same/i);\n+  });\n+});\n","metadata":{"output":"...\n\nd);\n       assertSafeTitle(options.title);\n@@ -195,18 +207,33 @@ function createProgram(\n         projectId: projectConfig.projectId,\n         title: options.title,\n       });\n-      const fixtures = attestedFixtures(fixtureAnchor);\n+      const reviewer = configuredReviewer(options);\n+      if (reviewer === null) {\n+        const denied = await withSystem(\n+          options.database,\n+          configs,\n+          \"read-write\",\n+          (system) => Promise.resolve(denyMissingReviewer(system.tasks, {\n+            taskId: options.taskId,\n+            projectId: projectConfig.projectId,\n+            title: options.title,\n+          })),\n+        );\n+        setResult(taskRunResult(denied));\n+        return;\n+      }\n+      const workerFixture = attestedWorkerFixture(fixtureAnchor);\n       const result = await withSystem(options.database, configs, \"read-write\", async (system) => {\n-        const taskView = await system.execution(fixtures).orchestrator.run({\n+        const taskView = await system.execution(workerFixture, reviewer.adapter).orchestrator.run({\n           taskId: options.taskId,\n           projectId: projectConfig.projectId,\n           title: options.title,\n           workerId: WORKER_ID,\n-          reviewerId: REVIEWER_ID,\n+          reviewerId: reviewer.reviewerId,\n           workerRequest: {\n             executable: process.execPath,\n             args: [\n-              fixtures[\"deterministic-worker.mjs\"],\n+              workerFixture,\n               \"--file\",\n               options.file,\n               \"--content\",\n@@ -316,7 +343,7 @@ function composeSystem(\n       const { projects, git, worktrees } = developmentDependencies();\n       return new RecoveryService(journal, tasks, projects, worktrees, git);\n     },\n-    execution(fixtures: BundledFixturePaths) {\n+    execution(workerFixture: string, reviewer: ReviewerAdapter) {\n       const { projects, git, worktrees } = developmentDependencies();\n       const supervisor = new ProcessSupervisor();\n       const validations = new ValidationRunner(supervisor);\n@@ -327,10 +354,7 @@ function composeSystem(\n           worktrees,\n           supervisor,\n           validations,\n-          new DeterministicReviewerAdapter(\n-            supervisor,\n-            fixtures[\"deterministic-reviewer.mjs\"],\n-          ),\n+          reviewer,\n           new ReviewGate(),\n           new IntegrationQueue(git, validations),\n         ),\n@@ -373,14 +397,61 @@ function loadProjects(configPath: string): readonly ProjectConfig[] {\n   }\n }\n \n-function attestedFixtures(anchor?: string | URL): BundledFixturePaths {\n+function attestedWorkerFixture(anchor?: string | URL): string {\n   try {\n-    return anchor === undefined ? resolveBundledFixtures() : resolveBundledFixtures(anchor);\n+    return anchor === undefined\n+      ? resolveBundledFixture(\"deterministic-worker.mjs\")\n+      : resolveBundledFixture(\"deterministic-worker.mjs\", anchor);\n   } catch {\n     throw new CliFailure(\"BUNDLED_FIXTURE_INVALID\");\n   }\n }\n \n+function configuredReviewer(options: RunOptions): {\n+  readonly reviewerId: string;\n+  readonly adapter: ReviewerAdapter;\n+} | null {\n+  const executable = options.reviewerExecutable;\n+  const reviewerId = options.reviewerId;\n+  if (executable === undefined && reviewerId === undefined && options.reviewerArgument.length === 0) {\n+    return null;\n+  }\n+  if (\n+    executable === undefined ||\n+    executable.length === 0 ||\n+    reviewerId === undefined ||\n+    reviewerId.length === 0\n+  ) {\n+    throw new CliFailure(\"INVALID_COMMAND\");\n+  }\n+  return {\n+    reviewerId,\n+    adapter: new ProcessReviewerAdapter({\n+      executable,\n+      args: options.reviewerArgument,\n+    }),\n+  };\n+}\n+\n+function denyMissingReviewer(\n+  tasks: TaskService,\n+  input: { readonly taskId: string; readonly projectId: string; readonly title: string },\n+): TaskView {\n+  tasks.create({\n+    ...input,\n+    correlationId: input.taskId,\n+  });\n+  return tasks.append(\n+    input.taskId,\n+    \"task.denied\",\n+    {\n+      stage: \"setup\",\n+      reason: \"content-aware reviewer is not configured\",\n+    },\n+    null,\n+  );\n+}\n+\n function assertSafeRootFile(candidate: string): void {\n   if (\n     candidate.length === 0 ||\ndiff --git a/src/reviews/reviewer-adapter.ts b/src/reviews/reviewer-adapter.ts\nindex fec96ed..74a606b 100644\n--- a/src/reviews/reviewer-adapter.ts\n+++ b/src/reviews/reviewer-adapter.ts\n@@ -1,4 +1,5 @@\n import { createHash } from \"node:crypto\";\n+import { spawn } from \"node:child_process\";\n import { z } from \"zod\";\n import type { ProcessSupervisor } from \"../workers/process-supervisor.js\";\n import type { ValidationReport } from \"../capabilities/validation-runner.js\";\n@@ -31,7 +32,7 @@ export class ReviewerExecutionError extends Error {\n     stderr: string,\n   ) {\n     super(\n-      `reviewer fixture failed: outcome=${outcome}${\n+      `reviewer process failed: outcome=${outcome}${\n         stderr === \"\" ? \"\" : `, stderr=${stderr}`\n       }`,\n     );\n@@ -47,6 +48,29 @@ export const ReviewDecisionSchema = z.strictObject({\n   reason: z.string().min(1),\n });\n \n+const ProcessReviewDecisionSchema = z.strictObject({\n+  reviewerId: z.string().min(1),\n+  decision: z.enum([\"approve\", \"deny\"]),\n+  diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n+  validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n+  decidedAt: z.string().datetime({ offset: true }),\n+  reason: z.string().min(1).max(4_096),\n+});\n+\n+const REVIEWER_ENV_ALLOWLIST = [\"PATH\", \"HOME\", \"TMPDIR\", \"LANG\", \"LC_ALL\"] as const;\n+const DEFAULT_REVIEWER_TIMEOUT_MS = 30_000;\n+const DEFAULT_MAX_REVIEW_INPUT_BYTES = 2 * 1_024 * 1_024;\n+const DEFAULT_MAX_REVIEW_OUTPUT_BYTES = 16 * 1_024;\n+\n+export interface ProcessReviewerOptions {\n+  readonly executable: string;\n+  readonly args?: readonly string[];\n+  readonly cwd?: string;\n+  readonly timeoutMs?: number;\n+  readonly maxInputBytes?: number;\n+  readonly maxOutputBytes?: number;\n+}\n+\n function sha256(value: string): string {\n   return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n }\n@@ -78,6 +102,176 @@ export function canonicalValidationDigest(validation: ValidationReport): string\n   return sha256(canonical);\n }\n \n+export class ProcessReviewerAdapter implements ReviewerAdapter {\n+  private readonly args: readonly string[];\n+  private readonly cwd: string;\n+  private readonly timeoutMs: number;\n+  private readonly maxInputBytes: number;\n+  private readonly maxOutputBytes: number;\n+\n+  constructor(private readonly options: ProcessReviewerOptions) {\n+    this.args = options.args ?? [];\n+    this.cwd = options.cwd ?? \"/tmp\";\n+    this.timeoutMs = options.timeoutMs ?? DEFAULT_REVIEWER_TIMEOUT_MS;\n+    this.maxInputBytes = options.maxInputBytes ?? DEFAULT_MAX_REVIEW_INPUT_BYTES;\n+    this.maxOutputBytes = options.maxOutputBytes ?? DEFAULT_MAX_REVIEW_OUTPUT_BYTES;\n+    for (const [name, value] of [\n+      [\"timeoutMs\", this.timeoutMs],\n+      [\"maxInputBytes\", this.maxInputBytes],\n+      [\"maxOutputBytes\", this.maxOutputBytes],\n+    ] as const) {\n+      if (!Number.isSafeInteger(value) || value <= 0) {\n+        throw new Error(`${name} must be a positive safe integer`);\n+      }\n+    }\n+  }\n+\n+  async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n+    if (input.workerId === input.reviewerId) {\n+      throw new Error(\n+        `reviewer identity must differ from worker identity: ${input.workerId}`,\n+      );\n+    }\n+\n+    const diffSha256 = sha256(input.diff);\n+    const validationSha256 = canonicalValidationDigest(input.validation);\n+    const request = JSON.stringify({\n+      schemaVersion: 1,\n+      workerId: input.workerId,\n+      reviewerId: input.reviewerId,\n+      diff: input.diff,\n+      validation: input.validation,\n+      diffSha256,\n+      validationSha256,\n+    });\n+    const requestBytes = Buffer.byteLength(request, \"utf8\");\n+    if (requestBytes > this.maxInputBytes) {\n+      throw new Error(\n+        `reviewer protocol input exceeded bounded evidence limit of ${this.maxInputBytes} bytes`,\n+      );\n+    }\n+\n+    const output = await this.execute(request, signal);\n+    const lines = output.trim().split(\"\\n\").filter((line) => line.trim() !== \"\");\n+    if (lines.length !== 1) {\n+      throw new Error(\n+        `reviewer protocol requires exactly one decision, received ${lines.length}`,\n+      );\n+    }\n+\n+    let decoded: unknown;\n+    try {\n+      decoded = JSON.parse(lines[0]!);\n+    } catch {\n+      throw new Error(\"reviewer protocol returned invalid JSON\");\n+    }\n+    const parsed = ProcessReviewDecisionSchema.safeParse(decoded);\n+    if (!parsed.success) {\n+      throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n+    }\n+    if (parsed.data.reviewerId !== input.reviewerId) {\n+      throw new Error(\"reviewer protocol returned a reviewer identity mismatch\");\n+    }\n+    if (parsed.data.diffSha256 !== diffSha256) {\n+      throw new Error(\"reviewer protocol returned a diff evidence digest mismatch\");\n+    }\n+    if (parsed.data.validationSha256 !== validationSha256) {\n+      throw new Error(\"reviewer protocol returned a validation evidence digest mismatch\");\n+    }\n+\n+    return ReviewDecisionSchema.parse({\n+      reviewerId: parsed.data.reviewerId,\n+      approved: parsed.data.decision === \"approve\",\n+      diffSha256: parsed.data.diffSha256,\n+      validationSha256: parsed.data.validationSha256,\n+      decidedAt: parsed.data.decidedAt,\n+      reason: parsed.data.reason,\n+    });\n+  }\n+\n+  private execute(request: string, signal: AbortSignal): Promise<string> {\n+    if (signal.aborted) {\n+      return Promise.reject(new ReviewerExecutionError(\"cancelled\", \"\"));\n+    }\n+\n+    return new Promise((resolve, reject) => {\n+      const env: Record<string, string> = {};\n+      for (const key of REVIEWER_ENV_ALLOWLIST) {\n+        const value = process.env[key];\n+        if (value !== undefined) env[key] = value;\n+      }\n+      const child = spawn(this.options.executable, [...this.args], {\n+        cwd: this.cwd,\n+        shell: false,\n+        detached: true,\n+        env,\n+        stdio: [\"pipe\", \"pipe\", \"pipe\"],\n+      });\n+      const stdout: Buffer[] = [];\n+      const stderr: Buffer[] = [];\n+      let outputBytes = 0;\n+      let outcome: \"cancelled\" | \"timed_out\" | \"failed\" | undefined;\n+      let reason = \"\";\n+      let settled = false;\n+\n+      const kill = (): void => {\n+        if (child.pid === undefined) return;\n+        try {\n+          process.kill(-child.pid, \"SIGKILL\");\n+        } catch {\n+          child.kill(\"SIGKILL\");\n+        }\n+      };\n+      const stop = (\n+        nextOutcome: \"cancelled\" | \"timed_out\" | \"failed\",\n+        nextReason: string,\n+      ): void => {\n+        if (outcome !== undefined) return;\n+        outcome = nextOutcome;\n+        reason = nextReason;\n+        kill();\n+      };\n+      const timer = setTimeout(\n+        () => stop(\"timed_out\", \"reviewer timeout exceeded\"),\n+        this.timeoutMs,\n+      );\n+      const onAbort = (): void => stop(\"cancelled\", \"reviewer cancelled\");\n+      signal.addEventListener(\"abort\", onAbort, { once: true });\n+      const capture = (target: Buffer[], chunk: Buffer): void => {\n+        const remaining = this.maxOutputBytes - outputBytes;\n+        if (remaining > 0) target.push(chunk.subarray(0, Math.min(remaining, chunk.byteLength)));\n+        outputBytes += chunk.byteLength;\n+        if (outputBytes > this.maxOutputBytes) {\n+          stop(\"failed\", `reviewer output limit of ${this.maxOutputBytes} bytes exceeded`);\n+        }\n+      };\n+      child.stdout.on(\"data\", (chunk: Buffer) => capture(stdout, chunk));\n+      child.stderr.on(\"data\", (chunk: Buffer) => capture(stderr, chunk));\n+      child.on(\"error\", (error) => stop(\"failed\", `reviewer spawn failed: ${error.message}`));\n+      child.on(\"close\", (code) => {\n+        if (settled) return;\n+        settled = true;\n+        clearTimeout(timer);\n+        signal.removeEventListener(\"abort\", onAbort);\n+        const stderrText = Buffer.concat(stderr).toString(\"utf8\");\n+        if (outcome !== undefined) {\n+          reject(new ReviewerExecutionError(outcome, reason || stderrText));\n+          return;\n+        }\n+        if (code !== 0) {\n+          reject(new ReviewerExecutionError(\"failed\", stderrText));\n+          return;\n+        }\n+        resolve(Buffer.concat(stdout).toString(\"utf8\"));\n+      });\n+      child.stdin.on(\"error\", () => {\n+        // The close event reports the authoritative process outcome.\n+      });\n+      child.stdin.end(request);\n+    });\n+  }\n+}\n+\n export class DeterministicReviewerAdapter implements ReviewerAdapter {\n   constructor(\n     private readonly supervisor: ProcessSupervisor,\ndiff --git a/tests/fixtures/content-aware-reviewer.mjs b/tests/fixtures/content-aware-reviewer.mjs\nnew file mode 100644\nindex 0000000..8a81098\n--- /dev/null\n+++ b/tests/fixtures/content-aware-reviewer.mjs\n@@ -0,0 +1,26 @@\n+#!/usr/bin/env node\n+\n+import { createHash } from \"node:crypto\";\n+\n+if (process.argv.length !== 2) {\n+  throw new Error(\"review evidence must be provided only through stdin\");\n+}\n+\n+let input = \"\";\n+process.stdin.setEncoding(\"utf8\");\n+for await (const chunk of process.stdin) input += chunk;\n+\n+const request = JSON.parse(input);\n+const dangerous = request.diff.includes(\"requireAuthentication = false\");\n+const decision = {\n+  reviewerId: request.reviewerId,\n+  decision: dangerous ? \"deny\" : \"approve\",\n+  diffSha256: createHash(\"sha256\").update(request.diff, \"utf8\").digest(\"hex\"),\n+  validationSha256: request.validationSha256,\n+  decidedAt: new Date().toISOString(),\n+  reason: dangerous\n+    ? \"Denied dangerous authentication bypass in the reviewed diff\"\n+    : `Reviewed exact diff and validation output: ${request.validation.stdout}`,\n+};\n+\n+process.stdout.write(`${JSON.stringify(decision)}\\n`);\ndiff --git a/tests/orchestration/cli.test.ts b/tests/orchestration/cli.test.ts\nindex a6b8eec..c1c416e 100644\n--- a/tests/orchestration/cli.test.ts\n+++ b/tests/orchestration/cli.test.ts\n@@ -30,6 +30,10 @@ import { GitClient } from \"../../src/workspaces/git-client.js\";\n const execFileAsync = promisify(execFile);\n const temporaryDirectories: string[] = [];\n const ATTACKER_MARKER = \"ATTACKER_MARKER_DO_NOT_PRINT\";\n+const CONTENT_AWARE_REVIEWER = path.resolve(\n+  import.meta.dirname,\n+  \"../fixtures/content-aware-reviewer.mjs\",\n+);\n \n interface Fixture {\n   readonly baseDirectory: string;\n@@ -72,9 +76,14 @@ async function fixture(): Promise<Fixture> {\n   await gitOk(repositoryPath, [\"config\", \"user.email\", \"fixture@zentra.local\"]);\n   mkdirSync(path.join(repositoryPath, \"test\"));\n   writeFileSync(path.join(repositoryPath, \"greeting.txt\"), \"hello\\n\", \"utf8\");\n+  writeFileSync(\n+    path.join(repositoryPath, \"auth.ts\"),\n+    \"export const requireAuthentication = true;\\n\",\n+    \"utf8\",\n+  );\n   writeFileSync(\n     path.join(repositoryPath, \"test\", \"greeting.test.mjs\"),\n-    'import assert from \"node:assert/strict\";\\nimport { readFile } from \"node:fs/promises\";\\nimport test from \"node:test\";\\ntest(\"greeting\", async () => assert.equal(await readFile(new URL(\"../greeting.txt\", import.meta.url), \"utf8\"), \"hello from CLI\\\\n\"));\\n',\n+    'import assert from \"node:assert/strict\";\\nimport { readFile } from \"node:fs/promises\";\\nimport test from \"node:test\";\\ntest(\"greeting\", async () => assert.match(await readFile(new URL(\"../greeting.txt\", import.meta.url), \"utf8\"), /^hello(?: from CLI)?\\\\n$/));\\n',\n     \"utf8\",\n   );\n   writeFileSync(\n@@ -120,8 +129,12 @@ async function invoke(\n   };\n }\n \n-function runArguments(testFixture: Fixture, taskId = \"task-cli\"): readonly string[] {\n-  return [\n+function runArguments(\n+  testFixture: Fixture,\n+  taskId = \"task-cli\",\n+  configureReviewer = true,\n+): readonly string[] {\n+  const args = [\n     \"task\", \"run\",\n     \"--config\", testFixture.configPath,\n     \"--database\", testFixture.databasePath,\n@@ -130,6 +143,14 @@ function runArguments(testFixture: Fixture, taskId = \"task-cli\"): readonly strin\n     \"--file\", \"greeting.txt\",\n     \"--content\", \"hello from CLI\\n\",\n   ];\n+  if (configureReviewer) {\n+    args.push(\n+      \"--reviewer-executable\", process.execPath,\n+      \"--reviewer-argument\", CONTENT_AWARE_REVIEWER,\n+      \"--reviewer-id\", \"content-reviewer-1\",\n+    );\n+  }\n+  return args;\n }\n \n function fileSha256(filePath: string): string {\n@@ -147,7 +168,11 @@ function databaseSchema(databasePath: string): unknown[] {\n   }\n }\n \n-function copiedSourceFixtureLayout(): { readonly anchor: URL; readonly reviewer: string } {\n+function copiedSourceFixtureLayout(): {\n+  readonly anchor: URL;\n+  readonly reviewer: string;\n+  readonly worker: string;\n+} {\n   const baseDirectory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-fixture-layout-\")));\n   temporaryDirectories.push(baseDirectory);\n   const moduleDirectory = path.join(baseDirectory, \"src\", \"fixtures\");\n@@ -155,15 +180,13 @@ function copiedSourceFixtureLayout(): { readonly anchor: URL; readonly reviewer:\n   mkdirSync(moduleDirectory, { recursive: true });\n   mkdirSync(fixtureDirectory);\n   const root = path.resolve(import.meta.dirname, \"../..\");\n-  copyFileSync(\n-    path.join(root, \"fixtures\", \"deterministic-worker.mjs\"),\n-    path.join(fixtureDirectory, \"deterministic-worker.mjs\"),\n-  );\n+  const worker = path.join(fixtureDirectory, \"deterministic-worker.mjs\");\n+  copyFileSync(path.join(root, \"fixtures\", \"deterministic-worker.mjs\"), worker);\n   const reviewer = path.join(fixtureDirectory, \"deterministic-reviewer.mjs\");\n   copyFileSync(path.join(root, \"fixtures\", \"deterministic-reviewer.mjs\"), reviewer);\n   const anchorPath = path.join(moduleDirectory, \"bundled-fixtures.ts\");\n   writeFileSync(anchorPath, \"// isolated resolver anchor\\n\", \"utf8\");\n-  return { anchor: pathToFileURL(anchorPath), reviewer };\n+  return { anchor: pathToFileURL(anchorPath), reviewer, worker };\n }\n \n describe(\"Zentra CLI\", () => {\n@@ -216,6 +239,59 @@ describe(\"Zentra CLI\", () => {\n     expect(status.json).toEqual({ command: \"task.status\", task: run.json.task });\n   });\n \n+  it(\"denies by default without reviewer configuration before commit or integration\", async () => {\n+    const testFixture = await fixture();\n+    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n+\n+    const result = await invoke(runArguments(testFixture, \"task-no-reviewer\", false));\n+\n+    expect(result).toMatchObject({\n+      code: 1,\n+      stdout: \"\",\n+      json: {\n+        command: \"task.run\",\n+        outcome: \"denied\",\n+        task: {\n+          taskId: \"task-no-reviewer\",\n+          lifecycle: \"terminal\",\n+          terminalOutcome: \"denied\",\n+        },\n+      },\n+    });\n+    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n+    expect((await new GitClient().run(testFixture.repositoryPath, [\n+      \"show-ref\",\n+      \"--verify\",\n+      \"refs/heads/zentra/integration\",\n+    ])).exitCode).not.toBe(0);\n+    expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\"))).toBe(false);\n+  });\n+\n+  it(\"denies an adversarial diff that passes focused validation without committing it\", async () => {\n+    const testFixture = await fixture();\n+    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n+    const args = [\n+      ...runArguments(testFixture, \"task-dangerous-review\"),\n+      \"--file\", \"auth.ts\",\n+      \"--content\", \"export const requireAuthentication = false;\\n\",\n+    ];\n+\n+    const result = await invoke(args);\n+\n+    expect(result).toMatchObject({\n+      code: 1,\n+      stdout: \"\",\n+      json: {\n+        command: \"task.run\",\n+        outcome: \"denied\",\n+        task: { terminalOutcome: \"denied\" },\n+      },\n+    });\n+    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n+    expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:auth.ts\"]))\n+      .toBe(\"export const requireAuthentication = true;\");\n+  });\n+\n   it(\"returns a successful no-op recovery decision for a completed task\", async () => {\n     const testFixture = await fixture();\n     await invoke(runArguments(testFixture, \"task-recover\"));\n@@ -877,7 +953,7 @@ describe(\"Zentra CLI\", () => {\n     });\n   });\n \n-  it.each([\"invalid layout\", \"missing fixture\", \"modified reviewer\"] as const)(\n+  it.each([\"invalid layout\", \"missing worker\", \"modified worker\"] as const)(\n     \"rejects %s before the task journal, reviewer adapter, or worktree is reached\",\n     async (failure) => {\n       const testFixture = await fixture();\n@@ -885,10 +961,10 @@ describe(\"Zentra CLI\", () => {\n       let anchor = layout.anchor;\n       if (failure === \"invalid layout\") {\n         anchor = pathToFileURL(path.join(path.dirname(layout.reviewer), \"bundled-fixtures.ts\"));\n-      } else if (failure === \"missing fixture\") {\n-        rmSync(layout.reviewer);\n+      } else if (failure === \"missing worker\") {\n+        rmSync(layout.worker);\n       } else {\n-        writeFileSync(layout.reviewer, \"process.exit(0);\\n\", \"utf8\");\n+        writeFileSync(layout.worker, \"process.exit(0);\\n\", \"utf8\");\n       }\n \n       const result = await invoke(\n@@ -934,7 +1010,7 @@ describe(\"built CLI help\", () => {\n     expect(result.stdout).toMatch(/\\brecover\\b/);\n   });\n \n-  it(\"runs the bundled worker and reviewer through the built entry point\", async () => {\n+  it(\"runs the bundled worker and configured content-aware reviewer through the built entry point\", async () => {\n     const root = path.resolve(import.meta.dirname, \"../..\");\n     const testFixture = await fixture();\n \n@@ -952,6 +1028,14 @@ describe(\"built CLI help\", () => {\n     });\n   });\n \n+  it(\"does not wire the deterministic test reviewer into the built production CLI\", () => {\n+    const root = path.resolve(import.meta.dirname, \"../..\");\n+    const builtCli = readFileSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), \"utf8\");\n+\n+    expect(builtCli).not.toContain(\"deterministic-reviewer.mjs\");\n+    expect(builtCli).not.toContain(\"DeterministicReviewerAdapter\");\n+  });\n+\n   it(\"executes help through a real symlink to the built entry point\", async () => {\n     const root = path.resolve(import.meta.dirname, \"../..\");\n     const directory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-cli-link-\")));\ndiff --git a/tests/reviews/deterministic-reviewer-adapter.test.ts b/tests/reviews/deterministic-reviewer-adapter.test.ts\nindex 238dd5f..a52d29c 100644\n--- a/tests/reviews/deterministic-reviewer-adapter.test.ts\n+++ b/tests/reviews/deterministic-reviewer-adapter.test.ts\n@@ -2,6 +2,7 @@ import { afterEach, describe, expect, it } from \"vitest\";\n import { ProcessSupervisor } from \"../../src/workers/process-supervisor.js\";\n import {\n   DeterministicReviewerAdapter,\n+  ProcessReviewerAdapter,\n   ReviewerExecutionError,\n } from \"../../src/reviews/reviewer-adapter.js\";\n import type { ValidationReport } from \"../../src/capabilities/validation-runner.js\";\n@@ -36,6 +37,29 @@ function getFixturePath(): string {\n   return path.join(__dirname, \"../../fixtures/deterministic-reviewer.mjs\");\n }\n \n+function getContentAwareFixturePath(): string {\n+  return path.resolve(import.meta.dirname, \"../fixtures/content-aware-reviewer.mjs\");\n+}\n+\n+function processReviewer(\n+  fixture: string,\n+  options: { readonly timeoutMs?: number; readonly maxInputBytes?: number; readonly maxOutputBytes?: number } = {},\n+): ProcessReviewerAdapter {\n+  return new ProcessReviewerAdapter({\n+    executable: process.execPath,\n+    args: [fixture],\n+    timeoutMs: options.timeoutMs ?? 5_000,\n+    ...(options.maxInputBytes === undefined ? {} : { maxInputBytes: options.maxInputBytes }),\n+    ...(options.maxOutputBytes === undefined ? {} : { maxOutputBytes: options.maxOutputBytes }),\n+  });\n+}\n+\n+function reviewerScript(source: string): string {\n+  const fixture = path.join(makeTempDir(), \"reviewer.mjs\");\n+  writeFileSync(fixture, source, \"utf8\");\n+  return fixture;\n+}\n+\n describe(\"DeterministicReviewerAdapter\", () => {\n   const validationReport: ValidationReport = {\n     name: \"focused\",\n@@ -97,6 +121,34 @@ describe(\"DeterministicReviewerAdapter\", () => {\n     expect(decision.approved).toBe(true);\n   });\n \n+  it(\"denies a dangerous diff even when focused validation passes\", async () => {\n+    const adapter = processReviewer(getContentAwareFixturePath());\n+    const dangerousDiff = [\n+      \"diff --git a/src/auth.ts b/src/auth.ts\",\n+      \"--- a/src/auth.ts\",\n+      \"+++ b/src/auth.ts\",\n+      \"@@ -1 +1 @@\",\n+      \"-export const requireAuthentication = true;\",\n+      \"+export const requireAuthentication = false;\",\n+      \"\",\n+    ].join(\"\\n\");\n+\n+    const decision = await adapter.review(\n+      {\n+        workerId: \"worker-1\",\n+        reviewerId: \"reviewer-1\",\n+        diff: dangerousDiff,\n+        validation: validationReport,\n+      },\n+      AbortSignal.timeout(5_000),\n+    );\n+\n+    expect(validationReport.outcome).toBe(\"completed\");\n+    expect(validationReport.exitCode).toBe(0);\n+    expect(decision.approved).toBe(false);\n+    expect(decision.reason).toMatch(/authentication|dangerous|security/i);\n+  });\n+\n   it(\"rejects when worker and reviewer have the same identity\", async () => {\n     const supervisor = new ProcessSupervisor();\n     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n@@ -252,3 +304,120 @@ describe(\"DeterministicReviewerAdapter\", () => {\n     },\n   );\n });\n+\n+describe(\"ProcessReviewerAdapter\", () => {\n+  const validationReport: ValidationReport = {\n+    name: \"focused\",\n+    outcome: \"completed\",\n+    exitCode: 0,\n+    stdout: \"focused validation passed\",\n+    stderr: \"\",\n+    startedAt: \"2026-01-01T00:00:00.000Z\",\n+    finishedAt: \"2026-01-01T00:00:01.000Z\",\n+    command: [\"node\", \"--test\"],\n+    argvSha256: createHash(\"sha256\").update(JSON.stringify([\"node\", \"--test\"])).digest(\"hex\"),\n+    outputSha256: createHash(\"sha256\")\n+      .update(JSON.stringify({ stdout: \"focused validation passed\", stderr: \"\" }))\n+      .digest(\"hex\"),\n+    provenance: {\n+      invocationId: \"content-review-validation\",\n+      canonicalCwd: \"/tmp\",\n+      subjectSha256: \"content-review-subject\",\n+    },\n+  };\n+  const input = {\n+    workerId: \"worker-1\",\n+    reviewerId: \"reviewer-1\",\n+    diff: \"diff --git a/file b/file\\n-old\\n+new\\n\",\n+    validation: validationReport,\n+  } as const;\n+\n+  it(\"sends the exact diff and validation evidence through stdin rather than argv\", async () => {\n+    const decision = await processReviewer(getContentAwareFixturePath()).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    );\n+\n+    expect(decision.approved).toBe(true);\n+    expect(decision.reason).toContain(validationReport.stdout);\n+  });\n+\n+  it(\"rejects reviewer input that exceeds its byte bound before spawning\", async () => {\n+    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n+    const adapter = processReviewer(fixture, { maxInputBytes: 128 });\n+\n+    await expect(adapter.review(\n+      { ...input, diff: \"x\".repeat(129) },\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/input.*limit|evidence.*large|bounded/i);\n+  });\n+\n+  it(\"fails closed when the reviewer returns an evidence digest mismatch\", async () => {\n+    const fixture = reviewerScript(`\n+      let body = \"\";\n+      process.stdin.setEncoding(\"utf8\");\n+      for await (const chunk of process.stdin) body += chunk;\n+      const request = JSON.parse(body);\n+      console.log(JSON.stringify({\n+        reviewerId: request.reviewerId,\n+        decision: \"approve\",\n+        diffSha256: \"0\".repeat(64),\n+        validationSha256: request.validationSha256,\n+        decidedAt: new Date().toISOString(),\n+        reason: \"reviewed\"\n+      }));\n+    `);\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/diff.*digest.*mismatch|evidence.*mismatch/i);\n+  });\n+\n+  it(\"fails closed when the reviewer times out\", async () => {\n+    const fixture = reviewerScript(\"setInterval(() => {}, 1_000);\");\n+\n+    await expect(processReviewer(fixture, { timeoutMs: 20 }).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toEqual(expect.objectContaining({ outcome: \"timed_out\" }));\n+  });\n+\n+  it(\"fails closed on malformed reviewer output\", async () => {\n+    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => console.log(\"not json\"));');\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/invalid|protocol|json/i);\n+  });\n+\n+  it(\"fails closed when reviewer output exceeds its byte bound\", async () => {\n+    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => process.stdout.write(\"x\".repeat(1_024)));');\n+\n+    await expect(processReviewer(fixture, { maxOutputBytes: 128 }).review(\n+      input,\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/output.*limit|truncat/i);\n+  });\n+\n+  it(\"requires exactly one reviewer decision\", async () => {\n+    const fixture = reviewerScript(`\n+      let body = \"\";\n+      process.stdin.setEncoding(\"utf8\");\n+      for await (const chunk of process.stdin) body += chunk;\n+      const request = JSON.parse(body);\n+      const decision = JSON.stringify({ reviewerId: request.reviewerId, decision: \"deny\", diffSha256: request.diffSha256, validationSha256: request.validationSha256, decidedAt: new Date().toISOString(), reason: \"denied\" });\n+      console.log(decision);\n+      console.log(decision);\n+    `);\n+\n+    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n+      .rejects.toThrow(/exactly one|single.*decision|protocol/i);\n+  });\n+\n+  it(\"rejects matching worker and reviewer identities before spawning\", async () => {\n+    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n+\n+    await expect(processReviewer(fixture).review(\n+      { ...input, reviewerId: input.workerId },\n+      AbortSignal.timeout(5_000),\n+    )).rejects.toThrow(/identity|differ|same/i);\n+  });\n+});\n","exit":0,"truncated":false},"title":"git diff \"$(git merge-base fix/pre-deployment HEAD)\"..HEAD","time":{"start":1783897842898,"end":1783897842901}},"metadata":{"azure":{"itemId":"fc_0c74edd636192079016a541ef4b2d48190a6c19989d57f2b32"}},"id":"prt_f5898e483001F65o48w1ptBlET","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898c6de001d15dRXL42stTuI"}}
{"type":"tool_use","timestamp":1783897842910,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"bash","callID":"call_apd4Kp0MSbcWd0dOWxIozr2y","state":{"status":"completed","input":{"command":"git log --oneline --decorate \"$(git merge-base fix/pre-deployment HEAD)\"..HEAD","workdir":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts","timeout":120000},"output":"0efdc61 (HEAD -> fix/predeploy-b-review-artifacts, origin/fix/predeploy-b-review-artifacts) docs: report issue 009 implementation\ne034742 fix: require content-aware independent review\n","metadata":{"output":"0efdc61 (HEAD -> fix/predeploy-b-review-artifacts, origin/fix/predeploy-b-review-artifacts) docs: report issue 009 implementation\ne034742 fix: require content-aware independent review\n","exit":0,"truncated":false},"title":"git log --oneline --decorate \"$(git merge-base fix/pre-deployment HEAD)\"..HEAD","time":{"start":1783897842902,"end":1783897842904}},"metadata":{"azure":{"itemId":"fc_0c74edd636192079016a541ef4b2e48190a42186fb33cc9b0b"}},"id":"prt_f5898e487001iwBrk1obfckOWQ","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898c6de001d15dRXL42stTuI"}}
{"type":"step_finish","timestamp":1783897842972,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5898e519001bpMNA6ODS2JxlM","reason":"tool-calls","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","messageID":"msg_f5898c6de001d15dRXL42stTuI","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","type":"step-finish","tokens":{"total":11862,"input":11426,"output":309,"reasoning":127,"cache":{"write":0,"read":0}},"cost":0}}
{"type":"step_start","timestamp":1783897843547,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5898e75a001F9z9ClUs45D3sv","messageID":"msg_f5898e54d0012rnV17tx4I2QNi","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","type":"step-start"}}
{"type":"text","timestamp":1783897851037,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5898f80e001JtawXfCGY6R5Tz","messageID":"msg_f5898e54d0012rnV17tx4I2QNi","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","type":"text","text":"The branch removes the implicit reviewer fallback and introduces a bounded STDIN protocol. I’m now checking the downstream orchestration failure mapping and package surface, because protocol rejection is only fail-closed if it cannot proceed toward integration, and “test-only” must hold in the distributable artifact rather than only in CLI imports.","time":{"start":1783897847822,"end":1783897851013},"metadata":{"azure":{"itemId":"msg_02116c1c406775f1016a541ef9df0081968af643e4cdf0a851","phase":"commentary"}}}}
{"type":"tool_use","timestamp":1783897851049,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_BRdjbWWqrcxXv9qbJ9MirXai","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/package.json"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/package.json</path>\n<type>file</type>\n<content>\n1: {\n2:   \"name\": \"zentra\",\n3:   \"version\": \"0.1.0\",\n4:   \"private\": true,\n5:   \"type\": \"module\",\n6:   \"bin\": {\n7:     \"zentra\": \"./dist/src/cli/main.js\"\n8:   },\n9:   \"scripts\": {\n10:     \"build\": \"tsc -p tsconfig.json\",\n11:     \"check\": \"tsc -p tsconfig.json --noEmit\",\n12:     \"test\": \"vitest run\",\n13:     \"test:watch\": \"vitest\",\n14:     \"start\": \"node dist/src/cli/main.js\"\n15:   },\n16:   \"engines\": {\n17:     \"node\": \">=24\"\n18:   },\n19:   \"dependencies\": {\n20:     \"better-sqlite3\": \"^12.0.0\",\n21:     \"commander\": \"^14.0.0\",\n22:     \"zod\": \"^4.0.0\"\n23:   },\n24:   \"devDependencies\": {\n25:     \"@types/better-sqlite3\": \"^7.6.0\",\n26:     \"@types/node\": \"^24.0.0\",\n27:     \"typescript\": \"^5.9.0\",\n28:     \"vitest\": \"^3.2.0\"\n29:   },\n30:   \"packageManager\": \"pnpm@10.0.0\",\n31:   \"pnpm\": {\n32:     \"onlyBuiltDependencies\": [\n33:       \"better-sqlite3\",\n34:       \"esbuild\"\n35:     ]\n36:   }\n37: }\n\n(End of file - total 37 lines)\n</content>","metadata":{"preview":"{\n  \"name\": \"zentra\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"bin\": {\n    \"zentra\": \"./dist/src/cli/main.js\"\n  },\n  \"scripts\": {\n    \"build\": \"tsc -p tsconfig.json\",\n    \"check\": \"tsc -p tsconfig.json --noEmit\",\n    \"test\": \"vitest run\",\n    \"test:watch\": \"vitest\",\n    \"start\": \"node dist/src/cli/main.js\"\n  },\n  \"engines\": {\n    \"node\": \">=24\"\n  },\n  \"dependencies\": {\n    \"better-sqlite3\": \"^12.0.0\",","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/package.json","text":"{\n  \"name\": \"zentra\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"bin\": {\n    \"zentra\": \"./dist/src/cli/main.js\"\n  },\n  \"scripts\": {\n    \"build\": \"tsc -p tsconfig.json\",\n    \"check\": \"tsc -p tsconfig.json --noEmit\",\n    \"test\": \"vitest run\",\n    \"test:watch\": \"vitest\",\n    \"start\": \"node dist/src/cli/main.js\"\n  },\n  \"engines\": {\n    \"node\": \">=24\"\n  },\n  \"dependencies\": {\n    \"better-sqlite3\": \"^12.0.0\",\n    \"commander\": \"^14.0.0\",\n    \"zod\": \"^4.0.0\"\n  },\n  \"devDependencies\": {\n    \"@types/better-sqlite3\": \"^7.6.0\",\n    \"@types/node\": \"^24.0.0\",\n    \"typescript\": \"^5.9.0\",\n    \"vitest\": \"^3.2.0\"\n  },\n  \"packageManager\": \"pnpm@10.0.0\",\n  \"pnpm\": {\n    \"onlyBuiltDependencies\": [\n      \"better-sqlite3\",\n      \"esbuild\"\n    ]\n  }\n}","lineStart":1,"lineEnd":37,"totalLines":37,"truncated":false}},"title":"package.json","time":{"start":1783897851026,"end":1783897851040}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdd908196ab2b66ed4a077a55"}},"id":"prt_f589904900016GQGUa9BVsxycl","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851049,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_HWj26pHk6l74mlcFonCMutLH","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/fixtures/bundled-fixtures.ts"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/fixtures/bundled-fixtures.ts</path>\n<type>file</type>\n<content>\n1: import { createHash } from \"node:crypto\";\n2: import { lstatSync, readFileSync, realpathSync } from \"node:fs\";\n3: import path from \"node:path\";\n4: import { fileURLToPath } from \"node:url\";\n5: \n6: export const BUNDLED_FIXTURE_SHA256 = Object.freeze({\n7:   \"deterministic-worker.mjs\": \"9839f5c1ae46c984bd0a3180b4dcaa9967bf81b73610b024d0a38538f973ce22\",\n8:   \"deterministic-reviewer.mjs\": \"bf28776ad1e54b7ce7117ae91bec10f19a9c96d551e5e5bfbc3ec7bd950f4745\",\n9: });\n10: \n11: export type BundledFixtureName = keyof typeof BUNDLED_FIXTURE_SHA256;\n12: export type BundledFixturePaths = Readonly<Record<BundledFixtureName, string>>;\n13: \n14: const SOURCE_TAIL = [\"src\", \"fixtures\", \"bundled-fixtures.ts\"] as const;\n15: const BUILT_TAIL = [\"dist\", \"src\", \"fixtures\", \"bundled-fixtures.js\"] as const;\n16: \n17: export function resolveBundledFixture(\n18:   name: BundledFixtureName,\n19:   anchor: string | URL = import.meta.url,\n20: ): string {\n21:   if (!Object.hasOwn(BUNDLED_FIXTURE_SHA256, name)) {\n22:     throw new Error(`unknown bundled fixture name: ${String(name)}`);\n23:   }\n24: \n25:   const anchorPath = fileURLToPath(anchor);\n26:   const components = anchorPath.split(path.sep).filter(Boolean);\n27:   const sourceLayout = hasExactTail(components, SOURCE_TAIL);\n28:   const builtLayout = hasExactTail(components, BUILT_TAIL);\n29:   if (!sourceLayout && !builtLayout) {\n30:     throw new Error(\"bundled fixture resolver anchor is outside the exact source or built layout\");\n31:   }\n32: \n33:   const expected = path.resolve(\n34:     path.dirname(anchorPath),\n35:     builtLayout ? \"../../../fixtures\" : \"../../fixtures\",\n36:     name,\n37:   );\n38:   let stat;\n39:   try {\n40:     stat = lstatSync(expected);\n41:   } catch {\n42:     throw new Error(`bundled fixture is unavailable or not a regular file: ${name}`);\n43:   }\n44:   if (stat.isSymbolicLink() || !stat.isFile()) {\n45:     throw new Error(`bundled fixture must be a regular non-symlink file: ${name}`);\n46:   }\n47: \n48:   const canonical = realpathSync(expected);\n49:   if (canonical !== expected) {\n50:     throw new Error(`bundled fixture canonical path does not equal its expected path: ${name}`);\n51:   }\n52:   const digest = createHash(\"sha256\").update(readFileSync(canonical)).digest(\"hex\");\n53:   if (digest !== BUNDLED_FIXTURE_SHA256[name]) {\n54:     throw new Error(`bundled fixture SHA-256 attestation failed: ${name}`);\n55:   }\n56:   return canonical;\n57: }\n58: \n59: export function resolveBundledFixtures(\n60:   anchor: string | URL = import.meta.url,\n61: ): BundledFixturePaths {\n62:   return Object.freeze({\n63:     \"deterministic-worker.mjs\": resolveBundledFixture(\"deterministic-worker.mjs\", anchor),\n64:     \"deterministic-reviewer.mjs\": resolveBundledFixture(\"deterministic-reviewer.mjs\", anchor),\n65:   });\n66: }\n67: \n68: function hasExactTail(\n69:   components: readonly string[],\n70:   tail: readonly string[],\n71: ): boolean {\n72:   return components.length >= tail.length &&\n73:     tail.every((component, index) => component === components[components.length - tail.length + index]);\n74: }\n\n(End of file - total 74 lines)\n</content>","metadata":{"preview":"import { createHash } from \"node:crypto\";\nimport { lstatSync, readFileSync, realpathSync } from \"node:fs\";\nimport path from \"node:path\";\nimport { fileURLToPath } from \"node:url\";\n\nexport const BUNDLED_FIXTURE_SHA256 = Object.freeze({\n  \"deterministic-worker.mjs\": \"9839f5c1ae46c984bd0a3180b4dcaa9967bf81b73610b024d0a38538f973ce22\",\n  \"deterministic-reviewer.mjs\": \"bf28776ad1e54b7ce7117ae91bec10f19a9c96d551e5e5bfbc3ec7bd950f4745\",\n});\n\nexport type BundledFixtureName = keyof typeof BUNDLED_FIXTURE_SHA256;\nexport type BundledFixturePaths = Readonly<Record<BundledFixtureName, string>>;\n\nconst SOURCE_TAIL = [\"src\", \"fixtures\", \"bundled-fixtures.ts\"] as const;\nconst BUILT_TAIL = [\"dist\", \"src\", \"fixtures\", \"bundled-fixtures.js\"] as const;\n\nexport function resolveBundledFixture(\n  name: BundledFixtureName,\n  anchor: string | URL = import.meta.url,\n): string {","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/fixtures/bundled-fixtures.ts","text":"import { createHash } from \"node:crypto\";\nimport { lstatSync, readFileSync, realpathSync } from \"node:fs\";\nimport path from \"node:path\";\nimport { fileURLToPath } from \"node:url\";\n\nexport const BUNDLED_FIXTURE_SHA256 = Object.freeze({\n  \"deterministic-worker.mjs\": \"9839f5c1ae46c984bd0a3180b4dcaa9967bf81b73610b024d0a38538f973ce22\",\n  \"deterministic-reviewer.mjs\": \"bf28776ad1e54b7ce7117ae91bec10f19a9c96d551e5e5bfbc3ec7bd950f4745\",\n});\n\nexport type BundledFixtureName = keyof typeof BUNDLED_FIXTURE_SHA256;\nexport type BundledFixturePaths = Readonly<Record<BundledFixtureName, string>>;\n\nconst SOURCE_TAIL = [\"src\", \"fixtures\", \"bundled-fixtures.ts\"] as const;\nconst BUILT_TAIL = [\"dist\", \"src\", \"fixtures\", \"bundled-fixtures.js\"] as const;\n\nexport function resolveBundledFixture(\n  name: BundledFixtureName,\n  anchor: string | URL = import.meta.url,\n): string {\n  if (!Object.hasOwn(BUNDLED_FIXTURE_SHA256, name)) {\n    throw new Error(`unknown bundled fixture name: ${String(name)}`);\n  }\n\n  const anchorPath = fileURLToPath(anchor);\n  const components = anchorPath.split(path.sep).filter(Boolean);\n  const sourceLayout = hasExactTail(components, SOURCE_TAIL);\n  const builtLayout = hasExactTail(components, BUILT_TAIL);\n  if (!sourceLayout && !builtLayout) {\n    throw new Error(\"bundled fixture resolver anchor is outside the exact source or built layout\");\n  }\n\n  const expected = path.resolve(\n    path.dirname(anchorPath),\n    builtLayout ? \"../../../fixtures\" : \"../../fixtures\",\n    name,\n  );\n  let stat;\n  try {\n    stat = lstatSync(expected);\n  } catch {\n    throw new Error(`bundled fixture is unavailable or not a regular file: ${name}`);\n  }\n  if (stat.isSymbolicLink() || !stat.isFile()) {\n    throw new Error(`bundled fixture must be a regular non-symlink file: ${name}`);\n  }\n\n  const canonical = realpathSync(expected);\n  if (canonical !== expected) {\n    throw new Error(`bundled fixture canonical path does not equal its expected path: ${name}`);\n  }\n  const digest = createHash(\"sha256\").update(readFileSync(canonical)).digest(\"hex\");\n  if (digest !== BUNDLED_FIXTURE_SHA256[name]) {\n    throw new Error(`bundled fixture SHA-256 attestation failed: ${name}`);\n  }\n  return canonical;\n}\n\nexport function resolveBundledFixtures(\n  anchor: string | URL = import.meta.url,\n): BundledFixturePaths {\n  return Object.freeze({\n    \"deterministic-worker.mjs\": resolveBundledFixture(\"deterministic-worker.mjs\", anchor),\n    \"deterministic-reviewer.mjs\": resolveBundledFixture(\"deterministic-reviewer.mjs\", anchor),\n  });\n}\n\nfunction hasExactTail(\n  components: readonly string[],\n  tail: readonly string[],\n): boolean {\n  return components.length >= tail.length &&\n    tail.every((component, index) => component === components[components.length - tail.length + index]);\n}","lineStart":1,"lineEnd":74,"totalLines":74,"truncated":false}},"title":"src/fixtures/bundled-fixtures.ts","time":{"start":1783897851028,"end":1783897851044}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdda08196a77852a2966cc89a"}},"id":"prt_f58990492001UgTzlmN1dk9hwm","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851049,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_LC47JjJXWMyEVskCyXhk2EaP","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/review-gate.ts"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/review-gate.ts</path>\n<type>file</type>\n<content>\n1: import { createHash } from \"node:crypto\";\n2: import type { ReviewInput, ReviewDecision } from \"./reviewer-adapter.js\";\n3: import {\n4:   canonicalValidationDigest,\n5:   ReviewDecisionSchema,\n6: } from \"./reviewer-adapter.js\";\n7: import {\n8:   isVerifiedValidationReport,\n9:   isVerifiedValidationSubject,\n10: } from \"../capabilities/validation-runner.js\";\n11: \n12: const verifiedDecisions = new WeakSet<ReviewDecision>();\n13: \n14: export function isVerifiedReviewDecision(decision: ReviewDecision): boolean {\n15:   return verifiedDecisions.has(decision);\n16: }\n17: \n18: export class ReviewGate {\n19:   verifyEvidence(input: ReviewInput, decision: ReviewDecision): ReviewDecision {\n20:     if (!isVerifiedValidationReport(input.validation)) {\n21:       throw new Error(\"review gate: validation report lacks provenance\");\n22:     }\n23:     const captured = {\n24:       reviewerId: decision.reviewerId,\n25:       approved: decision.approved,\n26:       diffSha256: decision.diffSha256,\n27:       validationSha256: decision.validationSha256,\n28:       decidedAt: decision.decidedAt,\n29:       reason: decision.reason,\n30:     };\n31:     const expectedKeys = Object.keys(captured).sort();\n32:     const actualKeys = Object.keys(decision).sort();\n33:     if (JSON.stringify(actualKeys) !== JSON.stringify(expectedKeys)) {\n34:       throw new Error(\"review gate: invalid decision fields\");\n35:     }\n36:     const parsed = ReviewDecisionSchema.safeParse(captured);\n37:     if (!parsed.success) {\n38:       throw new Error(`review gate: invalid decision: ${parsed.error.message}`);\n39:     }\n40:     const snapshot: ReviewDecision = parsed.data;\n41:     // Reject nonempty diff requirement\n42:     if (!input.diff || input.diff.trim() === \"\") {\n43:       throw new Error(\"review gate: diff is empty\");\n44:     }\n45:     const inputDiffSha256 = createHash(\"sha256\").update(input.diff, \"utf8\").digest(\"hex\");\n46:     if (!isVerifiedValidationSubject(input.validation, inputDiffSha256)) {\n47:       throw new Error(\"review gate: validation subject does not match input diff\");\n48:     }\n49: \n50:     // Reject if validation outcome is not completed\n51:     if (input.validation.outcome !== \"completed\") {\n52:       throw new Error(\n53:         `review gate: validation outcome must be 'completed', got '${input.validation.outcome}'`\n54:       );\n55:     }\n56: \n57:     // Reject if validation exit code is not 0\n58:     if (input.validation.exitCode !== 0) {\n59:       throw new Error(\n60:         `review gate: validation exit code must be 0, got ${input.validation.exitCode}`\n61:       );\n62:     }\n63: \n64:     if (input.validation.name !== \"focused\") {\n65:       throw new Error(\n66:         `review gate: validation name must be 'focused', got '${input.validation.name}'`,\n67:       );\n68:     }\n69: \n70:     // Reject if reviewer identity matches worker identity\n71:     if (input.workerId === input.reviewerId) {\n72:       throw new Error(\n73:         \"review gate: reviewer identity must differ from worker identity\"\n74:       );\n75:     }\n76: \n77:     if (snapshot.reviewerId !== input.reviewerId) {\n78:       throw new Error(\"review gate: decision reviewer identity does not match requested reviewer\");\n79:     }\n80: \n81:     // Compute current digests\n82:     const currentDiffSha256 = inputDiffSha256;\n83: \n84:     const currentValidationSha256 = canonicalValidationDigest(input.validation);\n85: \n86:     // Reject if diff digest does not match\n87:     if (snapshot.diffSha256 !== currentDiffSha256) {\n88:       throw new Error(\n89:         `review gate: diff digest mismatch - decision was ${snapshot.diffSha256}, current is ${currentDiffSha256}`\n90:       );\n91:     }\n92: \n93:     // Reject if validation digest does not match\n94:     if (snapshot.validationSha256 !== currentValidationSha256) {\n95:       throw new Error(\n96:         `review gate: validation digest mismatch - decision was ${snapshot.validationSha256}, current is ${currentValidationSha256}`\n97:       );\n98:     }\n99: \n100:     Object.freeze(snapshot);\n101:     return snapshot;\n102:   }\n103: \n104:   verify(input: ReviewInput, decision: ReviewDecision): ReviewDecision {\n105:     const snapshot = this.verifyEvidence(input, decision);\n106:     if (!snapshot.approved) {\n107:       throw new Error(`review gate: decision was not approved: ${snapshot.reason}`);\n108:     }\n109:     verifiedDecisions.add(snapshot);\n110:     return snapshot;\n111:   }\n112: }\n\n(End of file - total 112 lines)\n</content>","metadata":{"preview":"import { createHash } from \"node:crypto\";\nimport type { ReviewInput, ReviewDecision } from \"./reviewer-adapter.js\";\nimport {\n  canonicalValidationDigest,\n  ReviewDecisionSchema,\n} from \"./reviewer-adapter.js\";\nimport {\n  isVerifiedValidationReport,\n  isVerifiedValidationSubject,\n} from \"../capabilities/validation-runner.js\";\n\nconst verifiedDecisions = new WeakSet<ReviewDecision>();\n\nexport function isVerifiedReviewDecision(decision: ReviewDecision): boolean {\n  return verifiedDecisions.has(decision);\n}\n\nexport class ReviewGate {\n  verifyEvidence(input: ReviewInput, decision: ReviewDecision): ReviewDecision {\n    if (!isVerifiedValidationReport(input.validation)) {","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/review-gate.ts","text":"import { createHash } from \"node:crypto\";\nimport type { ReviewInput, ReviewDecision } from \"./reviewer-adapter.js\";\nimport {\n  canonicalValidationDigest,\n  ReviewDecisionSchema,\n} from \"./reviewer-adapter.js\";\nimport {\n  isVerifiedValidationReport,\n  isVerifiedValidationSubject,\n} from \"../capabilities/validation-runner.js\";\n\nconst verifiedDecisions = new WeakSet<ReviewDecision>();\n\nexport function isVerifiedReviewDecision(decision: ReviewDecision): boolean {\n  return verifiedDecisions.has(decision);\n}\n\nexport class ReviewGate {\n  verifyEvidence(input: ReviewInput, decision: ReviewDecision): ReviewDecision {\n    if (!isVerifiedValidationReport(input.validation)) {\n      throw new Error(\"review gate: validation report lacks provenance\");\n    }\n    const captured = {\n      reviewerId: decision.reviewerId,\n      approved: decision.approved,\n      diffSha256: decision.diffSha256,\n      validationSha256: decision.validationSha256,\n      decidedAt: decision.decidedAt,\n      reason: decision.reason,\n    };\n    const expectedKeys = Object.keys(captured).sort();\n    const actualKeys = Object.keys(decision).sort();\n    if (JSON.stringify(actualKeys) !== JSON.stringify(expectedKeys)) {\n      throw new Error(\"review gate: invalid decision fields\");\n    }\n    const parsed = ReviewDecisionSchema.safeParse(captured);\n    if (!parsed.success) {\n      throw new Error(`review gate: invalid decision: ${parsed.error.message}`);\n    }\n    const snapshot: ReviewDecision = parsed.data;\n    // Reject nonempty diff requirement\n    if (!input.diff || input.diff.trim() === \"\") {\n      throw new Error(\"review gate: diff is empty\");\n    }\n    const inputDiffSha256 = createHash(\"sha256\").update(input.diff, \"utf8\").digest(\"hex\");\n    if (!isVerifiedValidationSubject(input.validation, inputDiffSha256)) {\n      throw new Error(\"review gate: validation subject does not match input diff\");\n    }\n\n    // Reject if validation outcome is not completed\n    if (input.validation.outcome !== \"completed\") {\n      throw new Error(\n        `review gate: validation outcome must be 'completed', got '${input.validation.outcome}'`\n      );\n    }\n\n    // Reject if validation exit code is not 0\n    if (input.validation.exitCode !== 0) {\n      throw new Error(\n        `review gate: validation exit code must be 0, got ${input.validation.exitCode}`\n      );\n    }\n\n    if (input.validation.name !== \"focused\") {\n      throw new Error(\n        `review gate: validation name must be 'focused', got '${input.validation.name}'`,\n      );\n    }\n\n    // Reject if reviewer identity matches worker identity\n    if (input.workerId === input.reviewerId) {\n      throw new Error(\n        \"review gate: reviewer identity must differ from worker identity\"\n      );\n    }\n\n    if (snapshot.reviewerId !== input.reviewerId) {\n      throw new Error(\"review gate: decision reviewer identity does not match requested reviewer\");\n    }\n\n    // Compute current digests\n    const currentDiffSha256 = inputDiffSha256;\n\n    const currentValidationSha256 = canonicalValidationDigest(input.validation);\n\n    // Reject if diff digest does not match\n    if (snapshot.diffSha256 !== currentDiffSha256) {\n      throw new Error(\n        `review gate: diff digest mismatch - decision was ${snapshot.diffSha256}, current is ${currentDiffSha256}`\n      );\n    }\n\n    // Reject if validation digest does not match\n    if (snapshot.validationSha256 !== currentValidationSha256) {\n      throw new Error(\n        `review gate: validation digest mismatch - decision was ${snapshot.validationSha256}, current is ${currentValidationSha256}`\n      );\n    }\n\n    Object.freeze(snapshot);\n    return snapshot;\n  }\n\n  verify(input: ReviewInput, decision: ReviewDecision): ReviewDecision {\n    const snapshot = this.verifyEvidence(input, decision);\n    if (!snapshot.approved) {\n      throw new Error(`review gate: decision was not approved: ${snapshot.reason}`);\n    }\n    verifiedDecisions.add(snapshot);\n    return snapshot;\n  }\n}","lineStart":1,"lineEnd":112,"totalLines":112,"truncated":false}},"title":"src/reviews/review-gate.ts","time":{"start":1783897851024,"end":1783897851045}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdd88819691149919c49bce06"}},"id":"prt_f5899048f001NP75TN6e0utAdx","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851049,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_2N8OuXiu9q9p77nQ71Q3VzWa","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/reviews/deterministic-reviewer-adapter.test.ts","offset":300,"limit":150},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/reviews/deterministic-reviewer-adapter.test.ts</path>\n<type>file</type>\n<content>\n300:           name: \"ReviewerExecutionError\",\n301:           outcome,\n302:         }),\n303:       );\n304:     },\n305:   );\n306: });\n307: \n308: describe(\"ProcessReviewerAdapter\", () => {\n309:   const validationReport: ValidationReport = {\n310:     name: \"focused\",\n311:     outcome: \"completed\",\n312:     exitCode: 0,\n313:     stdout: \"focused validation passed\",\n314:     stderr: \"\",\n315:     startedAt: \"2026-01-01T00:00:00.000Z\",\n316:     finishedAt: \"2026-01-01T00:00:01.000Z\",\n317:     command: [\"node\", \"--test\"],\n318:     argvSha256: createHash(\"sha256\").update(JSON.stringify([\"node\", \"--test\"])).digest(\"hex\"),\n319:     outputSha256: createHash(\"sha256\")\n320:       .update(JSON.stringify({ stdout: \"focused validation passed\", stderr: \"\" }))\n321:       .digest(\"hex\"),\n322:     provenance: {\n323:       invocationId: \"content-review-validation\",\n324:       canonicalCwd: \"/tmp\",\n325:       subjectSha256: \"content-review-subject\",\n326:     },\n327:   };\n328:   const input = {\n329:     workerId: \"worker-1\",\n330:     reviewerId: \"reviewer-1\",\n331:     diff: \"diff --git a/file b/file\\n-old\\n+new\\n\",\n332:     validation: validationReport,\n333:   } as const;\n334: \n335:   it(\"sends the exact diff and validation evidence through stdin rather than argv\", async () => {\n336:     const decision = await processReviewer(getContentAwareFixturePath()).review(\n337:       input,\n338:       AbortSignal.timeout(5_000),\n339:     );\n340: \n341:     expect(decision.approved).toBe(true);\n342:     expect(decision.reason).toContain(validationReport.stdout);\n343:   });\n344: \n345:   it(\"rejects reviewer input that exceeds its byte bound before spawning\", async () => {\n346:     const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n347:     const adapter = processReviewer(fixture, { maxInputBytes: 128 });\n348: \n349:     await expect(adapter.review(\n350:       { ...input, diff: \"x\".repeat(129) },\n351:       AbortSignal.timeout(5_000),\n352:     )).rejects.toThrow(/input.*limit|evidence.*large|bounded/i);\n353:   });\n354: \n355:   it(\"fails closed when the reviewer returns an evidence digest mismatch\", async () => {\n356:     const fixture = reviewerScript(`\n357:       let body = \"\";\n358:       process.stdin.setEncoding(\"utf8\");\n359:       for await (const chunk of process.stdin) body += chunk;\n360:       const request = JSON.parse(body);\n361:       console.log(JSON.stringify({\n362:         reviewerId: request.reviewerId,\n363:         decision: \"approve\",\n364:         diffSha256: \"0\".repeat(64),\n365:         validationSha256: request.validationSha256,\n366:         decidedAt: new Date().toISOString(),\n367:         reason: \"reviewed\"\n368:       }));\n369:     `);\n370: \n371:     await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n372:       .rejects.toThrow(/diff.*digest.*mismatch|evidence.*mismatch/i);\n373:   });\n374: \n375:   it(\"fails closed when the reviewer times out\", async () => {\n376:     const fixture = reviewerScript(\"setInterval(() => {}, 1_000);\");\n377: \n378:     await expect(processReviewer(fixture, { timeoutMs: 20 }).review(\n379:       input,\n380:       AbortSignal.timeout(5_000),\n381:     )).rejects.toEqual(expect.objectContaining({ outcome: \"timed_out\" }));\n382:   });\n383: \n384:   it(\"fails closed on malformed reviewer output\", async () => {\n385:     const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => console.log(\"not json\"));');\n386: \n387:     await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n388:       .rejects.toThrow(/invalid|protocol|json/i);\n389:   });\n390: \n391:   it(\"fails closed when reviewer output exceeds its byte bound\", async () => {\n392:     const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => process.stdout.write(\"x\".repeat(1_024)));');\n393: \n394:     await expect(processReviewer(fixture, { maxOutputBytes: 128 }).review(\n395:       input,\n396:       AbortSignal.timeout(5_000),\n397:     )).rejects.toThrow(/output.*limit|truncat/i);\n398:   });\n399: \n400:   it(\"requires exactly one reviewer decision\", async () => {\n401:     const fixture = reviewerScript(`\n402:       let body = \"\";\n403:       process.stdin.setEncoding(\"utf8\");\n404:       for await (const chunk of process.stdin) body += chunk;\n405:       const request = JSON.parse(body);\n406:       const decision = JSON.stringify({ reviewerId: request.reviewerId, decision: \"deny\", diffSha256: request.diffSha256, validationSha256: request.validationSha256, decidedAt: new Date().toISOString(), reason: \"denied\" });\n407:       console.log(decision);\n408:       console.log(decision);\n409:     `);\n410: \n411:     await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n412:       .rejects.toThrow(/exactly one|single.*decision|protocol/i);\n413:   });\n414: \n415:   it(\"rejects matching worker and reviewer identities before spawning\", async () => {\n416:     const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n417: \n418:     await expect(processReviewer(fixture).review(\n419:       { ...input, reviewerId: input.workerId },\n420:       AbortSignal.timeout(5_000),\n421:     )).rejects.toThrow(/identity|differ|same/i);\n422:   });\n423: });\n\n(End of file - total 423 lines)\n</content>","metadata":{"preview":"          name: \"ReviewerExecutionError\",\n          outcome,\n        }),\n      );\n    },\n  );\n});\n\ndescribe(\"ProcessReviewerAdapter\", () => {\n  const validationReport: ValidationReport = {\n    name: \"focused\",\n    outcome: \"completed\",\n    exitCode: 0,\n    stdout: \"focused validation passed\",\n    stderr: \"\",\n    startedAt: \"2026-01-01T00:00:00.000Z\",\n    finishedAt: \"2026-01-01T00:00:01.000Z\",\n    command: [\"node\", \"--test\"],\n    argvSha256: createHash(\"sha256\").update(JSON.stringify([\"node\", \"--test\"])).digest(\"hex\"),\n    outputSha256: createHash(\"sha256\")","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/reviews/deterministic-reviewer-adapter.test.ts","text":"          name: \"ReviewerExecutionError\",\n          outcome,\n        }),\n      );\n    },\n  );\n});\n\ndescribe(\"ProcessReviewerAdapter\", () => {\n  const validationReport: ValidationReport = {\n    name: \"focused\",\n    outcome: \"completed\",\n    exitCode: 0,\n    stdout: \"focused validation passed\",\n    stderr: \"\",\n    startedAt: \"2026-01-01T00:00:00.000Z\",\n    finishedAt: \"2026-01-01T00:00:01.000Z\",\n    command: [\"node\", \"--test\"],\n    argvSha256: createHash(\"sha256\").update(JSON.stringify([\"node\", \"--test\"])).digest(\"hex\"),\n    outputSha256: createHash(\"sha256\")\n      .update(JSON.stringify({ stdout: \"focused validation passed\", stderr: \"\" }))\n      .digest(\"hex\"),\n    provenance: {\n      invocationId: \"content-review-validation\",\n      canonicalCwd: \"/tmp\",\n      subjectSha256: \"content-review-subject\",\n    },\n  };\n  const input = {\n    workerId: \"worker-1\",\n    reviewerId: \"reviewer-1\",\n    diff: \"diff --git a/file b/file\\n-old\\n+new\\n\",\n    validation: validationReport,\n  } as const;\n\n  it(\"sends the exact diff and validation evidence through stdin rather than argv\", async () => {\n    const decision = await processReviewer(getContentAwareFixturePath()).review(\n      input,\n      AbortSignal.timeout(5_000),\n    );\n\n    expect(decision.approved).toBe(true);\n    expect(decision.reason).toContain(validationReport.stdout);\n  });\n\n  it(\"rejects reviewer input that exceeds its byte bound before spawning\", async () => {\n    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n    const adapter = processReviewer(fixture, { maxInputBytes: 128 });\n\n    await expect(adapter.review(\n      { ...input, diff: \"x\".repeat(129) },\n      AbortSignal.timeout(5_000),\n    )).rejects.toThrow(/input.*limit|evidence.*large|bounded/i);\n  });\n\n  it(\"fails closed when the reviewer returns an evidence digest mismatch\", async () => {\n    const fixture = reviewerScript(`\n      let body = \"\";\n      process.stdin.setEncoding(\"utf8\");\n      for await (const chunk of process.stdin) body += chunk;\n      const request = JSON.parse(body);\n      console.log(JSON.stringify({\n        reviewerId: request.reviewerId,\n        decision: \"approve\",\n        diffSha256: \"0\".repeat(64),\n        validationSha256: request.validationSha256,\n        decidedAt: new Date().toISOString(),\n        reason: \"reviewed\"\n      }));\n    `);\n\n    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n      .rejects.toThrow(/diff.*digest.*mismatch|evidence.*mismatch/i);\n  });\n\n  it(\"fails closed when the reviewer times out\", async () => {\n    const fixture = reviewerScript(\"setInterval(() => {}, 1_000);\");\n\n    await expect(processReviewer(fixture, { timeoutMs: 20 }).review(\n      input,\n      AbortSignal.timeout(5_000),\n    )).rejects.toEqual(expect.objectContaining({ outcome: \"timed_out\" }));\n  });\n\n  it(\"fails closed on malformed reviewer output\", async () => {\n    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => console.log(\"not json\"));');\n\n    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n      .rejects.toThrow(/invalid|protocol|json/i);\n  });\n\n  it(\"fails closed when reviewer output exceeds its byte bound\", async () => {\n    const fixture = reviewerScript('process.stdin.resume(); process.stdin.on(\"end\", () => process.stdout.write(\"x\".repeat(1_024)));');\n\n    await expect(processReviewer(fixture, { maxOutputBytes: 128 }).review(\n      input,\n      AbortSignal.timeout(5_000),\n    )).rejects.toThrow(/output.*limit|truncat/i);\n  });\n\n  it(\"requires exactly one reviewer decision\", async () => {\n    const fixture = reviewerScript(`\n      let body = \"\";\n      process.stdin.setEncoding(\"utf8\");\n      for await (const chunk of process.stdin) body += chunk;\n      const request = JSON.parse(body);\n      const decision = JSON.stringify({ reviewerId: request.reviewerId, decision: \"deny\", diffSha256: request.diffSha256, validationSha256: request.validationSha256, decidedAt: new Date().toISOString(), reason: \"denied\" });\n      console.log(decision);\n      console.log(decision);\n    `);\n\n    await expect(processReviewer(fixture).review(input, AbortSignal.timeout(5_000)))\n      .rejects.toThrow(/exactly one|single.*decision|protocol/i);\n  });\n\n  it(\"rejects matching worker and reviewer identities before spawning\", async () => {\n    const fixture = reviewerScript('process.stdout.write(\"should not run\\\\n\");');\n\n    await expect(processReviewer(fixture).review(\n      { ...input, reviewerId: input.workerId },\n      AbortSignal.timeout(5_000),\n    )).rejects.toThrow(/identity|differ|same/i);\n  });\n});","lineStart":300,"lineEnd":423,"totalLines":423,"truncated":false}},"title":"tests/reviews/deterministic-reviewer-adapter.test.ts","time":{"start":1783897851030,"end":1783897851046}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdda88196b21e6c7db3ffa2a1"}},"id":"prt_f58990495001kZvH4K6XLZwkJ7","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851050,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_IFW2bLELB8blvzqA8gQqoUuC","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/reviewer-adapter.ts"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/reviewer-adapter.ts</path>\n<type>file</type>\n<content>\n1: import { createHash } from \"node:crypto\";\n2: import { spawn } from \"node:child_process\";\n3: import { z } from \"zod\";\n4: import type { ProcessSupervisor } from \"../workers/process-supervisor.js\";\n5: import type { ValidationReport } from \"../capabilities/validation-runner.js\";\n6: \n7: export interface ReviewInput {\n8:   readonly workerId: string;\n9:   readonly reviewerId: string;\n10:   readonly diff: string;\n11:   readonly validation: ValidationReport;\n12: }\n13: \n14: export interface ReviewDecision {\n15:   readonly reviewerId: string;\n16:   readonly approved: boolean;\n17:   readonly diffSha256: string;\n18:   readonly validationSha256: string;\n19:   readonly decidedAt: string;\n20:   readonly reason: string;\n21: }\n22: \n23: export interface ReviewerAdapter {\n24:   review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision>;\n25: }\n26: \n27: export class ReviewerExecutionError extends Error {\n28:   override readonly name = \"ReviewerExecutionError\";\n29: \n30:   constructor(\n31:     readonly outcome: \"cancelled\" | \"timed_out\" | \"failed\",\n32:     stderr: string,\n33:   ) {\n34:     super(\n35:       `reviewer process failed: outcome=${outcome}${\n36:         stderr === \"\" ? \"\" : `, stderr=${stderr}`\n37:       }`,\n38:     );\n39:   }\n40: }\n41: \n42: export const ReviewDecisionSchema = z.strictObject({\n43:   reviewerId: z.string().min(1),\n44:   approved: z.boolean(),\n45:   diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n46:   validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n47:   decidedAt: z.string().datetime({ offset: true }),\n48:   reason: z.string().min(1),\n49: });\n50: \n51: const ProcessReviewDecisionSchema = z.strictObject({\n52:   reviewerId: z.string().min(1),\n53:   decision: z.enum([\"approve\", \"deny\"]),\n54:   diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n55:   validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n56:   decidedAt: z.string().datetime({ offset: true }),\n57:   reason: z.string().min(1).max(4_096),\n58: });\n59: \n60: const REVIEWER_ENV_ALLOWLIST = [\"PATH\", \"HOME\", \"TMPDIR\", \"LANG\", \"LC_ALL\"] as const;\n61: const DEFAULT_REVIEWER_TIMEOUT_MS = 30_000;\n62: const DEFAULT_MAX_REVIEW_INPUT_BYTES = 2 * 1_024 * 1_024;\n63: const DEFAULT_MAX_REVIEW_OUTPUT_BYTES = 16 * 1_024;\n64: \n65: export interface ProcessReviewerOptions {\n66:   readonly executable: string;\n67:   readonly args?: readonly string[];\n68:   readonly cwd?: string;\n69:   readonly timeoutMs?: number;\n70:   readonly maxInputBytes?: number;\n71:   readonly maxOutputBytes?: number;\n72: }\n73: \n74: function sha256(value: string): string {\n75:   return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n76: }\n77: \n78: export function canonicalValidationDigest(validation: ValidationReport): string {\n79:   const argvSha256 = sha256(JSON.stringify(validation.command));\n80:   const outputSha256 = sha256(\n81:     JSON.stringify({ stdout: validation.stdout, stderr: validation.stderr }),\n82:   );\n83:   if (validation.argvSha256 !== argvSha256) {\n84:     throw new Error(\"validation argvSha256 is inconsistent with command evidence\");\n85:   }\n86:   if (validation.outputSha256 !== outputSha256) {\n87:     throw new Error(\"validation outputSha256 is inconsistent with output evidence\");\n88:   }\n89:   const canonical = JSON.stringify({\n90:     name: validation.name,\n91:     outcome: validation.outcome,\n92:     exitCode: validation.exitCode,\n93:     startedAt: validation.startedAt,\n94:     finishedAt: validation.finishedAt,\n95:     command: validation.command,\n96:     stdout: validation.stdout,\n97:     stderr: validation.stderr,\n98:     argvSha256,\n99:     outputSha256,\n100:     provenance: validation.provenance,\n101:   });\n102:   return sha256(canonical);\n103: }\n104: \n105: export class ProcessReviewerAdapter implements ReviewerAdapter {\n106:   private readonly args: readonly string[];\n107:   private readonly cwd: string;\n108:   private readonly timeoutMs: number;\n109:   private readonly maxInputBytes: number;\n110:   private readonly maxOutputBytes: number;\n111: \n112:   constructor(private readonly options: ProcessReviewerOptions) {\n113:     this.args = options.args ?? [];\n114:     this.cwd = options.cwd ?? \"/tmp\";\n115:     this.timeoutMs = options.timeoutMs ?? DEFAULT_REVIEWER_TIMEOUT_MS;\n116:     this.maxInputBytes = options.maxInputBytes ?? DEFAULT_MAX_REVIEW_INPUT_BYTES;\n117:     this.maxOutputBytes = options.maxOutputBytes ?? DEFAULT_MAX_REVIEW_OUTPUT_BYTES;\n118:     for (const [name, value] of [\n119:       [\"timeoutMs\", this.timeoutMs],\n120:       [\"maxInputBytes\", this.maxInputBytes],\n121:       [\"maxOutputBytes\", this.maxOutputBytes],\n122:     ] as const) {\n123:       if (!Number.isSafeInteger(value) || value <= 0) {\n124:         throw new Error(`${name} must be a positive safe integer`);\n125:       }\n126:     }\n127:   }\n128: \n129:   async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n130:     if (input.workerId === input.reviewerId) {\n131:       throw new Error(\n132:         `reviewer identity must differ from worker identity: ${input.workerId}`,\n133:       );\n134:     }\n135: \n136:     const diffSha256 = sha256(input.diff);\n137:     const validationSha256 = canonicalValidationDigest(input.validation);\n138:     const request = JSON.stringify({\n139:       schemaVersion: 1,\n140:       workerId: input.workerId,\n141:       reviewerId: input.reviewerId,\n142:       diff: input.diff,\n143:       validation: input.validation,\n144:       diffSha256,\n145:       validationSha256,\n146:     });\n147:     const requestBytes = Buffer.byteLength(request, \"utf8\");\n148:     if (requestBytes > this.maxInputBytes) {\n149:       throw new Error(\n150:         `reviewer protocol input exceeded bounded evidence limit of ${this.maxInputBytes} bytes`,\n151:       );\n152:     }\n153: \n154:     const output = await this.execute(request, signal);\n155:     const lines = output.trim().split(\"\\n\").filter((line) => line.trim() !== \"\");\n156:     if (lines.length !== 1) {\n157:       throw new Error(\n158:         `reviewer protocol requires exactly one decision, received ${lines.length}`,\n159:       );\n160:     }\n161: \n162:     let decoded: unknown;\n163:     try {\n164:       decoded = JSON.parse(lines[0]!);\n165:     } catch {\n166:       throw new Error(\"reviewer protocol returned invalid JSON\");\n167:     }\n168:     const parsed = ProcessReviewDecisionSchema.safeParse(decoded);\n169:     if (!parsed.success) {\n170:       throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n171:     }\n172:     if (parsed.data.reviewerId !== input.reviewerId) {\n173:       throw new Error(\"reviewer protocol returned a reviewer identity mismatch\");\n174:     }\n175:     if (parsed.data.diffSha256 !== diffSha256) {\n176:       throw new Error(\"reviewer protocol returned a diff evidence digest mismatch\");\n177:     }\n178:     if (parsed.data.validationSha256 !== validationSha256) {\n179:       throw new Error(\"reviewer protocol returned a validation evidence digest mismatch\");\n180:     }\n181: \n182:     return ReviewDecisionSchema.parse({\n183:       reviewerId: parsed.data.reviewerId,\n184:       approved: parsed.data.decision === \"approve\",\n185:       diffSha256: parsed.data.diffSha256,\n186:       validationSha256: parsed.data.validationSha256,\n187:       decidedAt: parsed.data.decidedAt,\n188:       reason: parsed.data.reason,\n189:     });\n190:   }\n191: \n192:   private execute(request: string, signal: AbortSignal): Promise<string> {\n193:     if (signal.aborted) {\n194:       return Promise.reject(new ReviewerExecutionError(\"cancelled\", \"\"));\n195:     }\n196: \n197:     return new Promise((resolve, reject) => {\n198:       const env: Record<string, string> = {};\n199:       for (const key of REVIEWER_ENV_ALLOWLIST) {\n200:         const value = process.env[key];\n201:         if (value !== undefined) env[key] = value;\n202:       }\n203:       const child = spawn(this.options.executable, [...this.args], {\n204:         cwd: this.cwd,\n205:         shell: false,\n206:         detached: true,\n207:         env,\n208:         stdio: [\"pipe\", \"pipe\", \"pipe\"],\n209:       });\n210:       const stdout: Buffer[] = [];\n211:       const stderr: Buffer[] = [];\n212:       let outputBytes = 0;\n213:       let outcome: \"cancelled\" | \"timed_out\" | \"failed\" | undefined;\n214:       let reason = \"\";\n215:       let settled = false;\n216: \n217:       const kill = (): void => {\n218:         if (child.pid === undefined) return;\n219:         try {\n220:           process.kill(-child.pid, \"SIGKILL\");\n221:         } catch {\n222:           child.kill(\"SIGKILL\");\n223:         }\n224:       };\n225:       const stop = (\n226:         nextOutcome: \"cancelled\" | \"timed_out\" | \"failed\",\n227:         nextReason: string,\n228:       ): void => {\n229:         if (outcome !== undefined) return;\n230:         outcome = nextOutcome;\n231:         reason = nextReason;\n232:         kill();\n233:       };\n234:       const timer = setTimeout(\n235:         () => stop(\"timed_out\", \"reviewer timeout exceeded\"),\n236:         this.timeoutMs,\n237:       );\n238:       const onAbort = (): void => stop(\"cancelled\", \"reviewer cancelled\");\n239:       signal.addEventListener(\"abort\", onAbort, { once: true });\n240:       const capture = (target: Buffer[], chunk: Buffer): void => {\n241:         const remaining = this.maxOutputBytes - outputBytes;\n242:         if (remaining > 0) target.push(chunk.subarray(0, Math.min(remaining, chunk.byteLength)));\n243:         outputBytes += chunk.byteLength;\n244:         if (outputBytes > this.maxOutputBytes) {\n245:           stop(\"failed\", `reviewer output limit of ${this.maxOutputBytes} bytes exceeded`);\n246:         }\n247:       };\n248:       child.stdout.on(\"data\", (chunk: Buffer) => capture(stdout, chunk));\n249:       child.stderr.on(\"data\", (chunk: Buffer) => capture(stderr, chunk));\n250:       child.on(\"error\", (error) => stop(\"failed\", `reviewer spawn failed: ${error.message}`));\n251:       child.on(\"close\", (code) => {\n252:         if (settled) return;\n253:         settled = true;\n254:         clearTimeout(timer);\n255:         signal.removeEventListener(\"abort\", onAbort);\n256:         const stderrText = Buffer.concat(stderr).toString(\"utf8\");\n257:         if (outcome !== undefined) {\n258:           reject(new ReviewerExecutionError(outcome, reason || stderrText));\n259:           return;\n260:         }\n261:         if (code !== 0) {\n262:           reject(new ReviewerExecutionError(\"failed\", stderrText));\n263:           return;\n264:         }\n265:         resolve(Buffer.concat(stdout).toString(\"utf8\"));\n266:       });\n267:       child.stdin.on(\"error\", () => {\n268:         // The close event reports the authoritative process outcome.\n269:       });\n270:       child.stdin.end(request);\n271:     });\n272:   }\n273: }\n274: \n275: export class DeterministicReviewerAdapter implements ReviewerAdapter {\n276:   constructor(\n277:     private readonly supervisor: ProcessSupervisor,\n278:     private readonly executable: string\n279:   ) {}\n280: \n281:   async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n282:     // Reject matching worker and reviewer identities before spawning\n283:     if (input.workerId === input.reviewerId) {\n284:       throw new Error(\n285:         `reviewer identity must differ from worker identity: ${input.workerId}`\n286:       );\n287:     }\n288: \n289:     const diffSha256 = createHash(\"sha256\")\n290:       .update(input.diff, \"utf8\")\n291:       .digest(\"hex\");\n292: \n293:     const validationSha256 = canonicalValidationDigest(input.validation);\n294: \n295:     const result = await this.supervisor.execute(\n296:       {\n297:         taskId: \"review\",\n298:         executable: process.execPath,\n299:         args: [\n300:           this.executable,\n301:           \"--diff-sha256\",\n302:           diffSha256,\n303:           \"--validation-sha256\",\n304:           validationSha256,\n305:           \"--worker-id\",\n306:           input.workerId,\n307:           \"--reviewer-id\",\n308:           input.reviewerId,\n309:         ],\n310:         cwd: \"/tmp\",\n311:         timeoutMs: 30_000,\n312:       },\n313:       signal\n314:     );\n315: \n316:     if (result.outcome !== \"completed\") {\n317:       throw new ReviewerExecutionError(result.outcome, result.stderr);\n318:     }\n319:     if (result.events.length !== 1) {\n320:       throw new Error(\n321:         `reviewer protocol requires exactly one event, received ${result.events.length}`,\n322:       );\n323:     }\n324: \n325:     const parsed = ReviewDecisionSchema.safeParse(result.events[0]);\n326:     if (!parsed.success) {\n327:       throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n328:     }\n329:     return parsed.data;\n330:   }\n331: }\n\n(End of file - total 331 lines)\n</content>","metadata":{"preview":"import { createHash } from \"node:crypto\";\nimport { spawn } from \"node:child_process\";\nimport { z } from \"zod\";\nimport type { ProcessSupervisor } from \"../workers/process-supervisor.js\";\nimport type { ValidationReport } from \"../capabilities/validation-runner.js\";\n\nexport interface ReviewInput {\n  readonly workerId: string;\n  readonly reviewerId: string;\n  readonly diff: string;\n  readonly validation: ValidationReport;\n}\n\nexport interface ReviewDecision {\n  readonly reviewerId: string;\n  readonly approved: boolean;\n  readonly diffSha256: string;\n  readonly validationSha256: string;\n  readonly decidedAt: string;\n  readonly reason: string;","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/reviewer-adapter.ts","text":"import { createHash } from \"node:crypto\";\nimport { spawn } from \"node:child_process\";\nimport { z } from \"zod\";\nimport type { ProcessSupervisor } from \"../workers/process-supervisor.js\";\nimport type { ValidationReport } from \"../capabilities/validation-runner.js\";\n\nexport interface ReviewInput {\n  readonly workerId: string;\n  readonly reviewerId: string;\n  readonly diff: string;\n  readonly validation: ValidationReport;\n}\n\nexport interface ReviewDecision {\n  readonly reviewerId: string;\n  readonly approved: boolean;\n  readonly diffSha256: string;\n  readonly validationSha256: string;\n  readonly decidedAt: string;\n  readonly reason: string;\n}\n\nexport interface ReviewerAdapter {\n  review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision>;\n}\n\nexport class ReviewerExecutionError extends Error {\n  override readonly name = \"ReviewerExecutionError\";\n\n  constructor(\n    readonly outcome: \"cancelled\" | \"timed_out\" | \"failed\",\n    stderr: string,\n  ) {\n    super(\n      `reviewer process failed: outcome=${outcome}${\n        stderr === \"\" ? \"\" : `, stderr=${stderr}`\n      }`,\n    );\n  }\n}\n\nexport const ReviewDecisionSchema = z.strictObject({\n  reviewerId: z.string().min(1),\n  approved: z.boolean(),\n  diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n  validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n  decidedAt: z.string().datetime({ offset: true }),\n  reason: z.string().min(1),\n});\n\nconst ProcessReviewDecisionSchema = z.strictObject({\n  reviewerId: z.string().min(1),\n  decision: z.enum([\"approve\", \"deny\"]),\n  diffSha256: z.string().regex(/^[a-f0-9]{64}$/),\n  validationSha256: z.string().regex(/^[a-f0-9]{64}$/),\n  decidedAt: z.string().datetime({ offset: true }),\n  reason: z.string().min(1).max(4_096),\n});\n\nconst REVIEWER_ENV_ALLOWLIST = [\"PATH\", \"HOME\", \"TMPDIR\", \"LANG\", \"LC_ALL\"] as const;\nconst DEFAULT_REVIEWER_TIMEOUT_MS = 30_000;\nconst DEFAULT_MAX_REVIEW_INPUT_BYTES = 2 * 1_024 * 1_024;\nconst DEFAULT_MAX_REVIEW_OUTPUT_BYTES = 16 * 1_024;\n\nexport interface ProcessReviewerOptions {\n  readonly executable: string;\n  readonly args?: readonly string[];\n  readonly cwd?: string;\n  readonly timeoutMs?: number;\n  readonly maxInputBytes?: number;\n  readonly maxOutputBytes?: number;\n}\n\nfunction sha256(value: string): string {\n  return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n}\n\nexport function canonicalValidationDigest(validation: ValidationReport): string {\n  const argvSha256 = sha256(JSON.stringify(validation.command));\n  const outputSha256 = sha256(\n    JSON.stringify({ stdout: validation.stdout, stderr: validation.stderr }),\n  );\n  if (validation.argvSha256 !== argvSha256) {\n    throw new Error(\"validation argvSha256 is inconsistent with command evidence\");\n  }\n  if (validation.outputSha256 !== outputSha256) {\n    throw new Error(\"validation outputSha256 is inconsistent with output evidence\");\n  }\n  const canonical = JSON.stringify({\n    name: validation.name,\n    outcome: validation.outcome,\n    exitCode: validation.exitCode,\n    startedAt: validation.startedAt,\n    finishedAt: validation.finishedAt,\n    command: validation.command,\n    stdout: validation.stdout,\n    stderr: validation.stderr,\n    argvSha256,\n    outputSha256,\n    provenance: validation.provenance,\n  });\n  return sha256(canonical);\n}\n\nexport class ProcessReviewerAdapter implements ReviewerAdapter {\n  private readonly args: readonly string[];\n  private readonly cwd: string;\n  private readonly timeoutMs: number;\n  private readonly maxInputBytes: number;\n  private readonly maxOutputBytes: number;\n\n  constructor(private readonly options: ProcessReviewerOptions) {\n    this.args = options.args ?? [];\n    this.cwd = options.cwd ?? \"/tmp\";\n    this.timeoutMs = options.timeoutMs ?? DEFAULT_REVIEWER_TIMEOUT_MS;\n    this.maxInputBytes = options.maxInputBytes ?? DEFAULT_MAX_REVIEW_INPUT_BYTES;\n    this.maxOutputBytes = options.maxOutputBytes ?? DEFAULT_MAX_REVIEW_OUTPUT_BYTES;\n    for (const [name, value] of [\n      [\"timeoutMs\", this.timeoutMs],\n      [\"maxInputBytes\", this.maxInputBytes],\n      [\"maxOutputBytes\", this.maxOutputBytes],\n    ] as const) {\n      if (!Number.isSafeInteger(value) || value <= 0) {\n        throw new Error(`${name} must be a positive safe integer`);\n      }\n    }\n  }\n\n  async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n    if (input.workerId === input.reviewerId) {\n      throw new Error(\n        `reviewer identity must differ from worker identity: ${input.workerId}`,\n      );\n    }\n\n    const diffSha256 = sha256(input.diff);\n    const validationSha256 = canonicalValidationDigest(input.validation);\n    const request = JSON.stringify({\n      schemaVersion: 1,\n      workerId: input.workerId,\n      reviewerId: input.reviewerId,\n      diff: input.diff,\n      validation: input.validation,\n      diffSha256,\n      validationSha256,\n    });\n    const requestBytes = Buffer.byteLength(request, \"utf8\");\n    if (requestBytes > this.maxInputBytes) {\n      throw new Error(\n        `reviewer protocol input exceeded bounded evidence limit of ${this.maxInputBytes} bytes`,\n      );\n    }\n\n    const output = await this.execute(request, signal);\n    const lines = output.trim().split(\"\\n\").filter((line) => line.trim() !== \"\");\n    if (lines.length !== 1) {\n      throw new Error(\n        `reviewer protocol requires exactly one decision, received ${lines.length}`,\n      );\n    }\n\n    let decoded: unknown;\n    try {\n      decoded = JSON.parse(lines[0]!);\n    } catch {\n      throw new Error(\"reviewer protocol returned invalid JSON\");\n    }\n    const parsed = ProcessReviewDecisionSchema.safeParse(decoded);\n    if (!parsed.success) {\n      throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n    }\n    if (parsed.data.reviewerId !== input.reviewerId) {\n      throw new Error(\"reviewer protocol returned a reviewer identity mismatch\");\n    }\n    if (parsed.data.diffSha256 !== diffSha256) {\n      throw new Error(\"reviewer protocol returned a diff evidence digest mismatch\");\n    }\n    if (parsed.data.validationSha256 !== validationSha256) {\n      throw new Error(\"reviewer protocol returned a validation evidence digest mismatch\");\n    }\n\n    return ReviewDecisionSchema.parse({\n      reviewerId: parsed.data.reviewerId,\n      approved: parsed.data.decision === \"approve\",\n      diffSha256: parsed.data.diffSha256,\n      validationSha256: parsed.data.validationSha256,\n      decidedAt: parsed.data.decidedAt,\n      reason: parsed.data.reason,\n    });\n  }\n\n  private execute(request: string, signal: AbortSignal): Promise<string> {\n    if (signal.aborted) {\n      return Promise.reject(new ReviewerExecutionError(\"cancelled\", \"\"));\n    }\n\n    return new Promise((resolve, reject) => {\n      const env: Record<string, string> = {};\n      for (const key of REVIEWER_ENV_ALLOWLIST) {\n        const value = process.env[key];\n        if (value !== undefined) env[key] = value;\n      }\n      const child = spawn(this.options.executable, [...this.args], {\n        cwd: this.cwd,\n        shell: false,\n        detached: true,\n        env,\n        stdio: [\"pipe\", \"pipe\", \"pipe\"],\n      });\n      const stdout: Buffer[] = [];\n      const stderr: Buffer[] = [];\n      let outputBytes = 0;\n      let outcome: \"cancelled\" | \"timed_out\" | \"failed\" | undefined;\n      let reason = \"\";\n      let settled = false;\n\n      const kill = (): void => {\n        if (child.pid === undefined) return;\n        try {\n          process.kill(-child.pid, \"SIGKILL\");\n        } catch {\n          child.kill(\"SIGKILL\");\n        }\n      };\n      const stop = (\n        nextOutcome: \"cancelled\" | \"timed_out\" | \"failed\",\n        nextReason: string,\n      ): void => {\n        if (outcome !== undefined) return;\n        outcome = nextOutcome;\n        reason = nextReason;\n        kill();\n      };\n      const timer = setTimeout(\n        () => stop(\"timed_out\", \"reviewer timeout exceeded\"),\n        this.timeoutMs,\n      );\n      const onAbort = (): void => stop(\"cancelled\", \"reviewer cancelled\");\n      signal.addEventListener(\"abort\", onAbort, { once: true });\n      const capture = (target: Buffer[], chunk: Buffer): void => {\n        const remaining = this.maxOutputBytes - outputBytes;\n        if (remaining > 0) target.push(chunk.subarray(0, Math.min(remaining, chunk.byteLength)));\n        outputBytes += chunk.byteLength;\n        if (outputBytes > this.maxOutputBytes) {\n          stop(\"failed\", `reviewer output limit of ${this.maxOutputBytes} bytes exceeded`);\n        }\n      };\n      child.stdout.on(\"data\", (chunk: Buffer) => capture(stdout, chunk));\n      child.stderr.on(\"data\", (chunk: Buffer) => capture(stderr, chunk));\n      child.on(\"error\", (error) => stop(\"failed\", `reviewer spawn failed: ${error.message}`));\n      child.on(\"close\", (code) => {\n        if (settled) return;\n        settled = true;\n        clearTimeout(timer);\n        signal.removeEventListener(\"abort\", onAbort);\n        const stderrText = Buffer.concat(stderr).toString(\"utf8\");\n        if (outcome !== undefined) {\n          reject(new ReviewerExecutionError(outcome, reason || stderrText));\n          return;\n        }\n        if (code !== 0) {\n          reject(new ReviewerExecutionError(\"failed\", stderrText));\n          return;\n        }\n        resolve(Buffer.concat(stdout).toString(\"utf8\"));\n      });\n      child.stdin.on(\"error\", () => {\n        // The close event reports the authoritative process outcome.\n      });\n      child.stdin.end(request);\n    });\n  }\n}\n\nexport class DeterministicReviewerAdapter implements ReviewerAdapter {\n  constructor(\n    private readonly supervisor: ProcessSupervisor,\n    private readonly executable: string\n  ) {}\n\n  async review(input: ReviewInput, signal: AbortSignal): Promise<ReviewDecision> {\n    // Reject matching worker and reviewer identities before spawning\n    if (input.workerId === input.reviewerId) {\n      throw new Error(\n        `reviewer identity must differ from worker identity: ${input.workerId}`\n      );\n    }\n\n    const diffSha256 = createHash(\"sha256\")\n      .update(input.diff, \"utf8\")\n      .digest(\"hex\");\n\n    const validationSha256 = canonicalValidationDigest(input.validation);\n\n    const result = await this.supervisor.execute(\n      {\n        taskId: \"review\",\n        executable: process.execPath,\n        args: [\n          this.executable,\n          \"--diff-sha256\",\n          diffSha256,\n          \"--validation-sha256\",\n          validationSha256,\n          \"--worker-id\",\n          input.workerId,\n          \"--reviewer-id\",\n          input.reviewerId,\n        ],\n        cwd: \"/tmp\",\n        timeoutMs: 30_000,\n      },\n      signal\n    );\n\n    if (result.outcome !== \"completed\") {\n      throw new ReviewerExecutionError(result.outcome, result.stderr);\n    }\n    if (result.events.length !== 1) {\n      throw new Error(\n        `reviewer protocol requires exactly one event, received ${result.events.length}`,\n      );\n    }\n\n    const parsed = ReviewDecisionSchema.safeParse(result.events[0]);\n    if (!parsed.success) {\n      throw new Error(`reviewer protocol returned invalid decision: ${parsed.error.message}`);\n    }\n    return parsed.data;\n  }\n}","lineStart":1,"lineEnd":331,"totalLines":331,"truncated":false}},"title":"src/reviews/reviewer-adapter.ts","time":{"start":1783897851017,"end":1783897851047}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdd4481968042f34bfe0bbb18"}},"id":"prt_f589904860019IejRXAbdeNRFs","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851050,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_abOdnTLleJWuSnSGKPgj0046","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/cli/main.ts","offset":160,"limit":290},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/cli/main.ts</path>\n<type>file</type>\n<content>\n160:   project\n161:     .command(\"validate\")\n162:     .description(\"Validate a Zentra project configuration file.\")\n163:     .requiredOption(\"--config <path>\", \"project configuration file\")\n164:     .action((options: ProjectOptions) => {\n165:       const configs = loadProjects(options.config);\n166:       new ProjectRegistry(configs);\n167:       setResult({\n168:         exitCode: 0,\n169:         value: {\n170:           command: \"project.validate\",\n171:           status: \"valid\",\n172:           projectIds: configs.map((config) => config.projectId),\n173:         },\n174:       });\n175:     });\n176: \n177:   const task = program.command(\"task\").description(\"Run and inspect deterministic tasks.\");\n178:   task\n179:     .command(\"run\")\n180:     .description(\"Run one deterministic local tracer-bullet task.\")\n181:     .requiredOption(\"--config <path>\", \"project configuration file\")\n182:     .requiredOption(\"--database <path>\", \"SQLite event journal\")\n183:     .requiredOption(\"--task-id <id>\", \"safe task identity\")\n184:     .requiredOption(\"--title <title>\", \"task title\")\n185:     .requiredOption(\"--file <relative-path>\", \"one root-level file\")\n186:     .requiredOption(\"--content <text>\", \"replacement file content\")\n187:     .option(\"--reviewer-executable <path>\", \"content-aware reviewer executable\")\n188:     .option(\n189:       \"--reviewer-argument <value>\",\n190:       \"argument passed to the configured reviewer executable\",\n191:       (value: string, previous: string[]) => [...previous, value],\n192:       [],\n193:     )\n194:     .option(\"--reviewer-id <id>\", \"independent reviewer identity\")\n195:     .action(async (options: RunOptions) => {\n196:       assertSafeTaskId(options.taskId);\n197:       assertSafeTitle(options.title);\n198:       assertSafeRootFile(options.file);\n199:       assertSafeContent(options.content);\n200:       const configs = loadProjects(options.config);\n201:       if (configs.length !== 1) {\n202:         throw new CliFailure(\"INVALID_CONFIG\");\n203:       }\n204:       const projectConfig = configs[0]!;\n205:       assertRepresentableTaskRun({\n206:         taskId: options.taskId,\n207:         projectId: projectConfig.projectId,\n208:         title: options.title,\n209:       });\n210:       const reviewer = configuredReviewer(options);\n211:       if (reviewer === null) {\n212:         const denied = await withSystem(\n213:           options.database,\n214:           configs,\n215:           \"read-write\",\n216:           (system) => Promise.resolve(denyMissingReviewer(system.tasks, {\n217:             taskId: options.taskId,\n218:             projectId: projectConfig.projectId,\n219:             title: options.title,\n220:           })),\n221:         );\n222:         setResult(taskRunResult(denied));\n223:         return;\n224:       }\n225:       const workerFixture = attestedWorkerFixture(fixtureAnchor);\n226:       const result = await withSystem(options.database, configs, \"read-write\", async (system) => {\n227:         const taskView = await system.execution(workerFixture, reviewer.adapter).orchestrator.run({\n228:           taskId: options.taskId,\n229:           projectId: projectConfig.projectId,\n230:           title: options.title,\n231:           workerId: WORKER_ID,\n232:           reviewerId: reviewer.reviewerId,\n233:           workerRequest: {\n234:             executable: process.execPath,\n235:             args: [\n236:               workerFixture,\n237:               \"--file\",\n238:               options.file,\n239:               \"--content\",\n240:               options.content,\n241:             ],\n242:             timeoutMs: WORKER_TIMEOUT_MS,\n243:           },\n244:           signal,\n245:         });\n246:         return taskView;\n247:       });\n248:       setResult(taskRunResult(result));\n249:     });\n250: \n251:   task\n252:     .command(\"status\")\n253:     .description(\"Replay one task status from the event journal.\")\n254:     .requiredOption(\"--database <path>\", \"SQLite event journal\")\n255:     .requiredOption(\"--task-id <id>\", \"safe task identity\")\n256:     .action(async (options: DatabaseTaskOptions) => {\n257:       assertSafeTaskId(options.taskId);\n258:       const taskView = await withSystem(options.database, [], \"read-only\", (system) =>\n259:         Promise.resolve(system.tasks.get(options.taskId)));\n260:       if (taskView === null) {\n261:         throw new CliFailure(\"TASK_NOT_FOUND\");\n262:       }\n263:       setResult({ exitCode: 0, value: { command: \"task.status\", task: taskView } });\n264:     });\n265: \n266:   program\n267:     .command(\"recover\")\n268:     .description(\"Inspect one task and return its safe recovery classification.\")\n269:     .requiredOption(\"--config <path>\", \"project configuration file\")\n270:     .requiredOption(\"--database <path>\", \"SQLite event journal\")\n271:     .requiredOption(\"--task-id <id>\", \"safe task identity\")\n272:     .action(async (options: RecoverOptions) => {\n273:       assertSafeTaskId(options.taskId);\n274:       const configs = loadProjects(options.config);\n275:       const decision = await withSystem(\n276:         options.database,\n277:         configs,\n278:         \"read-only\",\n279:         (system) =>\n280:         system.recovery().inspect(options.taskId));\n281:       setResult({\n282:         exitCode: decision.action === \"record_failure\" ? 1 : 0,\n283:         value: { command: \"recover\", decision: publicRecoveryDecision(decision) },\n284:       });\n285:     });\n286: \n287:   return program;\n288: }\n289: \n290: async function withSystem<T>(\n291:   databasePath: string,\n292:   configs: readonly ProjectConfig[],\n293:   mode: \"read-only\" | \"read-write\",\n294:   operation: (system: ReturnType<typeof composeSystem>) => Promise<T>,\n295: ): Promise<T> {\n296:   const journal = openJournal(databasePath, mode);\n297:   try {\n298:     return await operation(composeSystem(journal, configs));\n299:   } finally {\n300:     journal.close();\n301:   }\n302: }\n303: \n304: function openJournal(\n305:   databasePath: string,\n306:   mode: \"read-only\" | \"read-write\",\n307: ): SqliteEventJournal {\n308:   if (mode === \"read-write\") return new SqliteEventJournal(databasePath);\n309:   if (!existsSync(databasePath)) throw new CliFailure(\"DATABASE_NOT_FOUND\");\n310:   try {\n311:     return SqliteEventJournal.openReadOnly(databasePath);\n312:   } catch (error) {\n313:     if (!existsSync(databasePath)) throw new CliFailure(\"DATABASE_NOT_FOUND\");\n314:     throw error;\n315:   }\n316: }\n317: \n318: function composeSystem(\n319:   journal: SqliteEventJournal,\n320:   configs: readonly ProjectConfig[],\n321: ) {\n322:   const tasks = new TaskService(journal);\n323:   let development: {\n324:     readonly projects: ProjectRegistry;\n325:     readonly git: GitClient;\n326:     readonly worktrees: WorktreeManager;\n327:   } | undefined;\n328:   const developmentDependencies = () => {\n329:     development ??= (() => {\n330:       const git = new GitClient();\n331:       return {\n332:         projects: new ProjectRegistry(configs),\n333:         git,\n334:         worktrees: new WorktreeManager(git),\n335:       };\n336:     })();\n337:     return development;\n338:   };\n339: \n340:   return {\n341:     tasks,\n342:     recovery(): RecoveryService {\n343:       const { projects, git, worktrees } = developmentDependencies();\n344:       return new RecoveryService(journal, tasks, projects, worktrees, git);\n345:     },\n346:     execution(workerFixture: string, reviewer: ReviewerAdapter) {\n347:       const { projects, git, worktrees } = developmentDependencies();\n348:       const supervisor = new ProcessSupervisor();\n349:       const validations = new ValidationRunner(supervisor);\n350:       return {\n351:         orchestrator: new TracerBulletOrchestrator(\n352:           tasks,\n353:           projects,\n354:           worktrees,\n355:           supervisor,\n356:           validations,\n357:           reviewer,\n358:           new ReviewGate(),\n359:           new IntegrationQueue(git, validations),\n360:         ),\n361:       };\n362:     },\n363:   };\n364: }\n365: \n366: function taskRunResult(task: TaskView): CommandResult {\n367:   return {\n368:     exitCode: task.terminalOutcome === \"completed\" ? 0 : 1,\n369:     value: {\n370:       command: \"task.run\",\n371:       outcome: task.terminalOutcome,\n372:       task,\n373:     },\n374:   };\n375: }\n376: \n377: function loadProjects(configPath: string): readonly ProjectConfig[] {\n378:   try {\n379:     const configStat = statSync(configPath);\n380:     if (!configStat.isFile() || configStat.size > MAX_CONFIG_BYTES) {\n381:       throw new Error(\"invalid config file\");\n382:     }\n383:     const parsed: unknown = JSON.parse(readFileSync(configPath, \"utf8\"));\n384:     const candidates = Array.isArray(parsed) ? parsed : [parsed];\n385:     if (candidates.length === 0 || candidates.length > MAX_PROJECT_CONFIGS) {\n386:       throw new Error(\"invalid configuration count\");\n387:     }\n388:     const configs = candidates.map((candidate) => ProjectConfigSchema.parse(candidate));\n389:     if (configs.some((config) =>\n390:       Buffer.byteLength(config.projectId, \"utf8\") > MAX_PROJECT_ID_BYTES\n391:     )) {\n392:       throw new Error(\"project identity is too large\");\n393:     }\n394:     return configs;\n395:   } catch {\n396:     throw new CliFailure(\"INVALID_CONFIG\");\n397:   }\n398: }\n399: \n400: function attestedWorkerFixture(anchor?: string | URL): string {\n401:   try {\n402:     return anchor === undefined\n403:       ? resolveBundledFixture(\"deterministic-worker.mjs\")\n404:       : resolveBundledFixture(\"deterministic-worker.mjs\", anchor);\n405:   } catch {\n406:     throw new CliFailure(\"BUNDLED_FIXTURE_INVALID\");\n407:   }\n408: }\n409: \n410: function configuredReviewer(options: RunOptions): {\n411:   readonly reviewerId: string;\n412:   readonly adapter: ReviewerAdapter;\n413: } | null {\n414:   const executable = options.reviewerExecutable;\n415:   const reviewerId = options.reviewerId;\n416:   if (executable === undefined && reviewerId === undefined && options.reviewerArgument.length === 0) {\n417:     return null;\n418:   }\n419:   if (\n420:     executable === undefined ||\n421:     executable.length === 0 ||\n422:     reviewerId === undefined ||\n423:     reviewerId.length === 0\n424:   ) {\n425:     throw new CliFailure(\"INVALID_COMMAND\");\n426:   }\n427:   return {\n428:     reviewerId,\n429:     adapter: new ProcessReviewerAdapter({\n430:       executable,\n431:       args: options.reviewerArgument,\n432:     }),\n433:   };\n434: }\n435: \n436: function denyMissingReviewer(\n437:   tasks: TaskService,\n438:   input: { readonly taskId: string; readonly projectId: string; readonly title: string },\n439: ): TaskView {\n440:   tasks.create({\n441:     ...input,\n442:     correlationId: input.taskId,\n443:   });\n444:   return tasks.append(\n445:     input.taskId,\n446:     \"task.denied\",\n447:     {\n448:       stage: \"setup\",\n449:       reason: \"content-aware reviewer is not configured\",\n\n(Showing lines 160-449 of 579. Use offset=450 to continue.)\n</content>","metadata":{"preview":"  project\n    .command(\"validate\")\n    .description(\"Validate a Zentra project configuration file.\")\n    .requiredOption(\"--config <path>\", \"project configuration file\")\n    .action((options: ProjectOptions) => {\n      const configs = loadProjects(options.config);\n      new ProjectRegistry(configs);\n      setResult({\n        exitCode: 0,\n        value: {\n          command: \"project.validate\",\n          status: \"valid\",\n          projectIds: configs.map((config) => config.projectId),\n        },\n      });\n    });\n\n  const task = program.command(\"task\").description(\"Run and inspect deterministic tasks.\");\n  task\n    .command(\"run\")","truncated":true,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/cli/main.ts","text":"  project\n    .command(\"validate\")\n    .description(\"Validate a Zentra project configuration file.\")\n    .requiredOption(\"--config <path>\", \"project configuration file\")\n    .action((options: ProjectOptions) => {\n      const configs = loadProjects(options.config);\n      new ProjectRegistry(configs);\n      setResult({\n        exitCode: 0,\n        value: {\n          command: \"project.validate\",\n          status: \"valid\",\n          projectIds: configs.map((config) => config.projectId),\n        },\n      });\n    });\n\n  const task = program.command(\"task\").description(\"Run and inspect deterministic tasks.\");\n  task\n    .command(\"run\")\n    .description(\"Run one deterministic local tracer-bullet task.\")\n    .requiredOption(\"--config <path>\", \"project configuration file\")\n    .requiredOption(\"--database <path>\", \"SQLite event journal\")\n    .requiredOption(\"--task-id <id>\", \"safe task identity\")\n    .requiredOption(\"--title <title>\", \"task title\")\n    .requiredOption(\"--file <relative-path>\", \"one root-level file\")\n    .requiredOption(\"--content <text>\", \"replacement file content\")\n    .option(\"--reviewer-executable <path>\", \"content-aware reviewer executable\")\n    .option(\n      \"--reviewer-argument <value>\",\n      \"argument passed to the configured reviewer executable\",\n      (value: string, previous: string[]) => [...previous, value],\n      [],\n    )\n    .option(\"--reviewer-id <id>\", \"independent reviewer identity\")\n    .action(async (options: RunOptions) => {\n      assertSafeTaskId(options.taskId);\n      assertSafeTitle(options.title);\n      assertSafeRootFile(options.file);\n      assertSafeContent(options.content);\n      const configs = loadProjects(options.config);\n      if (configs.length !== 1) {\n        throw new CliFailure(\"INVALID_CONFIG\");\n      }\n      const projectConfig = configs[0]!;\n      assertRepresentableTaskRun({\n        taskId: options.taskId,\n        projectId: projectConfig.projectId,\n        title: options.title,\n      });\n      const reviewer = configuredReviewer(options);\n      if (reviewer === null) {\n        const denied = await withSystem(\n          options.database,\n          configs,\n          \"read-write\",\n          (system) => Promise.resolve(denyMissingReviewer(system.tasks, {\n            taskId: options.taskId,\n            projectId: projectConfig.projectId,\n            title: options.title,\n          })),\n        );\n        setResult(taskRunResult(denied));\n        return;\n      }\n      const workerFixture = attestedWorkerFixture(fixtureAnchor);\n      const result = await withSystem(options.database, configs, \"read-write\", async (system) => {\n        const taskView = await system.execution(workerFixture, reviewer.adapter).orchestrator.run({\n          taskId: options.taskId,\n          projectId: projectConfig.projectId,\n          title: options.title,\n          workerId: WORKER_ID,\n          reviewerId: reviewer.reviewerId,\n          workerRequest: {\n            executable: process.execPath,\n            args: [\n              workerFixture,\n              \"--file\",\n              options.file,\n              \"--content\",\n              options.content,\n            ],\n            timeoutMs: WORKER_TIMEOUT_MS,\n          },\n          signal,\n        });\n        return taskView;\n      });\n      setResult(taskRunResult(result));\n    });\n\n  task\n    .command(\"status\")\n    .description(\"Replay one task status from the event journal.\")\n    .requiredOption(\"--database <path>\", \"SQLite event journal\")\n    .requiredOption(\"--task-id <id>\", \"safe task identity\")\n    .action(async (options: DatabaseTaskOptions) => {\n      assertSafeTaskId(options.taskId);\n      const taskView = await withSystem(options.database, [], \"read-only\", (system) =>\n        Promise.resolve(system.tasks.get(options.taskId)));\n      if (taskView === null) {\n        throw new CliFailure(\"TASK_NOT_FOUND\");\n      }\n      setResult({ exitCode: 0, value: { command: \"task.status\", task: taskView } });\n    });\n\n  program\n    .command(\"recover\")\n    .description(\"Inspect one task and return its safe recovery classification.\")\n    .requiredOption(\"--config <path>\", \"project configuration file\")\n    .requiredOption(\"--database <path>\", \"SQLite event journal\")\n    .requiredOption(\"--task-id <id>\", \"safe task identity\")\n    .action(async (options: RecoverOptions) => {\n      assertSafeTaskId(options.taskId);\n      const configs = loadProjects(options.config);\n      const decision = await withSystem(\n        options.database,\n        configs,\n        \"read-only\",\n        (system) =>\n        system.recovery().inspect(options.taskId));\n      setResult({\n        exitCode: decision.action === \"record_failure\" ? 1 : 0,\n        value: { command: \"recover\", decision: publicRecoveryDecision(decision) },\n      });\n    });\n\n  return program;\n}\n\nasync function withSystem<T>(\n  databasePath: string,\n  configs: readonly ProjectConfig[],\n  mode: \"read-only\" | \"read-write\",\n  operation: (system: ReturnType<typeof composeSystem>) => Promise<T>,\n): Promise<T> {\n  const journal = openJournal(databasePath, mode);\n  try {\n    return await operation(composeSystem(journal, configs));\n  } finally {\n    journal.close();\n  }\n}\n\nfunction openJournal(\n  databasePath: string,\n  mode: \"read-only\" | \"read-write\",\n): SqliteEventJournal {\n  if (mode === \"read-write\") return new SqliteEventJournal(databasePath);\n  if (!existsSync(databasePath)) throw new CliFailure(\"DATABASE_NOT_FOUND\");\n  try {\n    return SqliteEventJournal.openReadOnly(databasePath);\n  } catch (error) {\n    if (!existsSync(databasePath)) throw new CliFailure(\"DATABASE_NOT_FOUND\");\n    throw error;\n  }\n}\n\nfunction composeSystem(\n  journal: SqliteEventJournal,\n  configs: readonly ProjectConfig[],\n) {\n  const tasks = new TaskService(journal);\n  let development: {\n    readonly projects: ProjectRegistry;\n    readonly git: GitClient;\n    readonly worktrees: WorktreeManager;\n  } | undefined;\n  const developmentDependencies = () => {\n    development ??= (() => {\n      const git = new GitClient();\n      return {\n        projects: new ProjectRegistry(configs),\n        git,\n        worktrees: new WorktreeManager(git),\n      };\n    })();\n    return development;\n  };\n\n  return {\n    tasks,\n    recovery(): RecoveryService {\n      const { projects, git, worktrees } = developmentDependencies();\n      return new RecoveryService(journal, tasks, projects, worktrees, git);\n    },\n    execution(workerFixture: string, reviewer: ReviewerAdapter) {\n      const { projects, git, worktrees } = developmentDependencies();\n      const supervisor = new ProcessSupervisor();\n      const validations = new ValidationRunner(supervisor);\n      return {\n        orchestrator: new TracerBulletOrchestrator(\n          tasks,\n          projects,\n          worktrees,\n          supervisor,\n          validations,\n          reviewer,\n          new ReviewGate(),\n          new IntegrationQueue(git, validations),\n        ),\n      };\n    },\n  };\n}\n\nfunction taskRunResult(task: TaskView): CommandResult {\n  return {\n    exitCode: task.terminalOutcome === \"completed\" ? 0 : 1,\n    value: {\n      command: \"task.run\",\n      outcome: task.terminalOutcome,\n      task,\n    },\n  };\n}\n\nfunction loadProjects(configPath: string): readonly ProjectConfig[] {\n  try {\n    const configStat = statSync(configPath);\n    if (!configStat.isFile() || configStat.size > MAX_CONFIG_BYTES) {\n      throw new Error(\"invalid config file\");\n    }\n    const parsed: unknown = JSON.parse(readFileSync(configPath, \"utf8\"));\n    const candidates = Array.isArray(parsed) ? parsed : [parsed];\n    if (candidates.length === 0 || candidates.length > MAX_PROJECT_CONFIGS) {\n      throw new Error(\"invalid configuration count\");\n    }\n    const configs = candidates.map((candidate) => ProjectConfigSchema.parse(candidate));\n    if (configs.some((config) =>\n      Buffer.byteLength(config.projectId, \"utf8\") > MAX_PROJECT_ID_BYTES\n    )) {\n      throw new Error(\"project identity is too large\");\n    }\n    return configs;\n  } catch {\n    throw new CliFailure(\"INVALID_CONFIG\");\n  }\n}\n\nfunction attestedWorkerFixture(anchor?: string | URL): string {\n  try {\n    return anchor === undefined\n      ? resolveBundledFixture(\"deterministic-worker.mjs\")\n      : resolveBundledFixture(\"deterministic-worker.mjs\", anchor);\n  } catch {\n    throw new CliFailure(\"BUNDLED_FIXTURE_INVALID\");\n  }\n}\n\nfunction configuredReviewer(options: RunOptions): {\n  readonly reviewerId: string;\n  readonly adapter: ReviewerAdapter;\n} | null {\n  const executable = options.reviewerExecutable;\n  const reviewerId = options.reviewerId;\n  if (executable === undefined && reviewerId === undefined && options.reviewerArgument.length === 0) {\n    return null;\n  }\n  if (\n    executable === undefined ||\n    executable.length === 0 ||\n    reviewerId === undefined ||\n    reviewerId.length === 0\n  ) {\n    throw new CliFailure(\"INVALID_COMMAND\");\n  }\n  return {\n    reviewerId,\n    adapter: new ProcessReviewerAdapter({\n      executable,\n      args: options.reviewerArgument,\n    }),\n  };\n}\n\nfunction denyMissingReviewer(\n  tasks: TaskService,\n  input: { readonly taskId: string; readonly projectId: string; readonly title: string },\n): TaskView {\n  tasks.create({\n    ...input,\n    correlationId: input.taskId,\n  });\n  return tasks.append(\n    input.taskId,\n    \"task.denied\",\n    {\n      stage: \"setup\",\n      reason: \"content-aware reviewer is not configured\",","lineStart":160,"lineEnd":449,"totalLines":579,"truncated":true}},"title":"src/cli/main.ts","time":{"start":1783897851020,"end":1783897851048}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdd6c8196bd748173fb63ec7b"}},"id":"prt_f5899048a001qy2hsZEs2yGujY","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851054,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_loLzV6HeR1tmyrmIrdRcCt0k","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","offset":220,"limit":90},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts</path>\n<type>file</type>\n<content>\n220:         },\n221:       },\n222:     });\n223:     expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:greeting.txt\"]))\n224:       .toBe(\"hello from CLI\");\n225:     expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\", \"task-cli\"))).toBe(false);\n226:     const ticketRef = await new GitClient().run(testFixture.repositoryPath, [\n227:       \"show-ref\",\n228:       \"--verify\",\n229:       \"refs/heads/ticket/task-cli\",\n230:     ]);\n231:     expect(ticketRef.exitCode).not.toBe(0);\n232: \n233:     const status = await invoke([\n234:       \"task\", \"status\",\n235:       \"--database\", testFixture.databasePath,\n236:       \"--task-id\", \"task-cli\",\n237:     ]);\n238:     expect(status.code).toBe(0);\n239:     expect(status.json).toEqual({ command: \"task.status\", task: run.json.task });\n240:   });\n241: \n242:   it(\"denies by default without reviewer configuration before commit or integration\", async () => {\n243:     const testFixture = await fixture();\n244:     const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n245: \n246:     const result = await invoke(runArguments(testFixture, \"task-no-reviewer\", false));\n247: \n248:     expect(result).toMatchObject({\n249:       code: 1,\n250:       stdout: \"\",\n251:       json: {\n252:         command: \"task.run\",\n253:         outcome: \"denied\",\n254:         task: {\n255:           taskId: \"task-no-reviewer\",\n256:           lifecycle: \"terminal\",\n257:           terminalOutcome: \"denied\",\n258:         },\n259:       },\n260:     });\n261:     expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n262:     expect((await new GitClient().run(testFixture.repositoryPath, [\n263:       \"show-ref\",\n264:       \"--verify\",\n265:       \"refs/heads/zentra/integration\",\n266:     ])).exitCode).not.toBe(0);\n267:     expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\"))).toBe(false);\n268:   });\n269: \n270:   it(\"denies an adversarial diff that passes focused validation without committing it\", async () => {\n271:     const testFixture = await fixture();\n272:     const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n273:     const args = [\n274:       ...runArguments(testFixture, \"task-dangerous-review\"),\n275:       \"--file\", \"auth.ts\",\n276:       \"--content\", \"export const requireAuthentication = false;\\n\",\n277:     ];\n278: \n279:     const result = await invoke(args);\n280: \n281:     expect(result).toMatchObject({\n282:       code: 1,\n283:       stdout: \"\",\n284:       json: {\n285:         command: \"task.run\",\n286:         outcome: \"denied\",\n287:         task: { terminalOutcome: \"denied\" },\n288:       },\n289:     });\n290:     expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n291:     expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:auth.ts\"]))\n292:       .toBe(\"export const requireAuthentication = true;\");\n293:   });\n294: \n295:   it(\"returns a successful no-op recovery decision for a completed task\", async () => {\n296:     const testFixture = await fixture();\n297:     await invoke(runArguments(testFixture, \"task-recover\"));\n298: \n299:     const result = await invoke([\n300:       \"recover\",\n301:       \"--config\", testFixture.configPath,\n302:       \"--database\", testFixture.databasePath,\n303:       \"--task-id\", \"task-recover\",\n304:     ]);\n305: \n306:     expect(result).toMatchObject({\n307:       code: 0,\n308:       stderr: \"\",\n309:       json: {\n\n(Showing lines 220-309 of 1053. Use offset=310 to continue.)\n</content>","metadata":{"preview":"        },\n      },\n    });\n    expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:greeting.txt\"]))\n      .toBe(\"hello from CLI\");\n    expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\", \"task-cli\"))).toBe(false);\n    const ticketRef = await new GitClient().run(testFixture.repositoryPath, [\n      \"show-ref\",\n      \"--verify\",\n      \"refs/heads/ticket/task-cli\",\n    ]);\n    expect(ticketRef.exitCode).not.toBe(0);\n\n    const status = await invoke([\n      \"task\", \"status\",\n      \"--database\", testFixture.databasePath,\n      \"--task-id\", \"task-cli\",\n    ]);\n    expect(status.code).toBe(0);\n    expect(status.json).toEqual({ command: \"task.status\", task: run.json.task });","truncated":true,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","text":"        },\n      },\n    });\n    expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:greeting.txt\"]))\n      .toBe(\"hello from CLI\");\n    expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\", \"task-cli\"))).toBe(false);\n    const ticketRef = await new GitClient().run(testFixture.repositoryPath, [\n      \"show-ref\",\n      \"--verify\",\n      \"refs/heads/ticket/task-cli\",\n    ]);\n    expect(ticketRef.exitCode).not.toBe(0);\n\n    const status = await invoke([\n      \"task\", \"status\",\n      \"--database\", testFixture.databasePath,\n      \"--task-id\", \"task-cli\",\n    ]);\n    expect(status.code).toBe(0);\n    expect(status.json).toEqual({ command: \"task.status\", task: run.json.task });\n  });\n\n  it(\"denies by default without reviewer configuration before commit or integration\", async () => {\n    const testFixture = await fixture();\n    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n\n    const result = await invoke(runArguments(testFixture, \"task-no-reviewer\", false));\n\n    expect(result).toMatchObject({\n      code: 1,\n      stdout: \"\",\n      json: {\n        command: \"task.run\",\n        outcome: \"denied\",\n        task: {\n          taskId: \"task-no-reviewer\",\n          lifecycle: \"terminal\",\n          terminalOutcome: \"denied\",\n        },\n      },\n    });\n    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n    expect((await new GitClient().run(testFixture.repositoryPath, [\n      \"show-ref\",\n      \"--verify\",\n      \"refs/heads/zentra/integration\",\n    ])).exitCode).not.toBe(0);\n    expect(existsSync(path.join(testFixture.baseDirectory, \"worktrees\"))).toBe(false);\n  });\n\n  it(\"denies an adversarial diff that passes focused validation without committing it\", async () => {\n    const testFixture = await fixture();\n    const initialHead = await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"]);\n    const args = [\n      ...runArguments(testFixture, \"task-dangerous-review\"),\n      \"--file\", \"auth.ts\",\n      \"--content\", \"export const requireAuthentication = false;\\n\",\n    ];\n\n    const result = await invoke(args);\n\n    expect(result).toMatchObject({\n      code: 1,\n      stdout: \"\",\n      json: {\n        command: \"task.run\",\n        outcome: \"denied\",\n        task: { terminalOutcome: \"denied\" },\n      },\n    });\n    expect(await gitOk(testFixture.repositoryPath, [\"rev-parse\", \"HEAD\"])).toBe(initialHead);\n    expect(await gitOk(testFixture.repositoryPath, [\"show\", \"zentra/integration:auth.ts\"]))\n      .toBe(\"export const requireAuthentication = true;\");\n  });\n\n  it(\"returns a successful no-op recovery decision for a completed task\", async () => {\n    const testFixture = await fixture();\n    await invoke(runArguments(testFixture, \"task-recover\"));\n\n    const result = await invoke([\n      \"recover\",\n      \"--config\", testFixture.configPath,\n      \"--database\", testFixture.databasePath,\n      \"--task-id\", \"task-recover\",\n    ]);\n\n    expect(result).toMatchObject({\n      code: 0,\n      stderr: \"\",\n      json: {","lineStart":220,"lineEnd":309,"totalLines":1053,"truncated":true}},"title":"tests/orchestration/cli.test.ts","time":{"start":1783897851032,"end":1783897851051}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcddb081969fe648f06ce9ef06"}},"id":"prt_f58990497001Xh5mrQVDqEno6m","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851054,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_fOBwqxgVi3HPjZZVj8RtGzs2","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","offset":1000,"limit":60},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts</path>\n<type>file</type>\n<content>\n1000:   });\n1001: \n1002:   it(\"supports the documented pnpm start help invocation\", async () => {\n1003:     const root = path.resolve(import.meta.dirname, \"../..\");\n1004: \n1005:     const result = await execFileAsync(\"pnpm\", [\"start\", \"--\", \"--help\"], { cwd: root });\n1006: \n1007:     expect(result.stderr).toBe(\"\");\n1008:     expect(result.stdout).toMatch(/\\bproject\\b/);\n1009:     expect(result.stdout).toMatch(/\\btask\\b/);\n1010:     expect(result.stdout).toMatch(/\\brecover\\b/);\n1011:   });\n1012: \n1013:   it(\"runs the bundled worker and configured content-aware reviewer through the built entry point\", async () => {\n1014:     const root = path.resolve(import.meta.dirname, \"../..\");\n1015:     const testFixture = await fixture();\n1016: \n1017:     const result = await execFileAsync(\n1018:       process.execPath,\n1019:       [\"dist/src/cli/main.js\", ...runArguments(testFixture, \"task-built\")],\n1020:       { cwd: root },\n1021:     );\n1022: \n1023:     expect(result.stderr).toBe(\"\");\n1024:     expect(JSON.parse(result.stdout)).toMatchObject({\n1025:       command: \"task.run\",\n1026:       outcome: \"completed\",\n1027:       task: { taskId: \"task-built\", terminalOutcome: \"completed\" },\n1028:     });\n1029:   });\n1030: \n1031:   it(\"does not wire the deterministic test reviewer into the built production CLI\", () => {\n1032:     const root = path.resolve(import.meta.dirname, \"../..\");\n1033:     const builtCli = readFileSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), \"utf8\");\n1034: \n1035:     expect(builtCli).not.toContain(\"deterministic-reviewer.mjs\");\n1036:     expect(builtCli).not.toContain(\"DeterministicReviewerAdapter\");\n1037:   });\n1038: \n1039:   it(\"executes help through a real symlink to the built entry point\", async () => {\n1040:     const root = path.resolve(import.meta.dirname, \"../..\");\n1041:     const directory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-cli-link-\")));\n1042:     temporaryDirectories.push(directory);\n1043:     const linkedEntry = path.join(directory, \"zentra-linked.mjs\");\n1044:     symlinkSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), linkedEntry);\n1045: \n1046:     const result = await execFileAsync(process.execPath, [linkedEntry, \"--help\"], { cwd: root });\n1047: \n1048:     expect(result.stderr).toBe(\"\");\n1049:     expect(result.stdout).toMatch(/\\bproject\\b/);\n1050:     expect(result.stdout).toMatch(/\\btask\\b/);\n1051:     expect(result.stdout).toMatch(/\\brecover\\b/);\n1052:   });\n1053: });\n\n(End of file - total 1053 lines)\n</content>","metadata":{"preview":"  });\n\n  it(\"supports the documented pnpm start help invocation\", async () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n\n    const result = await execFileAsync(\"pnpm\", [\"start\", \"--\", \"--help\"], { cwd: root });\n\n    expect(result.stderr).toBe(\"\");\n    expect(result.stdout).toMatch(/\\bproject\\b/);\n    expect(result.stdout).toMatch(/\\btask\\b/);\n    expect(result.stdout).toMatch(/\\brecover\\b/);\n  });\n\n  it(\"runs the bundled worker and configured content-aware reviewer through the built entry point\", async () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n    const testFixture = await fixture();\n\n    const result = await execFileAsync(\n      process.execPath,\n      [\"dist/src/cli/main.js\", ...runArguments(testFixture, \"task-built\")],","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","text":"  });\n\n  it(\"supports the documented pnpm start help invocation\", async () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n\n    const result = await execFileAsync(\"pnpm\", [\"start\", \"--\", \"--help\"], { cwd: root });\n\n    expect(result.stderr).toBe(\"\");\n    expect(result.stdout).toMatch(/\\bproject\\b/);\n    expect(result.stdout).toMatch(/\\btask\\b/);\n    expect(result.stdout).toMatch(/\\brecover\\b/);\n  });\n\n  it(\"runs the bundled worker and configured content-aware reviewer through the built entry point\", async () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n    const testFixture = await fixture();\n\n    const result = await execFileAsync(\n      process.execPath,\n      [\"dist/src/cli/main.js\", ...runArguments(testFixture, \"task-built\")],\n      { cwd: root },\n    );\n\n    expect(result.stderr).toBe(\"\");\n    expect(JSON.parse(result.stdout)).toMatchObject({\n      command: \"task.run\",\n      outcome: \"completed\",\n      task: { taskId: \"task-built\", terminalOutcome: \"completed\" },\n    });\n  });\n\n  it(\"does not wire the deterministic test reviewer into the built production CLI\", () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n    const builtCli = readFileSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), \"utf8\");\n\n    expect(builtCli).not.toContain(\"deterministic-reviewer.mjs\");\n    expect(builtCli).not.toContain(\"DeterministicReviewerAdapter\");\n  });\n\n  it(\"executes help through a real symlink to the built entry point\", async () => {\n    const root = path.resolve(import.meta.dirname, \"../..\");\n    const directory = realpathSync(mkdtempSync(path.join(tmpdir(), \"zentra-cli-link-\")));\n    temporaryDirectories.push(directory);\n    const linkedEntry = path.join(directory, \"zentra-linked.mjs\");\n    symlinkSync(path.join(root, \"dist\", \"src\", \"cli\", \"main.js\"), linkedEntry);\n\n    const result = await execFileAsync(process.execPath, [linkedEntry, \"--help\"], { cwd: root });\n\n    expect(result.stderr).toBe(\"\");\n    expect(result.stdout).toMatch(/\\bproject\\b/);\n    expect(result.stdout).toMatch(/\\btask\\b/);\n    expect(result.stdout).toMatch(/\\brecover\\b/);\n  });\n});","lineStart":1000,"lineEnd":1053,"totalLines":1053,"truncated":false}},"title":"tests/orchestration/cli.test.ts","time":{"start":1783897851034,"end":1783897851052}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcddbc8196868a93a9f7e3bfde"}},"id":"prt_f58990499001LVYSjuZO7nQT3Q","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"tool_use","timestamp":1783897851055,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_DcGt644kXrrz0XwIzjeNmlv1","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/orchestration/tracer-bullet.ts"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/orchestration/tracer-bullet.ts</path>\n<type>file</type>\n<content>\n1: import { createHash, randomUUID } from \"node:crypto\";\n2: import { lstat, readFile, realpath } from \"node:fs/promises\";\n3: import path from \"node:path\";\n4: \n5: import { z } from \"zod\";\n6: \n7: import type {\n8:   ValidationReport,\n9:   ValidationRunner,\n10: } from \"../capabilities/validation-runner.js\";\n11: import { isVerifiedValidationReport } from \"../capabilities/validation-runner.js\";\n12: import type { TerminalOutcome } from \"../contracts/task.js\";\n13: import { resolveBundledFixture } from \"../fixtures/bundled-fixtures.js\";\n14: import {\n15:   IntegrationExecutionError,\n16:   IntegrationUncertainError,\n17:   isVerifiedIntegrationReceipt,\n18:   type IntegrationQueue,\n19:   type IntegrationReceipt,\n20: } from \"../integration/integration-queue.js\";\n21: import type { ProjectRegistry } from \"../projects/project-registry.js\";\n22: import {\n23:   isVerifiedReviewDecision,\n24:   type ReviewGate,\n25: } from \"../reviews/review-gate.js\";\n26: import {\n27:   canonicalValidationDigest,\n28:   ReviewerExecutionError,\n29:   type ReviewDecision,\n30:   type ReviewerAdapter,\n31:   type ReviewInput,\n32: } from \"../reviews/reviewer-adapter.js\";\n33: import type { TaskView } from \"../tasks/task-projection.js\";\n34: import type { TaskService } from \"../tasks/task-service.js\";\n35: import type {\n36:   WorkerAdapter,\n37:   WorkerRequest,\n38:   WorkerResult,\n39: } from \"../workers/worker-adapter.js\";\n40: import {\n41:   assertNoGitObjectSubstitution,\n42:   GitClient,\n43:   type GitRunOptions,\n44: } from \"../workspaces/git-client.js\";\n45: import type {\n46:   WorkspaceLease,\n47:   WorktreeManager,\n48: } from \"../workspaces/worktree-manager.js\";\n49: import {\n50:   WorkspaceCommitUncertainError,\n51:   WorkspaceGitTerminationError,\n52: } from \"../workspaces/worktree-manager.js\";\n53: \n54: const GIT_OPERATION_TIMEOUT_MS = 30_000;\n55: const MAX_WORKER_TIMEOUT_MS = 300_000;\n56: const ArtifactReadySchema = z.strictObject({\n57:   type: z.literal(\"artifact.ready\"),\n58:   path: z.string().min(1),\n59:   sha256: z.string().regex(/^[a-f0-9]{64}$/),\n60: });\n61: \n62: type PatchArtifact = z.infer<typeof ArtifactReadySchema>;\n63: interface DeterministicWorkerInput {\n64:   readonly fixture: string;\n65:   readonly file: string;\n66:   readonly content: string;\n67: }\n68: type Stage =\n69:   | \"setup\"\n70:   | \"worker\"\n71:   | \"artifact\"\n72:   | \"validation\"\n73:   | \"review\"\n74:   | \"commit\"\n75:   | \"integration\"\n76:   | \"cleanup\"\n77:   | \"completion\";\n78: \n79: export class TracerBulletOrchestrator {\n80:   private readonly git = new GitClient();\n81: \n82:   constructor(\n83:     private readonly tasks: TaskService,\n84:     private readonly projects: ProjectRegistry,\n85:     private readonly worktrees: WorktreeManager,\n86:     private readonly worker: WorkerAdapter,\n87:     private readonly validations: ValidationRunner,\n88:     private readonly reviewer: ReviewerAdapter,\n89:     private readonly reviews: ReviewGate,\n90:     private readonly integrations: IntegrationQueue,\n91:   ) {}\n92: \n93:   async run(input: {\n94:     taskId: string;\n95:     projectId: string;\n96:     title: string;\n97:     workerId: string;\n98:     reviewerId: string;\n99:     workerRequest: Omit<WorkerRequest, \"taskId\" | \"cwd\">;\n100:     signal: AbortSignal;\n101:   }): Promise<TaskView> {\n102:     const workerTimeoutMs = input.workerRequest.timeoutMs;\n103:     if (\n104:       !Number.isFinite(workerTimeoutMs) ||\n105:       !Number.isInteger(workerTimeoutMs) ||\n106:       workerTimeoutMs <= 0 ||\n107:       workerTimeoutMs > MAX_WORKER_TIMEOUT_MS\n108:     ) {\n109:       throw new Error(\n110:         `worker timeout must be a finite positive integer at most ${MAX_WORKER_TIMEOUT_MS}ms`,\n111:       );\n112:     }\n113:     this.tasks.create({\n114:       taskId: input.taskId,\n115:       projectId: input.projectId,\n116:       title: input.title,\n117:       correlationId: input.taskId,\n118:     });\n119: \n120:     let lease: WorkspaceLease | null = null;\n121:     let stage: Stage = \"setup\";\n122:     try {\n123:       if (input.workerId === input.reviewerId) {\n124:         return this.terminate(\n125:           input.taskId,\n126:           \"denied\",\n127:           stage,\n128:           \"reviewer identity must differ from worker identity\",\n129:         );\n130:       }\n131:       if (\n132:         input.workerRequest.args.length === 0 ||\n133:         input.workerRequest.args.includes(\"--workspace\")\n134:       ) {\n135:         throw new Error(\n136:           \"worker args must contain a fixture entry point and omit --workspace\",\n137:         );\n138:       }\n139:       const workerInput = await validateWorkerAuthority(input.workerRequest);\n140:       const gitOptions: GitRunOptions = {\n141:         signal: input.signal,\n142:         timeoutMs: workerTimeoutMs,\n143:       };\n144: \n145:       const project = this.projects.get(input.projectId);\n146:       await this.worktrees.ensureIntegrationBranch(project, gitOptions);\n147:       lease = await this.worktrees.create(project, input.taskId, gitOptions);\n148:       this.tasks.append(\n149:         input.taskId,\n150:         \"task.leased\",\n151:         { leaseOwner: input.workerId, workspace: lease.path },\n152:         null,\n153:       );\n154:       this.tasks.append(\n155:         input.taskId,\n156:         \"task.started\",\n157:         { workerId: input.workerId },\n158:         null,\n159:       );\n160: \n161:       stage = \"worker\";\n162:       await validateWorkerTarget(lease.path, workerInput.file);\n163:       const workerResult = await this.worker.execute(\n164:         workerRequest(input.taskId, lease, input.workerRequest, workerInput),\n165:         input.signal,\n166:       );\n167:       if (workerResult.outcome !== \"completed\") {\n168:         return this.terminate(\n169:           input.taskId,\n170:           workerResult.outcome,\n171:           stage,\n172:           workerFailure(workerResult),\n173:         );\n174:       }\n175:       if (workerResult.exitCode !== 0) {\n176:         throw new Error(\"completed worker result must have exit code 0\");\n177:       }\n178: \n179:       stage = \"artifact\";\n180:       const patch = parseArtifact(workerResult);\n181:       const inspected = await this.worktrees.inspect(lease, gitOptions);\n182:       if (!inspected.dirty || inspected.diff === \"\") {\n183:         throw new Error(\"worker completed without a nonempty diff\");\n184:       }\n185:       await validateArtifact(lease, patch, this.git, gitOptions);\n186:       const diffSha256 = sha256(inspected.diff);\n187: \n188:       this.tasks.append(\n189:         input.taskId,\n190:         \"task.validation_started\",\n191:         { patch, diffSha256 },\n192:         null,\n193:       );\n194:       stage = \"validation\";\n195:       const validationInvocationId = randomUUID();\n196:       const canonicalLeasePath = await realpath(lease.path);\n197:       const validation = await this.validations.run(\n198:         project,\n199:         \"focused\",\n200:         lease.path,\n201:         input.signal,\n202:         { invocationId: validationInvocationId, subjectSha256: diffSha256 },\n203:       );\n204:       const validationOutcome = failedValidationOutcome(validation);\n205:       if (validationOutcome !== null) {\n206:         return this.terminate(\n207:           input.taskId,\n208:           validationOutcome,\n209:           stage,\n210:           \"focused validation did not complete successfully\",\n211:           { validation },\n212:         );\n213:       }\n214:       if (!isVerifiedValidationReport(validation, {\n215:         invocationId: validationInvocationId,\n216:         canonicalCwd: canonicalLeasePath,\n217:         subjectSha256: diffSha256,\n218:       })) {\n219:         return this.terminate(\n220:           input.taskId,\n221:           \"failed\",\n222:           stage,\n223:           \"focused validation report provenance mismatch\",\n224:           { validation },\n225:         );\n226:       }\n227:       if (\n228:         JSON.stringify(validation.command) !==\n229:         JSON.stringify(project.validations.focused)\n230:       ) {\n231:         return this.terminate(\n232:           input.taskId,\n233:           \"failed\",\n234:           stage,\n235:           \"focused validation command does not match project configuration\",\n236:           { validation },\n237:         );\n238:       }\n239: \n240:       this.tasks.append(\n241:         input.taskId,\n242:         \"task.review_requested\",\n243:         { reviewerId: input.reviewerId, validation },\n244:         null,\n245:       );\n246:       stage = \"review\";\n247:       const reviewInput: ReviewInput = {\n248:         workerId: input.workerId,\n249:         reviewerId: input.reviewerId,\n250:         diff: inspected.diff,\n251:         validation,\n252:       };\n253:       const decision = await this.reviewer.review(reviewInput, input.signal);\n254:       const verifiedEvidence = this.reviews.verifyEvidence(reviewInput, decision);\n255:       if (!verifiedEvidence.approved) {\n256:         return this.terminate(\n257:           input.taskId,\n258:           \"denied\",\n259:           stage,\n260:           verifiedEvidence.reason,\n261:           { review: verifiedEvidence },\n262:         );\n263:       }\n264:       const verifiedReview = this.reviews.verify(reviewInput, verifiedEvidence);\n265:       if (verifiedReview.diffSha256 !== diffSha256) {\n266:         throw new Error(\"verified review digest differs from inspected diff digest\");\n267:       }\n268:       this.tasks.append(\n269:         input.taskId,\n270:         \"task.review_approved\",\n271:         { review: verifiedReview },\n272:         null,\n273:       );\n274: \n275:       stage = \"commit\";\n276:       const sourceCommit = await this.worktrees.commit(\n277:         lease,\n278:         [patch.path],\n279:         input.title,\n280:         verifiedReview.diffSha256,\n281:         { signal: input.signal, timeoutMs: input.workerRequest.timeoutMs },\n282:       );\n283:       const postCommitOutcome = signalOutcome(input.signal);\n284:       if (postCommitOutcome !== null) {\n285:         return this.terminate(\n286:           input.taskId,\n287:           postCommitOutcome,\n288:           stage,\n289:           \"task signal ended after commit acknowledgement\",\n290:           { sourceCommit },\n291:         );\n292:       }\n293:       this.tasks.append(\n294:         input.taskId,\n295:         \"task.integration_started\",\n296:         { sourceCommit, review: verifiedReview },\n297:         null,\n298:       );\n299: \n300:       stage = \"integration\";\n301:       let receipt: IntegrationReceipt;\n302:       try {\n303:         receipt = await this.integrations.integrate({\n304:           project,\n305:           lease,\n306:           review: verifiedReview,\n307:           signal: input.signal,\n308:           onPrepared: (preparedReceipt) => {\n309:             this.tasks.append(\n310:               input.taskId,\n311:               \"task.integration_prepared\",\n312:               { receipt: preparedReceipt },\n313:               null,\n314:             );\n315:           },\n316:         });\n317:       } catch (error) {\n318:         if (error instanceof IntegrationUncertainError) {\n319:           return this.observeIntegration(input.taskId, {\n320:             reason: error.message,\n321:             evidence: error.evidence,\n322:           });\n323:         }\n324:         if (error instanceof IntegrationExecutionError) throw error;\n325:         return this.observeIntegration(input.taskId, {\n326:           error: { name: errorName(error), message: errorMessage(error) },\n327:         });\n328:       }\n329:       if (receipt.outcome !== \"completed\") {\n330:         return this.terminate(\n331:           input.taskId,\n332:           receipt.outcome,\n333:           stage,\n334:           \"integration did not complete successfully\",\n335:           {\n336:             receipt,\n337:             candidateCleanupFailures: this.integrationCleanupFailures(input.taskId),\n338:           },\n339:         );\n340:       }\n341:       try {\n342:         await validateIntegrationReceipt({\n343:           project,\n344:           taskId: input.taskId,\n345:           sourceCommit,\n346:           review: verifiedReview,\n347:           receipt,\n348:           git: this.git,\n349:         });\n350:       } catch (error) {\n351:         return this.observeIntegration(input.taskId, {\n352:           receipt,\n353:           verification: \"failed\",\n354:           reason: errorMessage(error),\n355:         });\n356:       }\n357:       this.tasks.append(\n358:         input.taskId,\n359:         \"task.integration_observed\",\n360:         {\n361:           receipt,\n362:           verification: \"verified\",\n363:           cleanupFailures: this.integrationCleanupFailures(input.taskId),\n364:         },\n365:         null,\n366:       );\n367:       stage = \"cleanup\";\n368:       this.tasks.append(\n369:         input.taskId,\n370:         \"task.cleanup_started\",\n371:         {\n372:           sourceCommit,\n373:           resultCommit: receipt.resultCommit,\n374:           workspace: lease.path,\n375:           branch: lease.branch,\n376:         },\n377:         null,\n378:       );\n379:       try {\n380:         await this.worktrees.cleanupCompleted(project, lease, sourceCommit, {\n381:           timeoutMs: GIT_OPERATION_TIMEOUT_MS,\n382:         });\n383:       } catch (error) {\n384:         const cleanupError = error as {\n385:           readonly phase?: unknown;\n386:           readonly uncertain?: unknown;\n387:           readonly evidence?: unknown;\n388:         };\n389:         return this.tasks.append(\n390:           input.taskId,\n391:           \"task.cleanup_observed\",\n392:           {\n393:             phase: typeof cleanupError.phase === \"string\" ? cleanupError.phase : \"unknown\",\n394:             uncertain: cleanupError.uncertain === true,\n395:             evidence: cleanupError.evidence ?? {},\n396:             reason: errorMessage(error),\n397:           },\n398:           null,\n399:         );\n400:       }\n401:       this.tasks.append(\n402:         input.taskId,\n403:         \"task.cleanup_completed\",\n404:         {\n405:           sourceCommit,\n406:           resultCommit: receipt.resultCommit,\n407:           workspace: lease.path,\n408:           branch: lease.branch,\n409:         },\n410:         null,\n411:       );\n412:       stage = \"completion\";\n413:       return this.tasks.append(\n414:         input.taskId,\n415:         \"task.completed\",\n416:         { receipt },\n417:         null,\n418:       );\n419:     } catch (error) {\n420:       if (stage === \"completion\") throw error;\n421:       if (error instanceof WorkspaceCommitUncertainError) {\n422:         return this.tasks.append(\n423:           input.taskId,\n424:           \"task.commit_observed\",\n425:           { stage: \"commit\", reason: error.message },\n426:           null,\n427:         );\n428:       }\n429:       const dependencyOutcome =\n430:         error instanceof ReviewerExecutionError ||\n431:         error instanceof WorkspaceGitTerminationError ||\n432:         error instanceof IntegrationExecutionError\n433:           ? error.outcome\n434:           : null;\n435:       if (stage === \"integration\" && dependencyOutcome === null) {\n436:         return this.observeIntegration(input.taskId, {\n437:           error: { name: errorName(error), message: errorMessage(error) },\n438:         });\n439:       }\n440:       return this.terminate(\n441:         input.taskId,\n442:         dependencyOutcome ?? signalOutcome(input.signal) ?? \"failed\",\n443:         stage,\n444:         errorMessage(error),\n445:       );\n446:     }\n447:   }\n448: \n449:   private terminate(\n450:     taskId: string,\n451:     outcome: TerminalOutcome,\n452:     stage: Stage,\n453:     reason: string,\n454:     evidence: Record<string, unknown> = {},\n455:   ): TaskView {\n456:     const current = this.tasks.get(taskId);\n457:     if (current === null) throw new Error(`task ${taskId} not found`);\n458:     if (current.lifecycle === \"terminal\") return current;\n459:     return this.tasks.append(\n460:       taskId,\n461:       `task.${outcome}`,\n462:       { stage, reason, ...evidence },\n463:       null,\n464:     );\n465:   }\n466: \n467:   private observeIntegration(taskId: string, payload: unknown): TaskView {\n468:     const durablePayload = typeof payload === \"object\" && payload !== null\n469:       ? { ...payload, cleanupFailures: this.integrationCleanupFailures(taskId) }\n470:       : { evidence: payload, cleanupFailures: this.integrationCleanupFailures(taskId) };\n471:     return this.tasks.append(\n472:       taskId,\n473:       \"task.integration_observed\",\n474:       durablePayload,\n475:       null,\n476:     );\n477:   }\n478: \n479:   private integrationCleanupFailures(taskId: string): readonly unknown[] {\n480:     return this.integrations.getCleanupFailures().filter((failure) => failure.taskId === taskId);\n481:   }\n482: \n483:   private current(taskId: string): TaskView {\n484:     const current = this.tasks.get(taskId);\n485:     if (current === null) throw new Error(`task ${taskId} not found`);\n486:     return current;\n487:   }\n488: }\n489: \n490: async function validateWorkerAuthority(\n491:   request: Omit<WorkerRequest, \"taskId\" | \"cwd\">,\n492: ): Promise<DeterministicWorkerInput> {\n493:   const executable = await realpath(request.executable);\n494:   const nodeExecutable = await realpath(process.execPath);\n495:   if (executable !== nodeExecutable) {\n496:     throw new Error(\"tracer worker executable must be the current Node.js executable\");\n497:   }\n498: \n499:   const parsedInput = parseWorkerInput(request.args);\n500:   const fixture = parsedInput.fixture;\n501:   const fixturePath = await realpath(fixture);\n502:   const bundledFixture = resolveBundledFixture(\"deterministic-worker.mjs\");\n503:   if (fixturePath !== bundledFixture) {\n504:     throw new Error(\"tracer worker script must be Zentra's bundled deterministic fixture\");\n505:   }\n506:   return parsedInput;\n507: }\n508: \n509: function parseWorkerInput(args: readonly string[]): DeterministicWorkerInput {\n510:   const fixture = args[0];\n511:   if (fixture === undefined) throw new Error(\"worker fixture entry point is required\");\n512:   const values = new Map<string, string>();\n513:   const allowed = new Set([\"--file\", \"--content\"]);\n514:   for (let index = 1; index < args.length; index += 2) {\n515:     const flag = args[index];\n516:     const value = args[index + 1];\n517:     if (flag === undefined || !allowed.has(flag)) {\n518:       throw new Error(`worker args contain an unknown flag: ${String(flag)}`);\n519:     }\n520:     if (values.has(flag)) throw new Error(`worker args duplicate ${flag}`);\n521:     if (value === undefined) throw new Error(`worker args missing value for ${flag}`);\n522:     values.set(flag, value);\n523:   }\n524:   const file = values.get(\"--file\");\n525:   const content = values.get(\"--content\");\n526:   if (file === undefined || content === undefined || values.size !== 2) {\n527:     throw new Error(\"worker args require exactly --file and --content\");\n528:   }\n529:   // MVP fixture scope is deliberately one root-level file, currently greeting.txt.\n530:   if (file.includes(\"/\") || file.includes(\"\\\\\")) {\n531:     throw new Error(\"worker --file must be one root-level filename without slashes\");\n532:   }\n533:   validateRelativePath(file);\n534:   return { fixture, file, content };\n535: }\n536: \n537: async function validateWorkerTarget(workspace: string, file: string): Promise<void> {\n538:   const canonicalWorkspace = await realpath(workspace);\n539:   const segments = file.split(\"/\");\n540:   let current = canonicalWorkspace;\n541:   for (const segment of segments.slice(0, -1)) {\n542:     const candidate = path.join(current, segment);\n543:     try {\n544:       const stat = await lstat(candidate);\n545:       if (stat.isSymbolicLink() || !stat.isDirectory()) {\n546:         throw new Error(`worker target parent is not a real directory: ${segment}`);\n547:       }\n548:       current = await realpath(candidate);\n549:       if (!current.startsWith(`${canonicalWorkspace}${path.sep}`)) {\n550:         throw new Error(\"worker target parent escapes workspace\");\n551:       }\n552:     } catch (error) {\n553:       if ((error as NodeJS.ErrnoException).code === \"ENOENT\") break;\n554:       throw error;\n555:     }\n556:   }\n557:   const target = path.resolve(canonicalWorkspace, file);\n558:   try {\n559:     const stat = await lstat(target);\n560:     if (stat.isSymbolicLink()) throw new Error(\"worker target must not be a symbolic link\");\n561:     const canonicalTarget = await realpath(target);\n562:     if (!canonicalTarget.startsWith(`${canonicalWorkspace}${path.sep}`)) {\n563:       throw new Error(\"worker target escapes workspace\");\n564:     }\n565:   } catch (error) {\n566:     if ((error as NodeJS.ErrnoException).code !== \"ENOENT\") throw error;\n567:   }\n568: }\n569: \n570: // The deterministic fixture script is argv[0]. The orchestrator owns workspace\n571: // selection and injects it before the caller's fixture-specific arguments.\n572: function workerRequest(\n573:   taskId: string,\n574:   lease: WorkspaceLease,\n575:   request: Omit<WorkerRequest, \"taskId\" | \"cwd\">,\n576:   input: DeterministicWorkerInput,\n577: ): WorkerRequest {\n578:   return {\n579:     taskId,\n580:     executable: request.executable,\n581:     args: [\n582:       input.fixture,\n583:       \"--workspace\",\n584:       lease.path,\n585:       \"--file\",\n586:       input.file,\n587:       \"--content\",\n588:       input.content,\n589:     ],\n590:     cwd: lease.path,\n591:     timeoutMs: request.timeoutMs,\n592:   };\n593: }\n594: \n595: function parseArtifact(\n596:   result: WorkerResult,\n597: ): PatchArtifact {\n598:   if (result.events.length !== 1) {\n599:     throw new Error(\n600:       `worker protocol requires exactly one artifact.ready event, received ${result.events.length}`,\n601:     );\n602:   }\n603:   const parsed = ArtifactReadySchema.safeParse(result.events[0]);\n604:   if (!parsed.success) {\n605:     throw new Error(`worker protocol returned an invalid artifact.ready event: ${parsed.error.message}`);\n606:   }\n607:   const artifact = parsed.data;\n608:   validateRelativePath(artifact.path);\n609:   return artifact;\n610: }\n611: \n612: async function validateArtifact(\n613:   lease: WorkspaceLease,\n614:   artifact: PatchArtifact,\n615:   git: GitClient,\n616:   options: GitRunOptions,\n617: ): Promise<void> {\n618:   const workspacePath = await realpath(lease.path);\n619:   const artifactPath = path.resolve(workspacePath, artifact.path);\n620:   const canonicalArtifactPath = await realpath(artifactPath);\n621:   if (!canonicalArtifactPath.startsWith(`${workspacePath}${path.sep}`)) {\n622:     throw new Error(`artifact path escapes workspace: ${artifact.path}`);\n623:   }\n624:   const stat = await lstat(canonicalArtifactPath);\n625:   if (!stat.isFile()) throw new Error(`artifact path is not a regular file: ${artifact.path}`);\n626:   const actualSha256 = createHash(\"sha256\")\n627:     .update(await readFile(canonicalArtifactPath))\n628:     .digest(\"hex\");\n629:   if (actualSha256 !== artifact.sha256) {\n630:     throw new Error(\n631:       `artifact digest mismatch: event ${artifact.sha256}, file ${actualSha256}`,\n632:     );\n633:   }\n634: \n635:   const changed = await git.run(lease.path, [\n636:     \"-c\",\n637:     \"core.hooksPath=/dev/null\",\n638:     \"-c\",\n639:     \"core.fsmonitor=false\",\n640:     \"-c\",\n641:     \"core.quotepath=off\",\n642:     \"diff\",\n643:     \"--name-only\",\n644:     \"--no-ext-diff\",\n645:     \"--no-textconv\",\n646:   ], options);\n647:   if (changed.termination !== null) {\n648:     throw new WorkspaceGitTerminationError(\n649:       changed.termination,\n650:       \"artifact changed-path read\",\n651:     );\n652:   }\n653:   if (changed.exitCode !== 0 || changed.truncated) {\n654:     throw new Error(\"could not obtain the complete worker diff path list\");\n655:   }\n656:   const changedPaths = changed.stdout.split(\"\\n\").filter(Boolean);\n657:   if (changedPaths.length !== 1 || changedPaths[0] !== artifact.path) {\n658:     throw new Error(\"artifact.ready path does not exactly match the worker diff\");\n659:   }\n660: }\n661: \n662: function validateRelativePath(candidate: string): void {\n663:   if (\n664:     candidate.includes(\"\\0\") ||\n665:     candidate.includes(\"\\n\") ||\n666:     candidate.includes(\"\\r\") ||\n667:     path.isAbsolute(candidate) ||\n668:     path.win32.isAbsolute(candidate)\n669:   ) {\n670:     throw new Error(`artifact path must be a safe relative path: ${candidate}`);\n671:   }\n672:   const segments = candidate.split(/[\\\\/]/);\n673:   if (\n674:     segments.some((segment) => segment === \"\" || segment === \".\" || segment === \"..\") ||\n675:     path.posix.normalize(candidate) !== candidate\n676:   ) {\n677:     throw new Error(`artifact path must not contain traversal: ${candidate}`);\n678:   }\n679: }\n680: \n681: function failedValidationOutcome(\n682:   validation: ValidationReport,\n683: ): Exclude<TerminalOutcome, \"completed\" | \"denied\"> | null {\n684:   if (validation.outcome !== \"completed\") return validation.outcome;\n685:   return validation.exitCode === 0 ? null : \"failed\";\n686: }\n687: \n688: async function validateIntegrationReceipt(input: {\n689:   readonly project: {\n690:     readonly projectId: string;\n691:     readonly repositoryPath: string;\n692:     readonly integrationBranch: string;\n693:     readonly validations: { readonly full: readonly string[] };\n694:   };\n695:   readonly taskId: string;\n696:   readonly sourceCommit: string;\n697:   readonly review: ReviewDecision;\n698:   readonly receipt: IntegrationReceipt;\n699:   readonly git: GitClient;\n700: }): Promise<void> {\n701:   const { project, taskId, sourceCommit, review, receipt, git } = input;\n702:   await assertNoGitObjectSubstitution(git, project.repositoryPath, GIT_OPERATION_TIMEOUT_MS);\n703:   if (!isVerifiedIntegrationReceipt(receipt)) {\n704:     throw new Error(\"completed integration receipt lacks queue provenance\");\n705:   }\n706:   if (\n707:     receipt.outcome !== \"completed\" ||\n708:     receipt.taskId !== taskId ||\n709:     receipt.projectId !== project.projectId ||\n710:     receipt.sourceCommit !== sourceCommit\n711:   ) {\n712:     throw new Error(\"completed integration receipt identity does not match the task\");\n713:   }\n714:   if (\n715:     receipt.review !== review ||\n716:     !isVerifiedReviewDecision(receipt.review)\n717:   ) {\n718:     throw new Error(\"completed integration receipt does not retain verified review provenance\");\n719:   }\n720:   if (\n721:     receipt.originalIntegrationCommit === null ||\n722:     !/^(?:[0-9a-f]{40}|[0-9a-f]{64})$/.test(receipt.originalIntegrationCommit) ||\n723:     receipt.resultCommit === null ||\n724:     !/^(?:[0-9a-f]{40}|[0-9a-f]{64})$/.test(receipt.resultCommit)\n725:   ) {\n726:     throw new Error(\"completed integration receipt has no valid integration base or result commit\");\n727:   }\n728:   if (\n729:     receipt.validation.name !== \"full\" ||\n730:     receipt.validation.outcome !== \"completed\" ||\n731:     receipt.validation.exitCode !== 0 ||\n732:     JSON.stringify(receipt.validation.command) !==\n733:       JSON.stringify(project.validations.full)\n734:   ) {\n735:     throw new Error(\"completed integration receipt has invalid full validation evidence\");\n736:   }\n737:   canonicalValidationDigest(receipt.validation);\n738: \n739:   const integrationRef = `refs/heads/${project.integrationBranch}`;\n740:   const symbolic = await git.run(\n741:     project.repositoryPath,\n742:     [\"--no-replace-objects\", \"symbolic-ref\", \"--quiet\", integrationRef],\n743:     { timeoutMs: GIT_OPERATION_TIMEOUT_MS },\n744:   );\n745:   if (\n746:     symbolic.termination !== null ||\n747:     symbolic.truncated ||\n748:     symbolic.exitCode !== 1\n749:   ) {\n750:     throw new Error(\"integration ref must be an exact nonsymbolic ref\");\n751:   }\n752:   const head = await git.run(project.repositoryPath, [\n753:     \"--no-replace-objects\",\n754:     \"rev-parse\",\n755:     \"--verify\",\n756:     `${integrationRef}^{commit}`,\n757:   ], { timeoutMs: GIT_OPERATION_TIMEOUT_MS });\n758:   if (\n759:     head.termination !== null ||\n760:     head.exitCode !== 0 ||\n761:     head.truncated ||\n762:     head.stdout.trim() !== receipt.resultCommit\n763:   ) {\n764:     throw new Error(\"integration ref does not match the completed receipt\");\n765:   }\n766:   const parents = await git.run(\n767:     project.repositoryPath,\n768:     [\"--no-replace-objects\", \"rev-list\", \"--parents\", \"--max-count=1\", receipt.resultCommit],\n769:     { timeoutMs: GIT_OPERATION_TIMEOUT_MS },\n770:   );\n771:   if (\n772:     parents.termination !== null ||\n773:     parents.truncated ||\n774:     parents.exitCode !== 0\n775:   ) {\n776:     throw new Error(\"receipt result merge parents could not be inspected\");\n777:   }\n778:   const resultShape = parents.stdout.trim().split(/\\s+/);\n779:   if (\n780:     resultShape.length !== 3 ||\n781:     resultShape[0] !== receipt.resultCommit ||\n782:     resultShape[1] !== receipt.originalIntegrationCommit ||\n783:     resultShape[2] !== sourceCommit\n784:   ) {\n785:     throw new Error(\"receipt result does not have the exact integration-base/source merge shape\");\n786:   }\n787: }\n788: \n789: function signalOutcome(\n790:   signal: AbortSignal,\n791: ): \"cancelled\" | \"timed_out\" | null {\n792:   if (!signal.aborted) return null;\n793:   return signal.reason instanceof DOMException && signal.reason.name === \"TimeoutError\"\n794:     ? \"timed_out\"\n795:     : \"cancelled\";\n796: }\n797: \n798: function workerFailure(result: WorkerResult): string {\n799:   const stderr = result.stderr.trim();\n800:   return `worker outcome was ${result.outcome}${stderr === \"\" ? \"\" : `: ${stderr}`}`;\n801: }\n802: \n803: function sha256(value: string): string {\n804:   return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n805: }\n806: \n807: function errorMessage(error: unknown): string {\n808:   return error instanceof Error ? error.message : String(error);\n809: }\n810: \n811: function errorName(error: unknown): string {\n812:   return error instanceof Error ? error.name : \"UnknownError\";\n813: }\n\n(End of file - total 813 lines)\n</content>","metadata":{"preview":"import { createHash, randomUUID } from \"node:crypto\";\nimport { lstat, readFile, realpath } from \"node:fs/promises\";\nimport path from \"node:path\";\n\nimport { z } from \"zod\";\n\nimport type {\n  ValidationReport,\n  ValidationRunner,\n} from \"../capabilities/validation-runner.js\";\nimport { isVerifiedValidationReport } from \"../capabilities/validation-runner.js\";\nimport type { TerminalOutcome } from \"../contracts/task.js\";\nimport { resolveBundledFixture } from \"../fixtures/bundled-fixtures.js\";\nimport {\n  IntegrationExecutionError,\n  IntegrationUncertainError,\n  isVerifiedIntegrationReceipt,\n  type IntegrationQueue,\n  type IntegrationReceipt,\n} from \"../integration/integration-queue.js\";","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/orchestration/tracer-bullet.ts","text":"import { createHash, randomUUID } from \"node:crypto\";\nimport { lstat, readFile, realpath } from \"node:fs/promises\";\nimport path from \"node:path\";\n\nimport { z } from \"zod\";\n\nimport type {\n  ValidationReport,\n  ValidationRunner,\n} from \"../capabilities/validation-runner.js\";\nimport { isVerifiedValidationReport } from \"../capabilities/validation-runner.js\";\nimport type { TerminalOutcome } from \"../contracts/task.js\";\nimport { resolveBundledFixture } from \"../fixtures/bundled-fixtures.js\";\nimport {\n  IntegrationExecutionError,\n  IntegrationUncertainError,\n  isVerifiedIntegrationReceipt,\n  type IntegrationQueue,\n  type IntegrationReceipt,\n} from \"../integration/integration-queue.js\";\nimport type { ProjectRegistry } from \"../projects/project-registry.js\";\nimport {\n  isVerifiedReviewDecision,\n  type ReviewGate,\n} from \"../reviews/review-gate.js\";\nimport {\n  canonicalValidationDigest,\n  ReviewerExecutionError,\n  type ReviewDecision,\n  type ReviewerAdapter,\n  type ReviewInput,\n} from \"../reviews/reviewer-adapter.js\";\nimport type { TaskView } from \"../tasks/task-projection.js\";\nimport type { TaskService } from \"../tasks/task-service.js\";\nimport type {\n  WorkerAdapter,\n  WorkerRequest,\n  WorkerResult,\n} from \"../workers/worker-adapter.js\";\nimport {\n  assertNoGitObjectSubstitution,\n  GitClient,\n  type GitRunOptions,\n} from \"../workspaces/git-client.js\";\nimport type {\n  WorkspaceLease,\n  WorktreeManager,\n} from \"../workspaces/worktree-manager.js\";\nimport {\n  WorkspaceCommitUncertainError,\n  WorkspaceGitTerminationError,\n} from \"../workspaces/worktree-manager.js\";\n\nconst GIT_OPERATION_TIMEOUT_MS = 30_000;\nconst MAX_WORKER_TIMEOUT_MS = 300_000;\nconst ArtifactReadySchema = z.strictObject({\n  type: z.literal(\"artifact.ready\"),\n  path: z.string().min(1),\n  sha256: z.string().regex(/^[a-f0-9]{64}$/),\n});\n\ntype PatchArtifact = z.infer<typeof ArtifactReadySchema>;\ninterface DeterministicWorkerInput {\n  readonly fixture: string;\n  readonly file: string;\n  readonly content: string;\n}\ntype Stage =\n  | \"setup\"\n  | \"worker\"\n  | \"artifact\"\n  | \"validation\"\n  | \"review\"\n  | \"commit\"\n  | \"integration\"\n  | \"cleanup\"\n  | \"completion\";\n\nexport class TracerBulletOrchestrator {\n  private readonly git = new GitClient();\n\n  constructor(\n    private readonly tasks: TaskService,\n    private readonly projects: ProjectRegistry,\n    private readonly worktrees: WorktreeManager,\n    private readonly worker: WorkerAdapter,\n    private readonly validations: ValidationRunner,\n    private readonly reviewer: ReviewerAdapter,\n    private readonly reviews: ReviewGate,\n    private readonly integrations: IntegrationQueue,\n  ) {}\n\n  async run(input: {\n    taskId: string;\n    projectId: string;\n    title: string;\n    workerId: string;\n    reviewerId: string;\n    workerRequest: Omit<WorkerRequest, \"taskId\" | \"cwd\">;\n    signal: AbortSignal;\n  }): Promise<TaskView> {\n    const workerTimeoutMs = input.workerRequest.timeoutMs;\n    if (\n      !Number.isFinite(workerTimeoutMs) ||\n      !Number.isInteger(workerTimeoutMs) ||\n      workerTimeoutMs <= 0 ||\n      workerTimeoutMs > MAX_WORKER_TIMEOUT_MS\n    ) {\n      throw new Error(\n        `worker timeout must be a finite positive integer at most ${MAX_WORKER_TIMEOUT_MS}ms`,\n      );\n    }\n    this.tasks.create({\n      taskId: input.taskId,\n      projectId: input.projectId,\n      title: input.title,\n      correlationId: input.taskId,\n    });\n\n    let lease: WorkspaceLease | null = null;\n    let stage: Stage = \"setup\";\n    try {\n      if (input.workerId === input.reviewerId) {\n        return this.terminate(\n          input.taskId,\n          \"denied\",\n          stage,\n          \"reviewer identity must differ from worker identity\",\n        );\n      }\n      if (\n        input.workerRequest.args.length === 0 ||\n        input.workerRequest.args.includes(\"--workspace\")\n      ) {\n        throw new Error(\n          \"worker args must contain a fixture entry point and omit --workspace\",\n        );\n      }\n      const workerInput = await validateWorkerAuthority(input.workerRequest);\n      const gitOptions: GitRunOptions = {\n        signal: input.signal,\n        timeoutMs: workerTimeoutMs,\n      };\n\n      const project = this.projects.get(input.projectId);\n      await this.worktrees.ensureIntegrationBranch(project, gitOptions);\n      lease = await this.worktrees.create(project, input.taskId, gitOptions);\n      this.tasks.append(\n        input.taskId,\n        \"task.leased\",\n        { leaseOwner: input.workerId, workspace: lease.path },\n        null,\n      );\n      this.tasks.append(\n        input.taskId,\n        \"task.started\",\n        { workerId: input.workerId },\n        null,\n      );\n\n      stage = \"worker\";\n      await validateWorkerTarget(lease.path, workerInput.file);\n      const workerResult = await this.worker.execute(\n        workerRequest(input.taskId, lease, input.workerRequest, workerInput),\n        input.signal,\n      );\n      if (workerResult.outcome !== \"completed\") {\n        return this.terminate(\n          input.taskId,\n          workerResult.outcome,\n          stage,\n          workerFailure(workerResult),\n        );\n      }\n      if (workerResult.exitCode !== 0) {\n        throw new Error(\"completed worker result must have exit code 0\");\n      }\n\n      stage = \"artifact\";\n      const patch = parseArtifact(workerResult);\n      const inspected = await this.worktrees.inspect(lease, gitOptions);\n      if (!inspected.dirty || inspected.diff === \"\") {\n        throw new Error(\"worker completed without a nonempty diff\");\n      }\n      await validateArtifact(lease, patch, this.git, gitOptions);\n      const diffSha256 = sha256(inspected.diff);\n\n      this.tasks.append(\n        input.taskId,\n        \"task.validation_started\",\n        { patch, diffSha256 },\n        null,\n      );\n      stage = \"validation\";\n      const validationInvocationId = randomUUID();\n      const canonicalLeasePath = await realpath(lease.path);\n      const validation = await this.validations.run(\n        project,\n        \"focused\",\n        lease.path,\n        input.signal,\n        { invocationId: validationInvocationId, subjectSha256: diffSha256 },\n      );\n      const validationOutcome = failedValidationOutcome(validation);\n      if (validationOutcome !== null) {\n        return this.terminate(\n          input.taskId,\n          validationOutcome,\n          stage,\n          \"focused validation did not complete successfully\",\n          { validation },\n        );\n      }\n      if (!isVerifiedValidationReport(validation, {\n        invocationId: validationInvocationId,\n        canonicalCwd: canonicalLeasePath,\n        subjectSha256: diffSha256,\n      })) {\n        return this.terminate(\n          input.taskId,\n          \"failed\",\n          stage,\n          \"focused validation report provenance mismatch\",\n          { validation },\n        );\n      }\n      if (\n        JSON.stringify(validation.command) !==\n        JSON.stringify(project.validations.focused)\n      ) {\n        return this.terminate(\n          input.taskId,\n          \"failed\",\n          stage,\n          \"focused validation command does not match project configuration\",\n          { validation },\n        );\n      }\n\n      this.tasks.append(\n        input.taskId,\n        \"task.review_requested\",\n        { reviewerId: input.reviewerId, validation },\n        null,\n      );\n      stage = \"review\";\n      const reviewInput: ReviewInput = {\n        workerId: input.workerId,\n        reviewerId: input.reviewerId,\n        diff: inspected.diff,\n        validation,\n      };\n      const decision = await this.reviewer.review(reviewInput, input.signal);\n      const verifiedEvidence = this.reviews.verifyEvidence(reviewInput, decision);\n      if (!verifiedEvidence.approved) {\n        return this.terminate(\n          input.taskId,\n          \"denied\",\n          stage,\n          verifiedEvidence.reason,\n          { review: verifiedEvidence },\n        );\n      }\n      const verifiedReview = this.reviews.verify(reviewInput, verifiedEvidence);\n      if (verifiedReview.diffSha256 !== diffSha256) {\n        throw new Error(\"verified review digest differs from inspected diff digest\");\n      }\n      this.tasks.append(\n        input.taskId,\n        \"task.review_approved\",\n        { review: verifiedReview },\n        null,\n      );\n\n      stage = \"commit\";\n      const sourceCommit = await this.worktrees.commit(\n        lease,\n        [patch.path],\n        input.title,\n        verifiedReview.diffSha256,\n        { signal: input.signal, timeoutMs: input.workerRequest.timeoutMs },\n      );\n      const postCommitOutcome = signalOutcome(input.signal);\n      if (postCommitOutcome !== null) {\n        return this.terminate(\n          input.taskId,\n          postCommitOutcome,\n          stage,\n          \"task signal ended after commit acknowledgement\",\n          { sourceCommit },\n        );\n      }\n      this.tasks.append(\n        input.taskId,\n        \"task.integration_started\",\n        { sourceCommit, review: verifiedReview },\n        null,\n      );\n\n      stage = \"integration\";\n      let receipt: IntegrationReceipt;\n      try {\n        receipt = await this.integrations.integrate({\n          project,\n          lease,\n          review: verifiedReview,\n          signal: input.signal,\n          onPrepared: (preparedReceipt) => {\n            this.tasks.append(\n              input.taskId,\n              \"task.integration_prepared\",\n              { receipt: preparedReceipt },\n              null,\n            );\n          },\n        });\n      } catch (error) {\n        if (error instanceof IntegrationUncertainError) {\n          return this.observeIntegration(input.taskId, {\n            reason: error.message,\n            evidence: error.evidence,\n          });\n        }\n        if (error instanceof IntegrationExecutionError) throw error;\n        return this.observeIntegration(input.taskId, {\n          error: { name: errorName(error), message: errorMessage(error) },\n        });\n      }\n      if (receipt.outcome !== \"completed\") {\n        return this.terminate(\n          input.taskId,\n          receipt.outcome,\n          stage,\n          \"integration did not complete successfully\",\n          {\n            receipt,\n            candidateCleanupFailures: this.integrationCleanupFailures(input.taskId),\n          },\n        );\n      }\n      try {\n        await validateIntegrationReceipt({\n          project,\n          taskId: input.taskId,\n          sourceCommit,\n          review: verifiedReview,\n          receipt,\n          git: this.git,\n        });\n      } catch (error) {\n        return this.observeIntegration(input.taskId, {\n          receipt,\n          verification: \"failed\",\n          reason: errorMessage(error),\n        });\n      }\n      this.tasks.append(\n        input.taskId,\n        \"task.integration_observed\",\n        {\n          receipt,\n          verification: \"verified\",\n          cleanupFailures: this.integrationCleanupFailures(input.taskId),\n        },\n        null,\n      );\n      stage = \"cleanup\";\n      this.tasks.append(\n        input.taskId,\n        \"task.cleanup_started\",\n        {\n          sourceCommit,\n          resultCommit: receipt.resultCommit,\n          workspace: lease.path,\n          branch: lease.branch,\n        },\n        null,\n      );\n      try {\n        await this.worktrees.cleanupCompleted(project, lease, sourceCommit, {\n          timeoutMs: GIT_OPERATION_TIMEOUT_MS,\n        });\n      } catch (error) {\n        const cleanupError = error as {\n          readonly phase?: unknown;\n          readonly uncertain?: unknown;\n          readonly evidence?: unknown;\n        };\n        return this.tasks.append(\n          input.taskId,\n          \"task.cleanup_observed\",\n          {\n            phase: typeof cleanupError.phase === \"string\" ? cleanupError.phase : \"unknown\",\n            uncertain: cleanupError.uncertain === true,\n            evidence: cleanupError.evidence ?? {},\n            reason: errorMessage(error),\n          },\n          null,\n        );\n      }\n      this.tasks.append(\n        input.taskId,\n        \"task.cleanup_completed\",\n        {\n          sourceCommit,\n          resultCommit: receipt.resultCommit,\n          workspace: lease.path,\n          branch: lease.branch,\n        },\n        null,\n      );\n      stage = \"completion\";\n      return this.tasks.append(\n        input.taskId,\n        \"task.completed\",\n        { receipt },\n        null,\n      );\n    } catch (error) {\n      if (stage === \"completion\") throw error;\n      if (error instanceof WorkspaceCommitUncertainError) {\n        return this.tasks.append(\n          input.taskId,\n          \"task.commit_observed\",\n          { stage: \"commit\", reason: error.message },\n          null,\n        );\n      }\n      const dependencyOutcome =\n        error instanceof ReviewerExecutionError ||\n        error instanceof WorkspaceGitTerminationError ||\n        error instanceof IntegrationExecutionError\n          ? error.outcome\n          : null;\n      if (stage === \"integration\" && dependencyOutcome === null) {\n        return this.observeIntegration(input.taskId, {\n          error: { name: errorName(error), message: errorMessage(error) },\n        });\n      }\n      return this.terminate(\n        input.taskId,\n        dependencyOutcome ?? signalOutcome(input.signal) ?? \"failed\",\n        stage,\n        errorMessage(error),\n      );\n    }\n  }\n\n  private terminate(\n    taskId: string,\n    outcome: TerminalOutcome,\n    stage: Stage,\n    reason: string,\n    evidence: Record<string, unknown> = {},\n  ): TaskView {\n    const current = this.tasks.get(taskId);\n    if (current === null) throw new Error(`task ${taskId} not found`);\n    if (current.lifecycle === \"terminal\") return current;\n    return this.tasks.append(\n      taskId,\n      `task.${outcome}`,\n      { stage, reason, ...evidence },\n      null,\n    );\n  }\n\n  private observeIntegration(taskId: string, payload: unknown): TaskView {\n    const durablePayload = typeof payload === \"object\" && payload !== null\n      ? { ...payload, cleanupFailures: this.integrationCleanupFailures(taskId) }\n      : { evidence: payload, cleanupFailures: this.integrationCleanupFailures(taskId) };\n    return this.tasks.append(\n      taskId,\n      \"task.integration_observed\",\n      durablePayload,\n      null,\n    );\n  }\n\n  private integrationCleanupFailures(taskId: string): readonly unknown[] {\n    return this.integrations.getCleanupFailures().filter((failure) => failure.taskId === taskId);\n  }\n\n  private current(taskId: string): TaskView {\n    const current = this.tasks.get(taskId);\n    if (current === null) throw new Error(`task ${taskId} not found`);\n    return current;\n  }\n}\n\nasync function validateWorkerAuthority(\n  request: Omit<WorkerRequest, \"taskId\" | \"cwd\">,\n): Promise<DeterministicWorkerInput> {\n  const executable = await realpath(request.executable);\n  const nodeExecutable = await realpath(process.execPath);\n  if (executable !== nodeExecutable) {\n    throw new Error(\"tracer worker executable must be the current Node.js executable\");\n  }\n\n  const parsedInput = parseWorkerInput(request.args);\n  const fixture = parsedInput.fixture;\n  const fixturePath = await realpath(fixture);\n  const bundledFixture = resolveBundledFixture(\"deterministic-worker.mjs\");\n  if (fixturePath !== bundledFixture) {\n    throw new Error(\"tracer worker script must be Zentra's bundled deterministic fixture\");\n  }\n  return parsedInput;\n}\n\nfunction parseWorkerInput(args: readonly string[]): DeterministicWorkerInput {\n  const fixture = args[0];\n  if (fixture === undefined) throw new Error(\"worker fixture entry point is required\");\n  const values = new Map<string, string>();\n  const allowed = new Set([\"--file\", \"--content\"]);\n  for (let index = 1; index < args.length; index += 2) {\n    const flag = args[index];\n    const value = args[index + 1];\n    if (flag === undefined || !allowed.has(flag)) {\n      throw new Error(`worker args contain an unknown flag: ${String(flag)}`);\n    }\n    if (values.has(flag)) throw new Error(`worker args duplicate ${flag}`);\n    if (value === undefined) throw new Error(`worker args missing value for ${flag}`);\n    values.set(flag, value);\n  }\n  const file = values.get(\"--file\");\n  const content = values.get(\"--content\");\n  if (file === undefined || content === undefined || values.size !== 2) {\n    throw new Error(\"worker args require exactly --file and --content\");\n  }\n  // MVP fixture scope is deliberately one root-level file, currently greeting.txt.\n  if (file.includes(\"/\") || file.includes(\"\\\\\")) {\n    throw new Error(\"worker --file must be one root-level filename without slashes\");\n  }\n  validateRelativePath(file);\n  return { fixture, file, content };\n}\n\nasync function validateWorkerTarget(workspace: string, file: string): Promise<void> {\n  const canonicalWorkspace = await realpath(workspace);\n  const segments = file.split(\"/\");\n  let current = canonicalWorkspace;\n  for (const segment of segments.slice(0, -1)) {\n    const candidate = path.join(current, segment);\n    try {\n      const stat = await lstat(candidate);\n      if (stat.isSymbolicLink() || !stat.isDirectory()) {\n        throw new Error(`worker target parent is not a real directory: ${segment}`);\n      }\n      current = await realpath(candidate);\n      if (!current.startsWith(`${canonicalWorkspace}${path.sep}`)) {\n        throw new Error(\"worker target parent escapes workspace\");\n      }\n    } catch (error) {\n      if ((error as NodeJS.ErrnoException).code === \"ENOENT\") break;\n      throw error;\n    }\n  }\n  const target = path.resolve(canonicalWorkspace, file);\n  try {\n    const stat = await lstat(target);\n    if (stat.isSymbolicLink()) throw new Error(\"worker target must not be a symbolic link\");\n    const canonicalTarget = await realpath(target);\n    if (!canonicalTarget.startsWith(`${canonicalWorkspace}${path.sep}`)) {\n      throw new Error(\"worker target escapes workspace\");\n    }\n  } catch (error) {\n    if ((error as NodeJS.ErrnoException).code !== \"ENOENT\") throw error;\n  }\n}\n\n// The deterministic fixture script is argv[0]. The orchestrator owns workspace\n// selection and injects it before the caller's fixture-specific arguments.\nfunction workerRequest(\n  taskId: string,\n  lease: WorkspaceLease,\n  request: Omit<WorkerRequest, \"taskId\" | \"cwd\">,\n  input: DeterministicWorkerInput,\n): WorkerRequest {\n  return {\n    taskId,\n    executable: request.executable,\n    args: [\n      input.fixture,\n      \"--workspace\",\n      lease.path,\n      \"--file\",\n      input.file,\n      \"--content\",\n      input.content,\n    ],\n    cwd: lease.path,\n    timeoutMs: request.timeoutMs,\n  };\n}\n\nfunction parseArtifact(\n  result: WorkerResult,\n): PatchArtifact {\n  if (result.events.length !== 1) {\n    throw new Error(\n      `worker protocol requires exactly one artifact.ready event, received ${result.events.length}`,\n    );\n  }\n  const parsed = ArtifactReadySchema.safeParse(result.events[0]);\n  if (!parsed.success) {\n    throw new Error(`worker protocol returned an invalid artifact.ready event: ${parsed.error.message}`);\n  }\n  const artifact = parsed.data;\n  validateRelativePath(artifact.path);\n  return artifact;\n}\n\nasync function validateArtifact(\n  lease: WorkspaceLease,\n  artifact: PatchArtifact,\n  git: GitClient,\n  options: GitRunOptions,\n): Promise<void> {\n  const workspacePath = await realpath(lease.path);\n  const artifactPath = path.resolve(workspacePath, artifact.path);\n  const canonicalArtifactPath = await realpath(artifactPath);\n  if (!canonicalArtifactPath.startsWith(`${workspacePath}${path.sep}`)) {\n    throw new Error(`artifact path escapes workspace: ${artifact.path}`);\n  }\n  const stat = await lstat(canonicalArtifactPath);\n  if (!stat.isFile()) throw new Error(`artifact path is not a regular file: ${artifact.path}`);\n  const actualSha256 = createHash(\"sha256\")\n    .update(await readFile(canonicalArtifactPath))\n    .digest(\"hex\");\n  if (actualSha256 !== artifact.sha256) {\n    throw new Error(\n      `artifact digest mismatch: event ${artifact.sha256}, file ${actualSha256}`,\n    );\n  }\n\n  const changed = await git.run(lease.path, [\n    \"-c\",\n    \"core.hooksPath=/dev/null\",\n    \"-c\",\n    \"core.fsmonitor=false\",\n    \"-c\",\n    \"core.quotepath=off\",\n    \"diff\",\n    \"--name-only\",\n    \"--no-ext-diff\",\n    \"--no-textconv\",\n  ], options);\n  if (changed.termination !== null) {\n    throw new WorkspaceGitTerminationError(\n      changed.termination,\n      \"artifact changed-path read\",\n    );\n  }\n  if (changed.exitCode !== 0 || changed.truncated) {\n    throw new Error(\"could not obtain the complete worker diff path list\");\n  }\n  const changedPaths = changed.stdout.split(\"\\n\").filter(Boolean);\n  if (changedPaths.length !== 1 || changedPaths[0] !== artifact.path) {\n    throw new Error(\"artifact.ready path does not exactly match the worker diff\");\n  }\n}\n\nfunction validateRelativePath(candidate: string): void {\n  if (\n    candidate.includes(\"\\0\") ||\n    candidate.includes(\"\\n\") ||\n    candidate.includes(\"\\r\") ||\n    path.isAbsolute(candidate) ||\n    path.win32.isAbsolute(candidate)\n  ) {\n    throw new Error(`artifact path must be a safe relative path: ${candidate}`);\n  }\n  const segments = candidate.split(/[\\\\/]/);\n  if (\n    segments.some((segment) => segment === \"\" || segment === \".\" || segment === \"..\") ||\n    path.posix.normalize(candidate) !== candidate\n  ) {\n    throw new Error(`artifact path must not contain traversal: ${candidate}`);\n  }\n}\n\nfunction failedValidationOutcome(\n  validation: ValidationReport,\n): Exclude<TerminalOutcome, \"completed\" | \"denied\"> | null {\n  if (validation.outcome !== \"completed\") return validation.outcome;\n  return validation.exitCode === 0 ? null : \"failed\";\n}\n\nasync function validateIntegrationReceipt(input: {\n  readonly project: {\n    readonly projectId: string;\n    readonly repositoryPath: string;\n    readonly integrationBranch: string;\n    readonly validations: { readonly full: readonly string[] };\n  };\n  readonly taskId: string;\n  readonly sourceCommit: string;\n  readonly review: ReviewDecision;\n  readonly receipt: IntegrationReceipt;\n  readonly git: GitClient;\n}): Promise<void> {\n  const { project, taskId, sourceCommit, review, receipt, git } = input;\n  await assertNoGitObjectSubstitution(git, project.repositoryPath, GIT_OPERATION_TIMEOUT_MS);\n  if (!isVerifiedIntegrationReceipt(receipt)) {\n    throw new Error(\"completed integration receipt lacks queue provenance\");\n  }\n  if (\n    receipt.outcome !== \"completed\" ||\n    receipt.taskId !== taskId ||\n    receipt.projectId !== project.projectId ||\n    receipt.sourceCommit !== sourceCommit\n  ) {\n    throw new Error(\"completed integration receipt identity does not match the task\");\n  }\n  if (\n    receipt.review !== review ||\n    !isVerifiedReviewDecision(receipt.review)\n  ) {\n    throw new Error(\"completed integration receipt does not retain verified review provenance\");\n  }\n  if (\n    receipt.originalIntegrationCommit === null ||\n    !/^(?:[0-9a-f]{40}|[0-9a-f]{64})$/.test(receipt.originalIntegrationCommit) ||\n    receipt.resultCommit === null ||\n    !/^(?:[0-9a-f]{40}|[0-9a-f]{64})$/.test(receipt.resultCommit)\n  ) {\n    throw new Error(\"completed integration receipt has no valid integration base or result commit\");\n  }\n  if (\n    receipt.validation.name !== \"full\" ||\n    receipt.validation.outcome !== \"completed\" ||\n    receipt.validation.exitCode !== 0 ||\n    JSON.stringify(receipt.validation.command) !==\n      JSON.stringify(project.validations.full)\n  ) {\n    throw new Error(\"completed integration receipt has invalid full validation evidence\");\n  }\n  canonicalValidationDigest(receipt.validation);\n\n  const integrationRef = `refs/heads/${project.integrationBranch}`;\n  const symbolic = await git.run(\n    project.repositoryPath,\n    [\"--no-replace-objects\", \"symbolic-ref\", \"--quiet\", integrationRef],\n    { timeoutMs: GIT_OPERATION_TIMEOUT_MS },\n  );\n  if (\n    symbolic.termination !== null ||\n    symbolic.truncated ||\n    symbolic.exitCode !== 1\n  ) {\n    throw new Error(\"integration ref must be an exact nonsymbolic ref\");\n  }\n  const head = await git.run(project.repositoryPath, [\n    \"--no-replace-objects\",\n    \"rev-parse\",\n    \"--verify\",\n    `${integrationRef}^{commit}`,\n  ], { timeoutMs: GIT_OPERATION_TIMEOUT_MS });\n  if (\n    head.termination !== null ||\n    head.exitCode !== 0 ||\n    head.truncated ||\n    head.stdout.trim() !== receipt.resultCommit\n  ) {\n    throw new Error(\"integration ref does not match the completed receipt\");\n  }\n  const parents = await git.run(\n    project.repositoryPath,\n    [\"--no-replace-objects\", \"rev-list\", \"--parents\", \"--max-count=1\", receipt.resultCommit],\n    { timeoutMs: GIT_OPERATION_TIMEOUT_MS },\n  );\n  if (\n    parents.termination !== null ||\n    parents.truncated ||\n    parents.exitCode !== 0\n  ) {\n    throw new Error(\"receipt result merge parents could not be inspected\");\n  }\n  const resultShape = parents.stdout.trim().split(/\\s+/);\n  if (\n    resultShape.length !== 3 ||\n    resultShape[0] !== receipt.resultCommit ||\n    resultShape[1] !== receipt.originalIntegrationCommit ||\n    resultShape[2] !== sourceCommit\n  ) {\n    throw new Error(\"receipt result does not have the exact integration-base/source merge shape\");\n  }\n}\n\nfunction signalOutcome(\n  signal: AbortSignal,\n): \"cancelled\" | \"timed_out\" | null {\n  if (!signal.aborted) return null;\n  return signal.reason instanceof DOMException && signal.reason.name === \"TimeoutError\"\n    ? \"timed_out\"\n    : \"cancelled\";\n}\n\nfunction workerFailure(result: WorkerResult): string {\n  const stderr = result.stderr.trim();\n  return `worker outcome was ${result.outcome}${stderr === \"\" ? \"\" : `: ${stderr}`}`;\n}\n\nfunction sha256(value: string): string {\n  return createHash(\"sha256\").update(value, \"utf8\").digest(\"hex\");\n}\n\nfunction errorMessage(error: unknown): string {\n  return error instanceof Error ? error.message : String(error);\n}\n\nfunction errorName(error: unknown): string {\n  return error instanceof Error ? error.name : \"UnknownError\";\n}","lineStart":1,"lineEnd":813,"totalLines":813,"truncated":false}},"title":"src/orchestration/tracer-bullet.ts","time":{"start":1783897851022,"end":1783897851053}},"metadata":{"azure":{"itemId":"fc_02116c1c406775f1016a541efcdd7c8196b359728b0e2ccc22"}},"id":"prt_f5899048c0010J7l5wzEFA3Itx","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5898e54d0012rnV17tx4I2QNi"}}
{"type":"step_finish","timestamp":1783897851201,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5899053f001ModUp6yDMq02Bp","reason":"tool-calls","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","messageID":"msg_f5898e54d0012rnV17tx4I2QNi","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","type":"step-finish","tokens":{"total":24775,"input":12856,"output":500,"reasoning":155,"cache":{"write":0,"read":11264}},"cost":0}}
{"type":"step_start","timestamp":1783897851877,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f589907e40010dmKk7xF44KmTF","messageID":"msg_f5899056d001jsNXuagsaAxAxG","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","type":"step-start"}}
{"type":"tool_use","timestamp":1783897861783,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_SGhInt8dV2AFOtaQI4IOXOhJ","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tsconfig.json"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tsconfig.json</path>\n<type>file</type>\n<content>\n1: {\n2:   \"compilerOptions\": {\n3:     \"target\": \"ES2024\",\n4:     \"module\": \"NodeNext\",\n5:     \"moduleResolution\": \"NodeNext\",\n6:     \"rootDir\": \".\",\n7:     \"outDir\": \"dist\",\n8:     \"strict\": true,\n9:     \"noUncheckedIndexedAccess\": true,\n10:     \"exactOptionalPropertyTypes\": true,\n11:     \"verbatimModuleSyntax\": true,\n12:     \"declaration\": true,\n13:     \"sourceMap\": true,\n14:     \"skipLibCheck\": true\n15:   },\n16:   \"include\": [\"src/**/*.ts\", \"tests/**/*.ts\"]\n17: }\n\n(End of file - total 17 lines)\n</content>","metadata":{"preview":"{\n  \"compilerOptions\": {\n    \"target\": \"ES2024\",\n    \"module\": \"NodeNext\",\n    \"moduleResolution\": \"NodeNext\",\n    \"rootDir\": \".\",\n    \"outDir\": \"dist\",\n    \"strict\": true,\n    \"noUncheckedIndexedAccess\": true,\n    \"exactOptionalPropertyTypes\": true,\n    \"verbatimModuleSyntax\": true,\n    \"declaration\": true,\n    \"sourceMap\": true,\n    \"skipLibCheck\": true\n  },\n  \"include\": [\"src/**/*.ts\", \"tests/**/*.ts\"]\n}","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tsconfig.json","text":"{\n  \"compilerOptions\": {\n    \"target\": \"ES2024\",\n    \"module\": \"NodeNext\",\n    \"moduleResolution\": \"NodeNext\",\n    \"rootDir\": \".\",\n    \"outDir\": \"dist\",\n    \"strict\": true,\n    \"noUncheckedIndexedAccess\": true,\n    \"exactOptionalPropertyTypes\": true,\n    \"verbatimModuleSyntax\": true,\n    \"declaration\": true,\n    \"sourceMap\": true,\n    \"skipLibCheck\": true\n  },\n  \"include\": [\"src/**/*.ts\", \"tests/**/*.ts\"]\n}","lineStart":1,"lineEnd":17,"totalLines":17,"truncated":false}},"title":"tsconfig.json","time":{"start":1783897861778,"end":1783897861781}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb548196885c3236fc2c543e"}},"id":"prt_f58992e91001trN0zxt5ka3cZN","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861783,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_NrA34CHqhDZSqCXQXZSOChaI","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/fixtures/deterministic-reviewer.mjs"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/fixtures/deterministic-reviewer.mjs</path>\n<type>file</type>\n<content>\n1: #!/usr/bin/env node\n2: // Deterministic reviewer fixture for independent review decisions.\n3: // Accepts only: --diff-sha256, --validation-sha256, --worker-id, --reviewer-id\n4: // Emits one JSON line with review decision and exits 0.\n5: \n6: function fail(message) {\n7:   process.stderr.write(`deterministic-reviewer: ${message}\\n`);\n8:   process.exit(1);\n9: }\n10: \n11: const allowedFlags = new Set([\n12:   \"--diff-sha256\",\n13:   \"--validation-sha256\",\n14:   \"--worker-id\",\n15:   \"--reviewer-id\",\n16: ]);\n17: const values = new Map();\n18: \n19: const argv = process.argv.slice(2);\n20: for (let i = 0; i < argv.length; i += 2) {\n21:   const flag = argv[i];\n22:   if (!allowedFlags.has(flag)) {\n23:     fail(`unknown flag: ${flag}`);\n24:   }\n25:   if (values.has(flag)) {\n26:     fail(`duplicate flag: ${flag}`);\n27:   }\n28:   const value = argv[i + 1];\n29:   if (value === undefined) {\n30:     fail(`missing value for flag: ${flag}`);\n31:   }\n32:   values.set(flag, value);\n33: }\n34: \n35: for (const flag of allowedFlags) {\n36:   if (!values.has(flag)) {\n37:     fail(`missing required flag: ${flag}`);\n38:   }\n39: }\n40: \n41: const diffSha256 = values.get(\"--diff-sha256\");\n42: const validationSha256 = values.get(\"--validation-sha256\");\n43: const workerId = values.get(\"--worker-id\");\n44: const reviewerId = values.get(\"--reviewer-id\");\n45: \n46: // Validate that digests are 64-character hex strings\n47: if (!/^[a-f0-9]{64}$/.test(diffSha256)) {\n48:   fail(`--diff-sha256 must be 64-character hex: ${diffSha256}`);\n49: }\n50: if (!/^[a-f0-9]{64}$/.test(validationSha256)) {\n51:   fail(`--validation-sha256 must be 64-character hex: ${validationSha256}`);\n52: }\n53: \n54: // Approval logic: approve if worker != reviewer and both digests are valid hex\n55: const approved = workerId !== reviewerId;\n56: \n57: const decision = {\n58:   reviewerId,\n59:   approved,\n60:   diffSha256,\n61:   validationSha256,\n62:   decidedAt: new Date().toISOString(),\n63:   reason: approved ? \"deterministic approval\" : \"worker and reviewer are the same identity\",\n64: };\n65: \n66: process.stdout.write(`${JSON.stringify(decision)}\\n`);\n67: process.exit(0);\n\n(End of file - total 67 lines)\n</content>","metadata":{"preview":"#!/usr/bin/env node\n// Deterministic reviewer fixture for independent review decisions.\n// Accepts only: --diff-sha256, --validation-sha256, --worker-id, --reviewer-id\n// Emits one JSON line with review decision and exits 0.\n\nfunction fail(message) {\n  process.stderr.write(`deterministic-reviewer: ${message}\\n`);\n  process.exit(1);\n}\n\nconst allowedFlags = new Set([\n  \"--diff-sha256\",\n  \"--validation-sha256\",\n  \"--worker-id\",\n  \"--reviewer-id\",\n]);\nconst values = new Map();\n\nconst argv = process.argv.slice(2);\nfor (let i = 0; i < argv.length; i += 2) {","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/fixtures/deterministic-reviewer.mjs","text":"#!/usr/bin/env node\n// Deterministic reviewer fixture for independent review decisions.\n// Accepts only: --diff-sha256, --validation-sha256, --worker-id, --reviewer-id\n// Emits one JSON line with review decision and exits 0.\n\nfunction fail(message) {\n  process.stderr.write(`deterministic-reviewer: ${message}\\n`);\n  process.exit(1);\n}\n\nconst allowedFlags = new Set([\n  \"--diff-sha256\",\n  \"--validation-sha256\",\n  \"--worker-id\",\n  \"--reviewer-id\",\n]);\nconst values = new Map();\n\nconst argv = process.argv.slice(2);\nfor (let i = 0; i < argv.length; i += 2) {\n  const flag = argv[i];\n  if (!allowedFlags.has(flag)) {\n    fail(`unknown flag: ${flag}`);\n  }\n  if (values.has(flag)) {\n    fail(`duplicate flag: ${flag}`);\n  }\n  const value = argv[i + 1];\n  if (value === undefined) {\n    fail(`missing value for flag: ${flag}`);\n  }\n  values.set(flag, value);\n}\n\nfor (const flag of allowedFlags) {\n  if (!values.has(flag)) {\n    fail(`missing required flag: ${flag}`);\n  }\n}\n\nconst diffSha256 = values.get(\"--diff-sha256\");\nconst validationSha256 = values.get(\"--validation-sha256\");\nconst workerId = values.get(\"--worker-id\");\nconst reviewerId = values.get(\"--reviewer-id\");\n\n// Validate that digests are 64-character hex strings\nif (!/^[a-f0-9]{64}$/.test(diffSha256)) {\n  fail(`--diff-sha256 must be 64-character hex: ${diffSha256}`);\n}\nif (!/^[a-f0-9]{64}$/.test(validationSha256)) {\n  fail(`--validation-sha256 must be 64-character hex: ${validationSha256}`);\n}\n\n// Approval logic: approve if worker != reviewer and both digests are valid hex\nconst approved = workerId !== reviewerId;\n\nconst decision = {\n  reviewerId,\n  approved,\n  diffSha256,\n  validationSha256,\n  decidedAt: new Date().toISOString(),\n  reason: approved ? \"deterministic approval\" : \"worker and reviewer are the same identity\",\n};\n\nprocess.stdout.write(`${JSON.stringify(decision)}\\n`);\nprocess.exit(0);","lineStart":1,"lineEnd":67,"totalLines":67,"truncated":false}},"title":"fixtures/deterministic-reviewer.mjs","time":{"start":1783897861757,"end":1783897861782}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb248196a10b818d51bd4ae8"}},"id":"prt_f58992e7b001rksoW3jgeMh4S0","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861791,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_ifcm6UPJ8VzlxfytSrlgVrku","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/fixtures/content-aware-reviewer.mjs"},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/fixtures/content-aware-reviewer.mjs</path>\n<type>file</type>\n<content>\n1: #!/usr/bin/env node\n2: \n3: import { createHash } from \"node:crypto\";\n4: \n5: if (process.argv.length !== 2) {\n6:   throw new Error(\"review evidence must be provided only through stdin\");\n7: }\n8: \n9: let input = \"\";\n10: process.stdin.setEncoding(\"utf8\");\n11: for await (const chunk of process.stdin) input += chunk;\n12: \n13: const request = JSON.parse(input);\n14: const dangerous = request.diff.includes(\"requireAuthentication = false\");\n15: const decision = {\n16:   reviewerId: request.reviewerId,\n17:   decision: dangerous ? \"deny\" : \"approve\",\n18:   diffSha256: createHash(\"sha256\").update(request.diff, \"utf8\").digest(\"hex\"),\n19:   validationSha256: request.validationSha256,\n20:   decidedAt: new Date().toISOString(),\n21:   reason: dangerous\n22:     ? \"Denied dangerous authentication bypass in the reviewed diff\"\n23:     : `Reviewed exact diff and validation output: ${request.validation.stdout}`,\n24: };\n25: \n26: process.stdout.write(`${JSON.stringify(decision)}\\n`);\n\n(End of file - total 26 lines)\n</content>","metadata":{"preview":"#!/usr/bin/env node\n\nimport { createHash } from \"node:crypto\";\n\nif (process.argv.length !== 2) {\n  throw new Error(\"review evidence must be provided only through stdin\");\n}\n\nlet input = \"\";\nprocess.stdin.setEncoding(\"utf8\");\nfor await (const chunk of process.stdin) input += chunk;\n\nconst request = JSON.parse(input);\nconst dangerous = request.diff.includes(\"requireAuthentication = false\");\nconst decision = {\n  reviewerId: request.reviewerId,\n  decision: dangerous ? \"deny\" : \"approve\",\n  diffSha256: createHash(\"sha256\").update(request.diff, \"utf8\").digest(\"hex\"),\n  validationSha256: request.validationSha256,\n  decidedAt: new Date().toISOString(),","truncated":false,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/fixtures/content-aware-reviewer.mjs","text":"#!/usr/bin/env node\n\nimport { createHash } from \"node:crypto\";\n\nif (process.argv.length !== 2) {\n  throw new Error(\"review evidence must be provided only through stdin\");\n}\n\nlet input = \"\";\nprocess.stdin.setEncoding(\"utf8\");\nfor await (const chunk of process.stdin) input += chunk;\n\nconst request = JSON.parse(input);\nconst dangerous = request.diff.includes(\"requireAuthentication = false\");\nconst decision = {\n  reviewerId: request.reviewerId,\n  decision: dangerous ? \"deny\" : \"approve\",\n  diffSha256: createHash(\"sha256\").update(request.diff, \"utf8\").digest(\"hex\"),\n  validationSha256: request.validationSha256,\n  decidedAt: new Date().toISOString(),\n  reason: dangerous\n    ? \"Denied dangerous authentication bypass in the reviewed diff\"\n    : `Reviewed exact diff and validation output: ${request.validation.stdout}`,\n};\n\nprocess.stdout.write(`${JSON.stringify(decision)}\\n`);","lineStart":1,"lineEnd":26,"totalLines":26,"truncated":false}},"title":"tests/fixtures/content-aware-reviewer.mjs","time":{"start":1783897861770,"end":1783897861790}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb408196ae80b0c16b737902"}},"id":"prt_f58992e7e0010IbwwHUyaACd3a","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861792,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"read","callID":"call_ctOGObQO9d2qH664oudN2EXQ","state":{"status":"completed","input":{"filePath":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","offset":110,"limit":65},"output":"<path>/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts</path>\n<type>file</type>\n<content>\n110:   signalSource: SignalSource = process,\n111:   fixtureAnchor?: URL,\n112: ): Promise<Invocation> {\n113:   let stdout = \"\";\n114:   let stderr = \"\";\n115:   const code = await runCli(argv, {\n116:     stdout: (value) => { stdout += value; },\n117:     stderr: (value) => { stderr += value; },\n118:     signalSource,\n119:     ...(fixtureAnchor === undefined ? {} : { fixtureAnchor }),\n120:   });\n121:   const combined = `${stdout}${stderr}`;\n122:   const lines = combined.trim().split(\"\\n\").filter(Boolean);\n123:   expect(lines).toHaveLength(1);\n124:   return {\n125:     code,\n126:     stdout,\n127:     stderr,\n128:     json: JSON.parse(lines[0]!) as Record<string, unknown>,\n129:   };\n130: }\n131: \n132: function runArguments(\n133:   testFixture: Fixture,\n134:   taskId = \"task-cli\",\n135:   configureReviewer = true,\n136: ): readonly string[] {\n137:   const args = [\n138:     \"task\", \"run\",\n139:     \"--config\", testFixture.configPath,\n140:     \"--database\", testFixture.databasePath,\n141:     \"--task-id\", taskId,\n142:     \"--title\", \"Update greeting\",\n143:     \"--file\", \"greeting.txt\",\n144:     \"--content\", \"hello from CLI\\n\",\n145:   ];\n146:   if (configureReviewer) {\n147:     args.push(\n148:       \"--reviewer-executable\", process.execPath,\n149:       \"--reviewer-argument\", CONTENT_AWARE_REVIEWER,\n150:       \"--reviewer-id\", \"content-reviewer-1\",\n151:     );\n152:   }\n153:   return args;\n154: }\n155: \n156: function fileSha256(filePath: string): string {\n157:   return createHash(\"sha256\").update(readFileSync(filePath)).digest(\"hex\");\n158: }\n159: \n160: function databaseSchema(databasePath: string): unknown[] {\n161:   const database = new Database(databasePath, { readonly: true, fileMustExist: true });\n162:   try {\n163:     return database.prepare(\n164:       \"SELECT type, name, tbl_name, sql FROM sqlite_master ORDER BY type, name\",\n165:     ).all();\n166:   } finally {\n167:     database.close();\n168:   }\n169: }\n170: \n171: function copiedSourceFixtureLayout(): {\n172:   readonly anchor: URL;\n173:   readonly reviewer: string;\n174:   readonly worker: string;\n\n(Showing lines 110-174 of 1053. Use offset=175 to continue.)\n</content>","metadata":{"preview":"  signalSource: SignalSource = process,\n  fixtureAnchor?: URL,\n): Promise<Invocation> {\n  let stdout = \"\";\n  let stderr = \"\";\n  const code = await runCli(argv, {\n    stdout: (value) => { stdout += value; },\n    stderr: (value) => { stderr += value; },\n    signalSource,\n    ...(fixtureAnchor === undefined ? {} : { fixtureAnchor }),\n  });\n  const combined = `${stdout}${stderr}`;\n  const lines = combined.trim().split(\"\\n\").filter(Boolean);\n  expect(lines).toHaveLength(1);\n  return {\n    code,\n    stdout,\n    stderr,\n    json: JSON.parse(lines[0]!) as Record<string, unknown>,\n  };","truncated":true,"loaded":[],"display":{"type":"file","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts","text":"  signalSource: SignalSource = process,\n  fixtureAnchor?: URL,\n): Promise<Invocation> {\n  let stdout = \"\";\n  let stderr = \"\";\n  const code = await runCli(argv, {\n    stdout: (value) => { stdout += value; },\n    stderr: (value) => { stderr += value; },\n    signalSource,\n    ...(fixtureAnchor === undefined ? {} : { fixtureAnchor }),\n  });\n  const combined = `${stdout}${stderr}`;\n  const lines = combined.trim().split(\"\\n\").filter(Boolean);\n  expect(lines).toHaveLength(1);\n  return {\n    code,\n    stdout,\n    stderr,\n    json: JSON.parse(lines[0]!) as Record<string, unknown>,\n  };\n}\n\nfunction runArguments(\n  testFixture: Fixture,\n  taskId = \"task-cli\",\n  configureReviewer = true,\n): readonly string[] {\n  const args = [\n    \"task\", \"run\",\n    \"--config\", testFixture.configPath,\n    \"--database\", testFixture.databasePath,\n    \"--task-id\", taskId,\n    \"--title\", \"Update greeting\",\n    \"--file\", \"greeting.txt\",\n    \"--content\", \"hello from CLI\\n\",\n  ];\n  if (configureReviewer) {\n    args.push(\n      \"--reviewer-executable\", process.execPath,\n      \"--reviewer-argument\", CONTENT_AWARE_REVIEWER,\n      \"--reviewer-id\", \"content-reviewer-1\",\n    );\n  }\n  return args;\n}\n\nfunction fileSha256(filePath: string): string {\n  return createHash(\"sha256\").update(readFileSync(filePath)).digest(\"hex\");\n}\n\nfunction databaseSchema(databasePath: string): unknown[] {\n  const database = new Database(databasePath, { readonly: true, fileMustExist: true });\n  try {\n    return database.prepare(\n      \"SELECT type, name, tbl_name, sql FROM sqlite_master ORDER BY type, name\",\n    ).all();\n  } finally {\n    database.close();\n  }\n}\n\nfunction copiedSourceFixtureLayout(): {\n  readonly anchor: URL;\n  readonly reviewer: string;\n  readonly worker: string;","lineStart":110,"lineEnd":174,"totalLines":1053,"truncated":true}},"title":"tests/orchestration/cli.test.ts","time":{"start":1783897861776,"end":1783897861791}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb4c8196a12554b7abad997a"}},"id":"prt_f58992e8e001yPJ0NUsKEC4BYD","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861869,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"grep","callID":"call_3H4HiXJ1Q44FmjOSYH2HaaOB","state":{"status":"completed","input":{"pattern":"ProcessReviewerAdapter|DeterministicReviewerAdapter|deterministic-reviewer\\.mjs|resolveBundledFixtures","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts","include":"*.{ts,mjs,json}"},"output":"Found 36 matches\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/reviews/reviewer-adapter.ts:\n  Line 105: export class ProcessReviewerAdapter implements ReviewerAdapter {\n\n  Line 275: export class DeterministicReviewerAdapter implements ReviewerAdapter {\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/fixtures/bundled-fixtures.ts:\n  Line 8:   \"deterministic-reviewer.mjs\": \"bf28776ad1e54b7ce7117ae91bec10f19a9c96d551e5e5bfbc3ec7bd950f4745\",\n\n  Line 59: export function resolveBundledFixtures(\n\n  Line 64:     \"deterministic-reviewer.mjs\": resolveBundledFixture(\"deterministic-reviewer.mjs\", anchor),\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/src/cli/main.ts:\n  Line 21:   ProcessReviewerAdapter,\n\n  Line 429:     adapter: new ProcessReviewerAdapter({\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/reviews/deterministic-reviewer-adapter.test.ts:\n  Line 4:   DeterministicReviewerAdapter,\n\n  Line 5:   ProcessReviewerAdapter,\n\n  Line 37:   return path.join(__dirname, \"../../fixtures/deterministic-reviewer.mjs\");\n\n  Line 47: ): ProcessReviewerAdapter {\n\n  Line 48:   return new ProcessReviewerAdapter({\n\n  Line 63: describe(\"DeterministicReviewerAdapter\", () => {\n\n  Line 86:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 108:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 154:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 180:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 199:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 217:     const adapter = new DeterministicReviewerAdapter(supervisor, getFixturePath());\n\n  Line 241:     const adapter = new DeterministicReviewerAdapter(new ProcessSupervisor(), fixture);\n\n  Line 260:     const adapter = new DeterministicReviewerAdapter(new ProcessSupervisor(), fixture);\n\n  Line 283:       const adapter = new DeterministicReviewerAdapter(\n\n  Line 308: describe(\"ProcessReviewerAdapter\", () => {\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts:\n  Line 185:   const reviewer = path.join(fixtureDirectory, \"deterministic-reviewer.mjs\");\n\n  Line 186:   copyFileSync(path.join(root, \"fixtures\", \"deterministic-reviewer.mjs\"), reviewer);\n\n  Line 1035:     expect(builtCli).not.toContain(\"deterministic-reviewer.mjs\");\n\n  Line 1036:     expect(builtCli).not.toContain(\"DeterministicReviewerAdapter\");\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/fixtures/bundled-fixtures.test.ts:\n  Line 23:   \"deterministic-reviewer.mjs\",\n\n  Line 86:     const expected = copied.fixtures[\"deterministic-reviewer.mjs\"];\n\n  Line 92:     expect(() => resolveBundledFixture(\"deterministic-reviewer.mjs\", copied.anchor))\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/tracer-bullet.test.ts:\n  Line 36:   DeterministicReviewerAdapter,\n\n  Line 73: const reviewerFixture = fixturePath(\"deterministic-reviewer.mjs\");\n\n  Line 171:     overrides.reviewer ?? new DeterministicReviewerAdapter(supervisor, reviewerFixture),\n\n  Line 481:       new DeterministicReviewerAdapter(supervisor, reviewerFixture),\n\n  Line 566:       new DeterministicReviewerAdapter(supervisor, reviewerFixture),\n\n  Line 615:       new DeterministicReviewerAdapter(supervisor, reviewerFixture),\n","metadata":{"matches":36,"truncated":false},"title":"ProcessReviewerAdapter|DeterministicReviewerAdapter|deterministic-reviewer\\.mjs|resolveBundledFixtures","time":{"start":1783897861846,"end":1783897861867}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb608196a05de27df1ca2251"}},"id":"prt_f58992ea4001VUuF6lKB6MV9I7","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861869,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"grep","callID":"call_jimGnVpB7gf08EePsN0VY8bv","state":{"status":"completed","input":{"pattern":"validationSha256.*mismatch|validation evidence digest mismatch|diff evidence digest mismatch","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests","include":"*.ts"},"output":"No files found","metadata":{"matches":0,"truncated":false},"title":"validationSha256.*mismatch|validation evidence digest mismatch|diff evidence digest mismatch","time":{"start":1783897861854,"end":1783897861869}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb6881968359cd9aa238d245"}},"id":"prt_f58992ed7001kGAY45whzZqNI1","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"tool_use","timestamp":1783897861946,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"type":"tool","tool":"grep","callID":"call_IPZCMX8oCgITdq1wjwvf9E1m","state":{"status":"completed","input":{"pattern":"reviewer.*(malformed|timeout|output|digest|identity)|task.*review","path":"/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration","include":"*.ts"},"output":"Found 21 matches\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/cli.test.ts:\n  Line 246:     const result = await invoke(runArguments(testFixture, \"task-no-reviewer\", false));\n\n  Line 255:           taskId: \"task-no-reviewer\",\n\n  Line 274:       ...runArguments(testFixture, \"task-dangerous-review\"),\n\n  Line 957:     \"rejects %s before the task journal, reviewer adapter, or worktree is reached\",\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/recovery.test.ts:\n  Line 241:     \"task.review_requested\",\n\n  Line 253:   tasks.append(\"task-9\", \"task.review_approved\", { review }, null);\n\n  Line 508:     tasks.append(\"task-9\", \"task.review_requested\", {\n\n  Line 512:     tasks.append(\"task-9\", \"task.review_approved\", {\n\n  Line 631:     first.tasks.append(\"task-9\", \"task.review_requested\", {\n\n  Line 648:     first.tasks.append(\"task-9\", \"task.review_approved\", { review }, null);\n\n  Line 655:     first.tasks.append(\"task-9\", \"task.integration_started\", { sourceCommit, review }, null);\n\n  Line 713:     tasks.append(\"task-9\", \"task.review_requested\", {\n\n  Line 720:       reason: expect.stringMatching(/reviewer|identity|worker/i),\n\n  Line 722:     expect(journal.readStream(\"task-9\").at(-1)?.type).toBe(\"task.review_requested\");\n\n  Line 758:     tasks.append(\"task-9\", \"task.review_requested\", {\n\n\n/Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts/tests/orchestration/tracer-bullet.test.ts:\n  Line 269:       \"task.review_requested\",\n\n  Line 270:       \"task.review_approved\",\n\n  Line 1181:       taskId: `task-review-${kind}`,\n\n  Line 1191:       \"task.review_requested\",\n\n  Line 1212:       taskId: `task-malformed-review-${_case.replaceAll(\" \", \"-\")}`,\n\n  Line 1232:         taskId: `task-reviewer-${outcome}`,\n","metadata":{"matches":21,"truncated":false},"title":"reviewer.*(malformed|timeout|output|digest|identity)|task.*review","time":{"start":1783897861935,"end":1783897861945}},"metadata":{"azure":{"itemId":"fc_059d4318cf3637f9016a541f07bb788196b27746c40e1e2150"}},"id":"prt_f58992edf001dH93mxV5Ycx1Sn","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","messageID":"msg_f5899056d001jsNXuagsaAxAxG"}}
{"type":"step_finish","timestamp":1783897862095,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f58992fcd001BRf6yVfh1UE2iw","reason":"tool-calls","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","messageID":"msg_f5899056d001jsNXuagsaAxAxG","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","type":"step-finish","tokens":{"total":44718,"input":20365,"output":389,"reasoning":412,"cache":{"write":0,"read":23552}},"cost":0}}
{"type":"step_start","timestamp":1783897862960,"sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","part":{"id":"prt_f5899332f001ophrLuOkNUtA6D","messageID":"msg_f58992fff00185q0jmWa5TsxBs","sessionID":"ses_0a76739dbffeNDKTqZs2Lb8aNR","snapshot":"c2b33f059b54f67d9ac3e40009c2eb87ca4b7b46","type":"step-start"}}
