You are the independent OpenCode CODE-QUALITY and SECURITY reviewer for Zentra issue 009. This is READ-ONLY engineering review of code-review wiring for a local developer CLI. You are a DIFFERENT reviewer than the specification reviewer. Keep reasoning focused on concrete software engineering.

Worktree to review: /Users/talibilat/Documents/Projects/zentra/.worktrees/predeploy-b-review-artifacts
Branch: fix/predeploy-b-review-artifacts

CRITICAL CONSTRAINTS:
- Do NOT use the `skill` tool or invoke ANY skill by any name (they hang here). Use only file reads and read-only bash (git diff, git log, grep, cat).
- Do NOT modify, stage, commit, push, or run any state-changing command. No edits.

Read:
- AGENTS.md
- docs/issues/009-high-require-content-aware-independent-review.md
- docs/execution/issue-009-implementation-report.md
- The complete diff from the branch base to HEAD:
  git diff $(git merge-base fix/pre-deployment HEAD)..HEAD

Review code quality and correctness ONLY: is deny-by-default actually enforced on every path (no fallback that approves)? Is the STDIN framing bounded and safe against truncation/partial writes/deadlock on large diffs? Are digests computed over the exact bytes consumed and compared correctly (no mismatch bypass)? Is schema validation strict and single-decision, failing closed on timeout/malformed output? Can the deterministic identity-only reviewer be selected in a production build through any code path or env var? Process lifetime, stdin backpressure/deadlock, resource bounds, and regression risk. Concurrency and error handling.

Return:
- Quality verdict: PASS or ISSUES_FOUND
- Strengths
- Critical findings (deny-by-default bypass, digest-mismatch bypass, deterministic reviewer selectable in production, stdin deadlock)
- Important findings
- Minor findings
- Required fixes
- Final quality verdict

Every finding must cite exact file and line evidence. Return your review as your final text output.
